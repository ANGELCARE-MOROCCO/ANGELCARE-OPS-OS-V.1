import type { RevenueCommandSchedule } from './types'
export function validateSchedule(schedule:RevenueCommandSchedule){const errors:string[]=[];if(!schedule.code.trim())errors.push('Code planning requis.');if(!schedule.commandCode.trim())errors.push('Commande planning requise.');if(schedule.executionMode!=='shadow'&&schedule.executionMode!=='simulation')errors.push('MZ05 autorise uniquement Shadow ou Simulation pour les plannings.');if(schedule.cadence.split(/\s+/).length!==5)errors.push('Cadence cron invalide.');return{valid:errors.length===0,errors}}
export function isScheduleDue(schedule:RevenueCommandSchedule,now=new Date()){return Boolean(schedule.enabled&&schedule.nextRunAt&&new Date(schedule.nextRunAt).getTime()<=now.getTime())}
export function missedScheduleDecision(schedule:RevenueCommandSchedule,now=new Date()){if(!isScheduleDue(schedule,now))return{action:'wait' as const,reason:'Planification non échue.'};if(schedule.missedRunPolicy==='skip')return{action:'skip' as const,reason:'Exécution manquée ignorée selon politique.'};if(schedule.missedRunPolicy==='reschedule')return{action:'reschedule' as const,reason:'Exécution manquée replanifiée.'};return{action:'run-once' as const,reason:'Une exécution de reprise est autorisée.'}}
