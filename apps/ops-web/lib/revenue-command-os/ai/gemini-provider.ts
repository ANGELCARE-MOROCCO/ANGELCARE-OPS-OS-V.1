import crypto from 'node:crypto'
import {GoogleGenAI,ThinkingLevel} from '@google/genai'
import type {RevenueAiGenerationRequest,RevenueAiGenerationResult,RevenueAiProvider,RevenueAiProviderHealth} from './types'
import type {RevenueStrategy} from '../strategy-brain/types'
import {getRevenueAiConfig,assertGeminiSecret} from './config'
import {STRATEGY_ASSEMBLY_PROMPT} from './prompt-registry'
import {minimizeAiContext} from './context-minimizer'
import {redactSensitiveValues} from './sensitive-value-redactor'
import {GEMINI_STRATEGY_JSON_SCHEMA,geminiStrategyAssembly} from './structured-output'
import {withRevenueAiRetry} from './retry-controller'
import {classifyRevenueAiError,RevenueAiError} from './errors'
const hash=(value:unknown)=>crypto.createHash('sha256').update(typeof value==='string'?value:JSON.stringify(value)).digest('hex')
const now=()=>new Date().toISOString()
export class GeminiRevenueAiProvider implements RevenueAiProvider{readonly providerCode='gemini' as const;private client(){return new GoogleGenAI({apiKey:assertGeminiSecret()})}async checkHealth(live=false):Promise<RevenueAiProviderHealth>{const config=getRevenueAiConfig();try{assertGeminiSecret();if(!config.enabled)return{provider:this.providerCode,enabled:false,available:false,model:config.primaryModel,checkedAt:now(),message:'IA désactivée par configuration.'};if(live){const response=await this.client().models.generateContent({model:config.primaryModel,contents:'Reply exactly GEMINI_OK',config:{maxOutputTokens:256,thinkingConfig:{thinkingLevel:ThinkingLevel.LOW}}});if(!response.text?.includes('GEMINI_OK'))throw new Error('GEMINI_HEALTH_UNEXPECTED_OUTPUT')}return{provider:this.providerCode,enabled:true,available:true,model:config.primaryModel,checkedAt:now(),lastSuccessAt:live?now():undefined,message:live?'Connexion Gemini vérifiée.':'Configuration Gemini prête; test live non demandé.'}}catch(error){const c=classifyRevenueAiError(error);return{provider:this.providerCode,enabled:config.enabled,available:false,model:config.primaryModel,checkedAt:now(),lastFailureAt:now(),errorCode:c.code,message:c.message}}}async generateStructured(request:RevenueAiGenerationRequest):Promise<RevenueAiGenerationResult>{const config=getRevenueAiConfig();if(!config.enabled)throw new RevenueAiError('DISABLED','Revenue AI disabled',false,503);const started=Date.now();const minimized=redactSensitiveValues(minimizeAiContext(request));const serialized=JSON.stringify(minimized);const estimatedInputTokens=Math.ceil(serialized.length/4);if(estimatedInputTokens>config.maxInputTokensPerRun)throw new RevenueAiError('INVALID_OUTPUT',`Input token ceiling exceeded: ${estimatedInputTokens}`,false,413);const requestHash=hash({minimized,prompt:STRATEGY_ASSEMBLY_PROMPT.version,model:config.primaryModel});const response=await withRevenueAiRetry(async attempt=>{const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),config.timeoutMs);try{return await this.client().models.generateContent({model:attempt===0?config.primaryModel:config.fallbackModel,contents:JSON.stringify(minimized),config:{systemInstruction:STRATEGY_ASSEMBLY_PROMPT.content,responseMimeType:'application/json',responseJsonSchema:GEMINI_STRATEGY_JSON_SCHEMA,maxOutputTokens:config.maxOutputTokensPerRun,thinkingConfig:{thinkingLevel:ThinkingLevel.MEDIUM},abortSignal:controller.signal}})}finally{clearTimeout(timer)}},config.maxRetries);const text=response.text;if(!text)throw new RevenueAiError('INVALID_OUTPUT','Gemini returned no text',false,502);let parsed:unknown;try{parsed=JSON.parse(text)}catch(error){throw new RevenueAiError('INVALID_OUTPUT','Gemini returned malformed JSON',false,502,error)}const assembly=geminiStrategyAssembly.safeParse(parsed);if(!assembly.success)throw new RevenueAiError('INVALID_OUTPUT',`Gemini schema validation failed: ${assembly.error.issues.slice(0,8).map(x=>x.path.join('.')+':'+x.message).join('|')}`,false,502);const byCode=new Map(request.commands.map(x=>[x.commandCode,x]));const strategies:RevenueStrategy[]=assembly.data.strategies.map((draft,index)=>{const selected=draft.commandCodes.map(code=>byCode.get(code)).filter((x):x is NonNullable<typeof x>=>Boolean(x));if(selected.length<3)throw new RevenueAiError('INVALID_OUTPUT',`Strategy ${index+1} has fewer than 3 valid command references`,false,502);return{id:crypto.randomUUID(),code:`STRAT-GEMINI-${request.runId.slice(0,8)}-${String(index+1).padStart(2,'0')}`,tenantId:request.tenantId,objectiveId:request.objective.id,contextSnapshotId:request.context.id,archetype:draft.archetype,thesis:draft.thesis,objective:draft.objective,targetMarket:draft.targetMarket,targetSegments:draft.targetSegments,accountProfile:draft.accountProfile,businessProblem:draft.businessProblem,offer:draft.offer,valueProposition:draft.valueProposition,differentiatingAngle:draft.differentiatingAngle,channelMix:draft.channelMix,messageArchitecture:draft.messageArchitecture,campaignSequence:draft.campaignSequence,decisionMakerStrategy:draft.decisionMakerStrategy,meetingStrategy:draft.meetingStrategy,proposalStrategy:draft.proposalStrategy,pricingPosture:draft.pricingPosture,scarcityMechanism:draft.scarcityMechanism,trustEvidence:draft.trustEvidence,resourcesRequired:draft.resourcesRequired,capacityRequirements:draft.capacityRequirements,predictedResults:draft.predictedResults,assumptions:draft.assumptions.map(x=>({...x,id:crypto.randomUUID()})),confidence:draft.confidence,risks:draft.risks.map(x=>({...x,id:crypto.randomUUID()})),fallbackPlan:draft.fallbackPlan,stopConditions:draft.stopConditions,commandPortfolio:selected,scenarios:draft.scenarios.map(x=>({...x,id:crypto.randomUUID()})),status:'ready_for_comparison',version:'1.0.0',combinationLineage:[],assemblyRunId:request.runId,createdAt:now()}});const usageMeta=response.usageMetadata as {promptTokenCount?:number;candidatesTokenCount?:number;totalTokenCount?:number}|undefined;const usage={inputTokens:Number(usageMeta?.promptTokenCount||0),outputTokens:Number(usageMeta?.candidatesTokenCount||0),totalTokens:Number(usageMeta?.totalTokenCount||0),estimatedCostUsd:0};return{provider:this.providerCode,model:config.primaryModel,modelVersion:response.modelVersion,responseId:response.responseId,strategies,usage,latencyMs:Date.now()-started,requestHash,responseHash:hash(text),fallbackUsed:false,rawStatus:'completed'}}}
