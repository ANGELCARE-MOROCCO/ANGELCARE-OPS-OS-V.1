import type {RevenueAiRuntimeConfig,RevenueAiProviderCode} from './types'
const int=(name:string,fallback:number,min=0,max=1_000_000)=>{const value=Number(process.env[name]??fallback);return Number.isFinite(value)?Math.max(min,Math.min(max,Math.trunc(value))):fallback}
const bool=(name:string,fallback:boolean)=>{const raw=process.env[name];return raw===undefined?fallback:['1','true','yes','on'].includes(raw.toLowerCase())}
export function getRevenueAiConfig():RevenueAiRuntimeConfig{const provider=(process.env.REVENUE_OS_AI_PROVIDER||'gemini') as RevenueAiProviderCode;if(!['gemini','deterministic'].includes(provider))throw new Error('REVENUE_AI_PROVIDER_UNSUPPORTED');return{enabled:bool('REVENUE_OS_AI_ENABLED',false),provider,executionMode:'shadow',allowExternalActions:false,primaryModel:String(process.env.GEMINI_PRIMARY_MODEL||'gemini-2.5-flash'),fallbackModel:String(process.env.GEMINI_FALLBACK_MODEL||'gemini-2.5-flash-lite'),minimumStrategies:int('REVENUE_OS_AI_MIN_STRATEGIES',5,5,8),timeoutMs:int('REVENUE_OS_AI_TIMEOUT_MS',120000,5000,300000),maxRetries:int('REVENUE_OS_AI_MAX_RETRIES',2,0,5),maxConcurrentRuns:int('REVENUE_OS_AI_MAX_CONCURRENT_RUNS',2,1,20),maxRequestsPerMinute:int('REVENUE_OS_AI_MAX_REQUESTS_PER_MINUTE',5,1,1000),maxRequestsPerDay:int('REVENUE_OS_AI_MAX_REQUESTS_PER_DAY',100,1,100000),maxInputTokensPerRun:int('REVENUE_OS_AI_MAX_INPUT_TOKENS_PER_RUN',50000,1000,1_000_000),maxOutputTokensPerRun:int('REVENUE_OS_AI_MAX_OUTPUT_TOKENS_PER_RUN',12000,1000,65536),deterministicFallback:bool('REVENUE_OS_AI_DETERMINISTIC_FALLBACK',true),cacheEnabled:bool('REVENUE_OS_AI_CACHE_ENABLED',true),cacheTtlSeconds:int('REVENUE_OS_AI_CACHE_TTL_SECONDS',3600,0,86400),maxRepairAttempts:int('REVENUE_OS_AI_MAX_REPAIR_ATTEMPTS',1,0,2)}}
export function assertGeminiSecret(){const key=process.env.GEMINI_API_KEY;if(!key||key.includes('YOUR_'))throw new Error('GEMINI_API_KEY_MISSING');return key}
