import type {RevenueObjective} from './types';
export type ObjectiveIssue={code:string;severity:'blocker'|'warning';message:string;field?:string};
export function normalizeObjective(input:RevenueObjective){const issues:ObjectiveIssue[]=[]; if(!input.revenueTarget&&!input.successDefinition.length)issues.push({code:'MISSING_TARGET',severity:'blocker',message:'Un objectif mesurable est requis'}); if(input.marginTarget!==undefined&&input.marginTarget>0.8)issues.push({code:'MARGIN_UNREALISTIC',severity:'warning',message:'La marge cible nécessite une preuve explicite'}); if(!input.deadline)issues.push({code:'MISSING_DEADLINE',severity:'warning',message:'Aucune échéance définie'}); if(input.approvedOffers.some(x=>input.excludedOffers.includes(x)))issues.push({code:'OFFER_CONTRADICTION',severity:'blocker',message:'Une offre est simultanément autorisée et exclue'}); return {objective:{...input,title:input.title.trim(),constraints:[...new Set(input.constraints.map(x=>x.trim()).filter(Boolean))]},issues,ready:!issues.some(x=>x.severity==='blocker')};}
