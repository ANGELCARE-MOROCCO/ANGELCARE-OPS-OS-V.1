import type {RevenueStrategy} from './types';
const prohibited=['send_email','send_whatsapp','place_call','commit_price','apply_discount','sign_contract','release_payment'];
export function validateStrategyPreflight(s:RevenueStrategy){const errors:string[]=[];if(s.commandPortfolio.length<3)errors.push('insufficient_command_portfolio');if(s.fallbackPlan.length===0)errors.push('missing_fallback');if(s.stopConditions.length===0)errors.push('missing_stop_conditions');if(s.scenarios.length<3)errors.push('insufficient_scenarios');if(s.confidence<0||s.confidence>1)errors.push('invalid_confidence');const raw=JSON.stringify(s);for(const action of prohibited)if(raw.includes(`"${action}"`))errors.push(`prohibited_action:${action}`);if(!s.pricingPosture['source'])errors.push('pricing_source_missing');if(!s.capacityRequirements.every(x=>x.freshness==='fresh'))errors.push('capacity_not_fresh');return {pass:errors.length===0,errors};}
export function assertStrategySetDiversity(strategies:RevenueStrategy[]){const errors:string[]=[];if(strategies.length<5)errors.push('minimum_five_strategies_required');const archetypes=new Set(strategies.map(x=>x.archetype));if(archetypes.size<5)errors.push('weak_archetype_diversity');const theses=new Set(strategies.map(x=>x.thesis.toLowerCase().replace(/\s+/g,' ').trim()));if(theses.size!==strategies.length)errors.push('duplicate_thesis');return {pass:errors.length===0,errors};}
