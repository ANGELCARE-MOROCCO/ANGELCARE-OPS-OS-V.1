import crypto from 'node:crypto'
import {objectiveSchema} from './schemas'
import {buildContextSnapshot} from './context-assembler'
import {createSupabaseContextAdapters} from './supabase-context-adapters'
import {selectCommandPortfolio,type CandidateCommand} from './command-portfolio'
import {readRevenueCommandKernel} from '../command-kernel/repository'
import {compareStrategies} from './comparison'
import {validateStrategyPreflight} from './guardrails'
import {validateMaterialDiversity} from '../ai/diversity-validator'
import {resolveRevenueAiProvider,resolveDeterministicFallback} from '../ai/provider-registry'
import {getRevenueAiConfig} from '../ai/config'
import {STRATEGY_ASSEMBLY_PROMPT} from '../ai/prompt-registry'
import {assertAiQuota,createAiJob,updateAiJob,recordAiAttempt,persistAiAssembly} from '../ai/repository'
import type {RevenueObjective} from './types'
export async function runGeminiStrategyAssembly(input:{objective:RevenueObjective;userId:string;idempotencyKey?:string}){const objective=objectiveSchema.parse(input.objective);const config=getRevenueAiConfig();const idempotencyKey=input.idempotencyKey||crypto.createHash('sha256').update(`${objective.tenantId}:${objective.id}:${objective.status}`).digest('hex');await assertAiQuota(objective.tenantId,input.userId,{minute:config.maxRequestsPerMinute,day:config.maxRequestsPerDay,concurrency:config.maxConcurrentRuns});const job=await createAiJob({tenantId:objective.tenantId,userId:input.userId,objectiveId:objective.id,idempotencyKey});const runId=String(job.id);try{await updateAiJob(runId,'assembling_context');const context=await buildContextSnapshot(objective,createSupabaseContextAdapters());await updateAiJob(runId,'selecting_commands');const {bootstrap}=await readRevenueCommandKernel();const candidates:CandidateCommand[]=bootstrap.commands.map(c=>({commandCode:c.commandCode,version:c.activeVersion,family:c.family,health:'healthy',status:c.status,segments:c.segments,territories:c.territories,stages:c.commercialStages,requiredContext:c.requiredContext.map(x=>typeof x==='string'?x:String((x as any).key||'')),forbiddenTools:c.toolPermissions.filter(x=>(x as any).access==='forbidden').map(x=>(x as any).tool),performance:.8}));const commands=selectCommandPortfolio(objective,context,candidates);if(commands.length<3)throw new Error('INSUFFICIENT_ELIGIBLE_COMMANDS');const request={runId,tenantId:objective.tenantId,userId:input.userId,objective,context,commands,minimumStrategies:config.minimumStrategies,promptCode:STRATEGY_ASSEMBLY_PROMPT.code,promptVersion:STRATEGY_ASSEMBLY_PROMPT.version,idempotencyKey};await updateAiJob(runId,'calling_provider');let result;let providerError:unknown;try{result=await resolveRevenueAiProvider().generateStructured(request)}catch(error){providerError=error;if(!config.deterministicFallback)throw error;result=await resolveDeterministicFallback().generateStructured(request)}await updateAiJob(runId,'validating_output');for(const strategy of result.strategies){const preflight=validateStrategyPreflight(strategy);if(!preflight.pass)throw new Error(`STRATEGY_PREFLIGHT:${preflight.errors.join('|')}`)}const diversity=validateMaterialDiversity(result.strategies,config.minimumStrategies);if(!diversity.pass)throw new Error(`STRATEGY_DIVERSITY:${diversity.errors.join('|')}`);const comparison=compareStrategies(objective.id,result.strategies);await updateAiJob(runId,'persisting');await persistAiAssembly({objective,context,strategies:result.strategies,comparison,result,userId:input.userId});await recordAiAttempt(request,result,providerError);await updateAiJob(runId,'completed',{completed_at:new Date().toISOString(),result_ref:comparison.id,payload:{provider:result.provider,model:result.model,strategyCount:result.strategies.length,fallbackUsed:result.fallbackUsed,externalActions:0}});return{runId,objective,context,commands,strategies:result.strategies,comparison,provider:{code:result.provider,model:result.model,fallbackUsed:result.fallbackUsed,usage:result.usage,latencyMs:result.latencyMs},externalActions:0}}catch(error){await updateAiJob(runId,'failed',{completed_at:new Date().toISOString(),error_message:error instanceof Error?error.message:String(error)}).catch(()=>undefined);throw error}}
