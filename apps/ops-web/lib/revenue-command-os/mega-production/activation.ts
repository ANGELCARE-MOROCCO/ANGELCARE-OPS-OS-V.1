import type { ActivationLevel, ProductionActivation, SystemMode } from './types'
const levelMode:Record<ActivationLevel,SystemMode>={0:'disabled',1:'shadow',2:'internal_only',3:'draft_generation',4:'approval_required',5:'limited_autopilot',6:'department_autonomy'}
export function validateActivation(input:ProductionActivation,context:{securityCritical:number;regressionFailures:number;disasterRecoveryPassed:boolean;emergencyStop:boolean}):string[]{const issues:string[]=[];if(input.mode!==levelMode[input.level])issues.push('mode_level_mismatch');if(context.emergencyStop&&input.level>0)issues.push('emergency_stop_active');if(context.securityCritical>0&&input.level>=4)issues.push('critical_security_findings');if(context.regressionFailures>0&&input.level>=4)issues.push('regression_failures');if(!context.disasterRecoveryPassed&&input.level>=5)issues.push('disaster_recovery_not_proven');if(input.level>=5&&!input.approvedBy.length)issues.push('executive_approval_required');return issues}
export function modeForLevel(level:ActivationLevel):SystemMode{return levelMode[level]}
