import { hoursSince } from './text'
import type { CognitionState } from './types'
export function chooseFollowup(state:CognitionState){const s=state.signals;if(s.emotional.fatigue>.65)return {strategy:'silence',at:null,reason:'contact_fatigue'};if(s.satisfactionRisk>.6)return {strategy:'relationship_touch',at:new Date(Date.now()+4*3_600_000).toISOString(),reason:'relationship_recovery'};const readiness=s.buyingReadiness;const delayHours=readiness==='decision_ready'?8:readiness==='high_intent'?18:readiness==='evaluating'?30:s.customerType==='b2b'?48:36;const lastInboundAt=String((state.memory as any)?.lastInboundAt||'');const elapsed=hoursSince(lastInboundAt);const remaining=Math.max(2,delayHours-(Number.isFinite(elapsed)?elapsed:0));return {strategy:s.momentum==='stalled'?'new_value_or_question':'contextual_progression',at:new Date(Date.now()+remaining*3_600_000).toISOString(),reason:`readiness:${readiness}`}}
