import type { SupplierSpec } from '../types';import s from '../development.module.css'
export function SupplierSpecs({specs}:{specs:SupplierSpec[]}){return <div className={s.shell}><section className={s.hero}><div className={s.eyebrow}>SUPPLIER SPECIFICATIONS</div><h1 className={s.title}>Exigences produit sans ambiguïté</h1><p className={s.lead}>Les fournisseurs reçoivent des exigences obligatoires, des critères qualité et des matériaux interdits, versionnés et auditables.</p></section><section className={s.panel}><div className={s.list}>{specs.map(x=><div className={s.row} key={x.id}><div><strong>{x.title_fr}</strong><div className={s.subtle}>{x.mandatory_requirements.length} exigences · {x.prohibited_materials.length} interdits</div></div><span>{x.object_type}</span><span className={s.status}>{x.status}</span><span>Mis à jour</span></div>)}{!specs.length&&<div className={s.empty}>Aucune spécification fournisseur enregistrée.</div>}</div></section></div>}
