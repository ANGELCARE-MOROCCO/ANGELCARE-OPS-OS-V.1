import type { DevelopmentActivity } from '../types';import s from '../development.module.css'
export function ActivityRegistry({activities}:{activities:DevelopmentActivity[]}){return <div className={s.shell}><section className={s.hero}><div className={s.eyebrow}>ACTIVITY REGISTRY</div><h1 className={s.title}>Activités cognitives et Montessori</h1><p className={s.lead}>Chaque activité expose son objectif, sa tranche d’âge, sa difficulté, ses matériaux, sa sécurité et son statut de publication.</p></section><section className={s.panel}><div className={s.list}>{activities.map(a=><div className={s.row} key={a.id}><div><strong>{a.title_fr}</strong><div className={s.subtle}>{a.objective_fr}</div></div><span>{a.duration_minutes||'—'} min</span><span className={s.status}>{a.difficulty}</span><span className={s.status}>{a.status}</span></div>)}{!activities.length&&<div className={s.empty}>Aucune activité réelle n’est encore enregistrée.</div>}</div></section></div>}
