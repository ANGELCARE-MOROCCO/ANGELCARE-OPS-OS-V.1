import Link from 'next/link';import styles from '../academy.module.css';import type { AcademyProgram } from '../types'
export function PublicAcademyExperience({programs,locale}:{programs:AcademyProgram[];locale:'fr'|'en'|'ar'}){return <main className={`${styles.shell} ${locale==='ar'?styles.rtl:''}`}><section className={styles.publicHero}><div className={styles.eyebrow}>ANGELCARE ACADEMY</div><h1 className={styles.title}>Des compétences vérifiées pour des services qui inspirent confiance.</h1><p className={styles.copy}>Programmes professionnels, parcours B2B, évaluations et certifications gouvernées par des preuves — jamais par une simple attestation décorative.</p><div className={styles.publicGrid}>{programs.slice(0,6).map(p=><article className={styles.publicCard} key={p.id}><h2>{p.title_fr}</h2><p>{p.description_fr||'Programme de compétence ANGELCARE.'}</p><span>{p.status}</span></article>)}</div><Link href={`/angelcare-marketplace/${locale}/academy/request`}>Demander un parcours Academy →</Link></section></main>}
