# Route Inventory

## Customer
`/[locale]/account`, `/action-center`, `/journeys`, `/journeys/[journeyId]`, `/orders`, `/bookings`, `/enrollments`, `/quotations`, `/subscriptions`, `/assessments`, `/documents`, `/notifications`, `/support`.

## Backoffice
`/admin/journeys` plus action-center, orders, bookings, enrollments, quotations, subscriptions, assessments, fulfillment, documents, notifications, exceptions, recovery, analytics and configuration.
