# Route Integrity Report

The homepage intercepts only the canonical `accueil` slug in the existing localized catch-all. All other CMS and purpose-built routes retain their current resolver. Legacy localized aliases continue to use permanent redirects. Homepage API adapters remain thin and delegate to server handlers.
