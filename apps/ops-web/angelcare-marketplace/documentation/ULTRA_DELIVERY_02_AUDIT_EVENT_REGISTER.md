# Audit Events

Sensitive create and transition operations write marketplace audit evidence with actor, request ID, object, before/after values, reason, source and severity. Domain-specific events include development publication, catalog publication, quote-basket mutation, CRM stage transition, tenant creation and tenant activation/suspension.
