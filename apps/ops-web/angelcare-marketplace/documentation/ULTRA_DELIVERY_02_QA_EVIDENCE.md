# QA Evidence Contract

Required target checks: cumulative MZ01–03 and Ultra1 regressions, Ultra2 static verifier, parser validation, targeted TypeScript, SQL compatibility, permission denial, lifecycle rejection, persistence after refresh, public empty states, territory availability, cross-tenant denial, quote-basket totals, CRM win evidence and tenant activation gates. Runtime acceptance remains pending until target source and SQL are applied.
