# Known Dependencies

Runtime-render adapter requires an authenticated controlled browser harness. Database content scanning requires explicit source registrations. Scheduling uses the platform scheduler when available; manual and protected API execution are delivered now.
