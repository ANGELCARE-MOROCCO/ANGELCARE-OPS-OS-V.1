# Handover

Delivery package contains controlled payload files, installer, verifier, TypeScript project, additive migration, data-preserving rollback, manifest, checksum, documentation and standalone visual blueprints.

Installation order: verify checksum → apply source installer → inspect verifier/TypeScript result → apply SQL manually → validate public journeys and Backoffice queues with real records. No Git or deployment action is included.
