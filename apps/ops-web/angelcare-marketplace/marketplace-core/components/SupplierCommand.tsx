import type { Supplier } from '../types';import s from '../marketplace.module.css'
export function SupplierCommand({suppliers}:{suppliers:Supplier[]}){return <div className={s.shell}><section className={s.hero}><div className={s.eyebrow}>SUPPLIER NETWORK</div><h1 className={s.title}>Fournisseurs, offres et qualité</h1><p className={s.lead}>Aucun produit ne devient disponible sans fournisseur, offre, statut qualité et preuve de conformité exploitables.</p></section><section className={s.grid}>{suppliers.map(x=><article className={s.card} key={x.id}><span className={s.kind}>{x.supplier_code}</span><h3>{x.display_name}</h3><p className={s.subtle}>{x.legal_name} · {x.catalog_offer_count||0} offres</p><span className={s.status}>{x.quality_status}</span></article>)}{!suppliers.length&&<div className={s.empty}>Aucun fournisseur réel enregistré.</div>}</section></div>}
