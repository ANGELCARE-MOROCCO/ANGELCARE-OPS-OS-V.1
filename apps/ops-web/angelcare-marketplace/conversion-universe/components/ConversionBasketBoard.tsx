import Link from 'next/link'
import {ArrowRight,Clock3,FileText,MapPin,ShoppingBag,ShieldCheck} from 'lucide-react'
import type {ConversionBasketRecord} from '../types'
import styles from '../conversion.module.css'

export function ConversionBasketBoard({records}:{records:ConversionBasketRecord[]}){
  return <div className={styles.queueRoot} data-mode="baskets"><section className={styles.queueHero}><div><span>BASKET & QUOTE CONTROL</span><h1>Paniers transactionnels et demandes multi-solutions</h1><p>Chaque panier conserve ses lignes, son statut de prix, son périmètre et sa date d’expiration.</p></div><div><strong>{records.length}</strong><span>paniers visibles</span></div></section><div className={styles.queueToolbar}><div><button type="button">Tous</button><button type="button">Transactionnels</button><button type="button">Sur devis</button><button type="button">Expirants</button></div><Link href="/angelcare-marketplace/admin/conversion">Retour au cockpit <ArrowRight size={16}/></Link></div><section className={styles.queueGrid}>{records.map(record=><article key={record.id} data-status={record.status}><header><div><span>{record.kind}</span><h2>{record.publicReference}</h2></div><ShoppingBag size={25}/></header><div className={styles.queueSignals}><span><MapPin size={14}/>{record.territoryId||'global'}</span><span><Clock3 size={14}/>{record.expiresAt?new Date(record.expiresAt).toLocaleString('fr-FR'):'sans expiration'}</span><span><ShieldCheck size={14}/>{record.pricingStatus}</span></div><div className={styles.queueEvidence}><div><small>Lignes</small><b>{record.lineCount}</b></div><div><small>Sous-total</small><b>{record.subtotal} {record.currencyLabel}</b></div><div><small>Total connu</small><b>{record.grandTotal} {record.currencyLabel}</b></div><div><small>Statut</small><b>{record.status}</b></div></div><footer><span><FileText size={16}/> {record.kind==='quotation'?'proposition':'checkout'}</span><Link href={`/angelcare-marketplace/admin/conversion/sessions?basket=${record.id}`}>Voir les handovers <ArrowRight size={16}/></Link></footer></article>)}{!records.length?<div className={styles.queueEmpty}>Aucun panier réel dans ce périmètre.</div>:null}</section></div>
}
