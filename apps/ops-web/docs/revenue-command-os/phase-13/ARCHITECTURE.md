# Architecture

`MZ12 approval → source eligibility → deterministic blueprint → approval-condition compiler → capacity and assignment checks → dependency graph → conflict detection → persistence → delta/version/rollback → Shadow propagation package`.

Gemini may assist wording only. Source truth, conditions, identities, capacity, assignments, dependencies and readiness are controlled by deterministic AngelCare code.
