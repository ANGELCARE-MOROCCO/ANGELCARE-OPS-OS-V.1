# Known Limitations

External adapter activation requires the relevant Google, WhatsApp or internal module credentials and target endpoints. The release intentionally installs with external actions disabled. MZ16 will add distributed queue scaling, disaster recovery and final adaptive production hardening.
