# Architecture MZ05

Signal Fabric / manual situation → candidate registry → eligibility → context freshness → permission evaluation → approval class → score → deterministic plan → Shadow runtime → output validation → audit/run history.

Published command versions are immutable. Every run plan has a stable hash and idempotency key. External send, discount, contractual and payment tools remain prohibited.
