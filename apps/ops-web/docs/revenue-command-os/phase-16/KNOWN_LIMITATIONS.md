# Known Limitations
Production activation remains explicit. Learning confidence depends on real sample size and clean outcome data. External adapters require their own credentials and health. Infrastructure scaling depends on deployed worker runtime.
