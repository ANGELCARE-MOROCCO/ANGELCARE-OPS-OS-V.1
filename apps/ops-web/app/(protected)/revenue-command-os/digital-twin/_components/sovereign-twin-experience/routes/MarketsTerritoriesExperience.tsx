'use client'

import Link from 'next/link'
import { Compass, Edit3, Gauge, Globe2, MapPin, ShieldAlert, Sparkles } from 'lucide-react'
import { useDigitalTwin } from '../../DigitalTwinContext'
import { marketRecord } from '../twin-entity-mappers'
import type { TwinRouteProps } from '../twin-experience-types'
import { TwinEmpty, TwinPanel, TwinProgress, TwinRouteMasthead, TwinSafetyBanner, TwinStat, TwinStatus, TwinTag, twinExperienceStyles } from '../TwinExperiencePrimitives'

export default function MarketsTerritoriesExperience({ openEditor }: TwinRouteProps) {
  const { twin } = useDigitalTwin()
  const strategic = twin.markets.filter((item) => item.marketMaturity === 'strategic')
  const conditionalOffers = twin.markets.reduce((sum, item) => sum + item.conditionalOfferCodes.length, 0)
  const constraints = twin.markets.reduce((sum, item) => sum + item.deliveryConstraints.length, 0)
  const ranked = [...twin.markets].sort((a, b) => b.priority - a.priority)

  return <div className={`${twinExperienceStyles.routeShell} space-y-5`} data-twin-route-id="MZ25-TWIN-MARKETS-TERRITORIES">
    <TwinRouteMasthead eyebrow="Territory intelligence" title="Marchés & territoires" subtitle="Distinguer où AngelCare peut promouvoir, vendre et livrer immédiatement, sous condition ou pas encore, sans transformer une ambition géographique en promesse opérationnelle." concept="Territory Intelligence Command Map" icon={Globe2} mode={twin.storageMode} freshness={twin.generatedAt} authority="Couverture commerciale · faisabilité explicite" primary={{ label: 'Nouveau marché', onClick: () => openEditor('market') }} secondary={{ label: 'Capacités', href: '/revenue-command-os/digital-twin/capacity-constraints' }}>
      <div className="grid grid-cols-3 gap-2"><TwinStat label="Marchés" value={twin.markets.length} tone="blue"/><TwinStat label="Stratégiques" value={strategic.length} tone="violet"/><TwinStat label="Contraintes" value={constraints} tone={constraints ? 'amber' : 'emerald'}/></div>
    </TwinRouteMasthead>

    <section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><TwinStat label="Villes actives" value={new Set(twin.markets.map((item) => item.city)).size} note="Territoires identifiés" tone="cyan"/><TwinStat label="Offres livrables" value={new Set(twin.markets.flatMap((item) => item.immediatelyDeliverableOfferCodes)).size} note="Disponibilité immédiate déclarée" tone="emerald"/><TwinStat label="Offres conditionnelles" value={conditionalOffers} note="Soumises à des gates" tone={conditionalOffers ? 'amber' : 'emerald'}/><TwinStat label="Fenêtres saisonnières" value={new Set(twin.markets.flatMap((item) => item.seasonalWindows)).size} note="Opportunités temporelles" tone="violet"/></section>

    {twin.markets.length ? <div className="grid gap-5 xl:grid-cols-[1.25fr_.75fr]">
      <section className="grid gap-4 lg:grid-cols-2">{ranked.map((market, index) => <article key={market.code} className="relative overflow-hidden rounded-[30px] border border-slate-200 bg-white p-5 shadow-[0_18px_50px_rgba(15,23,42,.055)]"><div className="absolute right-4 top-3 text-7xl font-black tracking-[-.1em] text-slate-100">0{index + 1}</div><div className="relative"><div className="flex items-start justify-between gap-4"><div className="flex items-center gap-3"><span className="grid h-12 w-12 place-items-center rounded-2xl bg-blue-600 text-white"><MapPin size={21}/></span><div><div className="flex flex-wrap items-center gap-2"><TwinStatus status={market.status}/><TwinTag tone={market.marketMaturity === 'strategic' ? 'violet' : 'blue'}>{market.marketMaturity}</TwinTag></div><h2 className="mt-2 text-lg font-black text-slate-950">{market.city} · {market.region}</h2><p className="mt-1 text-[9px] font-black uppercase tracking-[.12em] text-slate-600">{market.code} · {market.country}</p></div></div><button onClick={() => openEditor('market', marketRecord(market))} aria-label={`Modifier ${market.city}`} className="grid h-10 w-10 place-items-center rounded-xl border border-slate-200 bg-white text-slate-700 hover:border-blue-300 hover:text-blue-800"><Edit3 size={16}/></button></div><div className="mt-5"><TwinProgress value={market.priority} label="Priorité territoriale" tone={market.priority >= 80 ? 'rose' : 'blue'}/></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><div className="rounded-2xl bg-emerald-50 p-3"><p className="text-[8px] font-black uppercase text-emerald-800">Immédiatement livrable</p><p className="mt-1 text-xl font-black text-slate-950">{market.immediatelyDeliverableOfferCodes.length}</p><div className="mt-2 flex flex-wrap gap-1.5">{market.immediatelyDeliverableOfferCodes.slice(0,4).map((code)=><TwinTag key={code} tone="emerald">{code}</TwinTag>)}</div></div><div className="rounded-2xl bg-amber-50 p-3"><p className="text-[8px] font-black uppercase text-amber-800">Sous condition</p><p className="mt-1 text-xl font-black text-slate-950">{market.conditionalOfferCodes.length}</p><div className="mt-2 flex flex-wrap gap-1.5">{market.conditionalOfferCodes.slice(0,4).map((code)=><TwinTag key={code} tone="amber">{code}</TwinTag>)}</div></div></div><div className="mt-4 flex flex-wrap gap-2">{market.zones.map((zone)=><TwinTag key={zone} tone="slate">{zone}</TwinTag>)}</div>{market.deliveryConstraints.length ? <div className="mt-4 rounded-[20px] border border-rose-200 bg-rose-50 p-3"><p className="text-[9px] font-black uppercase text-rose-800">Contraintes de livraison</p><div className="mt-2 space-y-1">{market.deliveryConstraints.map((item)=><p key={item} className="text-[10px] font-semibold leading-4 text-slate-800">• {item}</p>)}</div></div> : null}</div></article>)}</section>
      <div className="space-y-5"><TwinPanel eyebrow="Territory horizon" title="Maturité & pression" icon={Compass} tone="cyan"><div className="space-y-3">{ranked.slice(0,7).map((market)=><div key={market.code} className="rounded-[20px] border border-slate-200 bg-slate-50 p-3"><div className="flex items-center justify-between"><p className="text-xs font-black text-slate-950">{market.city}</p><TwinTag tone={market.priority >= 80 ? 'rose' : 'cyan'}>{market.priority}</TwinTag></div><p className="mt-1 text-[10px] font-semibold text-slate-700">{market.marketMaturity} · {market.activeBusinessUnitCodes.length} unité(s)</p></div>)}</div></TwinPanel><TwinPanel eyebrow="Delivery pressure" title="Gates territoriaux" icon={ShieldAlert} tone="amber"><div className="space-y-3"><div className="rounded-[20px] border border-amber-200 bg-amber-50 p-4"><p className="text-sm font-black text-slate-950">{conditionalOffers} relation(s) offre-territoire conditionnelle(s)</p><p className="mt-1 text-[11px] font-semibold leading-5 text-slate-700">Une relation conditionnelle reste visible mais ne devient jamais une disponibilité ferme.</p></div><div className="rounded-[20px] border border-slate-200 bg-slate-50 p-4"><p className="text-sm font-black text-slate-950">{constraints} contrainte(s) documentée(s)</p><p className="mt-1 text-[11px] font-semibold leading-5 text-slate-700">La pression territoriale doit être reliée aux capacités et dépendances.</p></div></div></TwinPanel><Link href="/revenue-command-os/digital-twin/capacity-constraints" className="flex items-center justify-between rounded-[26px] border border-violet-200 bg-violet-50 p-5 text-violet-950"><div><p className="text-[9px] font-black uppercase">Faisabilité</p><p className="mt-1 text-sm font-black">Vérifier les capacités</p></div><Gauge size={22}/></Link></div>
    </div> : <TwinEmpty title="Aucun marché modélisé" description="Le modèle est sain mais ne contient aucun territoire. Créez un marché et distinguez explicitement ce qui est immédiatement livrable de ce qui reste conditionnel."/>}
    <TwinSafetyBanner detail="La priorité territoriale exprime une importance de modèle; elle n’autorise ni prospection, ni campagne, ni promesse de livraison."/>
  </div>
}
