'use client'

import Link from 'next/link'
import { ArrowRight, Edit3, GitBranch, RefreshCcw, Sparkles, Target, TrendingUp } from 'lucide-react'
import { useDigitalTwin } from '../../DigitalTwinContext'
import { growthPathRecord } from '../twin-entity-mappers'
import type { TwinRouteProps } from '../twin-experience-types'
import { TwinEmpty, TwinPanel, TwinProgress, TwinRouteMasthead, TwinSafetyBanner, TwinStat, TwinStatus, TwinTag, twinExperienceStyles } from '../TwinExperiencePrimitives'

export default function ExpansionRenewalExperience({ openEditor }: TwinRouteProps) {
  const { twin } = useDigitalTwin()
  const byType = (type: string) => twin.growthPaths.filter((item) => item.pathType === type)
  const priorityPaths = [...twin.growthPaths].sort((a,b)=>b.priorityScore-a.priorityScore)
  const offerName = (code: string) => twin.offers.find((item) => item.code === code)?.commercialName || code

  return <div className={`${twinExperienceStyles.routeShell} space-y-5`} data-twin-route-id="MZ25-TWIN-EXPANSION-RENEWAL">
    <TwinRouteMasthead eyebrow="Growth architecture" title="Expansion & renouvellement" subtitle="Structurer les chemins de cross-sell, upsell, renouvellement et recommandation par leurs signaux, règles d’éligibilité, timing et logique réelle." concept="Growth Path Portfolio" icon={TrendingUp} mode={twin.storageMode} freshness={twin.generatedAt} authority="Chemins de croissance · aucun déclenchement externe" primary={{ label: 'Nouveau chemin', onClick: () => openEditor('growth-path') }} secondary={{ label: 'Dépendances', href: '/revenue-command-os/digital-twin/revenue-dependencies' }}>
      <div className="grid grid-cols-3 gap-2"><TwinStat label="Chemins" value={twin.growthPaths.length} tone="violet"/><TwinStat label="Cross-sell" value={byType('cross-sell').length} tone="cyan"/><TwinStat label="Renouvellement" value={byType('renewal').length} tone="emerald"/></div>
    </TwinRouteMasthead>

    <section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><TwinStat label="Upsell" value={byType('upsell').length} note="Montée en gamme" tone="blue"/><TwinStat label="Referral" value={byType('referral').length} note="Recommandation structurée" tone="violet"/><TwinStat label="Signaux déclencheurs" value={new Set(twin.growthPaths.flatMap((item) => item.triggerSignals)).size} note="Conditions observables" tone="cyan"/><TwinStat label="Règles d’éligibilité" value={new Set(twin.growthPaths.flatMap((item) => item.eligibilityRules)).size} note="Gates documentés" tone="amber"/></section>

    {twin.growthPaths.length ? <div className="grid gap-5 xl:grid-cols-[1.2fr_.8fr]">
      <section className="space-y-4">{priorityPaths.map((path) => <article key={path.code} className="rounded-[30px] border border-slate-200 bg-white p-5 shadow-[0_18px_50px_rgba(15,23,42,.055)]"><div className="flex items-start justify-between gap-4"><div><div className="flex flex-wrap items-center gap-2"><TwinStatus status={path.status}/><TwinTag tone={path.pathType === 'renewal' ? 'emerald' : path.pathType === 'upsell' ? 'blue' : 'violet'}>{path.pathType}</TwinTag></div><p className="mt-3 text-[9px] font-black uppercase tracking-[.12em] text-slate-600">{path.code}</p></div><button onClick={() => openEditor('growth-path', growthPathRecord(path))} aria-label={`Modifier ${path.code}`} className="grid h-10 w-10 place-items-center rounded-xl border border-slate-200 text-slate-700 hover:border-violet-300 hover:text-violet-800"><Edit3 size={16}/></button></div><div className="mt-4 grid grid-cols-[1fr_auto_1fr] items-center gap-3 rounded-[24px] bg-slate-950 p-4 text-white"><div><p className="text-[8px] font-black uppercase text-slate-400">Origine</p><p className="mt-1 text-sm font-black text-white">{offerName(path.sourceOfferCode)}</p></div><div className="flex flex-col items-center text-violet-200"><TrendingUp size={20}/><span className="mt-1 text-[8px] font-black uppercase">{path.pathType}</span></div><div className="text-right"><p className="text-[8px] font-black uppercase text-slate-400">Destination</p><p className="mt-1 text-sm font-black text-white">{offerName(path.destinationOfferCode)}</p></div></div><p className="mt-4 text-[11px] font-semibold leading-5 text-slate-700">{path.rationale}</p><div className="mt-4"><TwinProgress value={path.priorityScore} label="Priorité stratégique" tone={path.priorityScore >= 80 ? 'rose' : 'violet'}/></div><div className="mt-4 grid gap-3 sm:grid-cols-2"><div className="rounded-2xl bg-emerald-50 p-3"><p className="text-[8px] font-black uppercase text-emerald-800">Signaux</p><div className="mt-2 flex flex-wrap gap-1.5">{path.triggerSignals.map((item)=><TwinTag key={item} tone="emerald">{item}</TwinTag>)}</div></div><div className="rounded-2xl bg-blue-50 p-3"><p className="text-[8px] font-black uppercase text-blue-800">Éligibilité</p><div className="mt-2 flex flex-wrap gap-1.5">{path.eligibilityRules.map((item)=><TwinTag key={item} tone="blue">{item}</TwinTag>)}</div></div></div><div className="mt-4 rounded-[20px] border border-amber-200 bg-amber-50 p-3"><p className="text-[9px] font-black uppercase text-amber-800">Timing recommandé</p><p className="mt-1 text-[11px] font-semibold leading-5 text-slate-800">{path.recommendedTiming}</p></div></article>)}</section>
      <div className="space-y-5"><TwinPanel eyebrow="Portfolio mix" title="Architecture de croissance" icon={GitBranch} tone="violet"><div className="grid grid-cols-2 gap-3"><div className="rounded-[20px] bg-cyan-50 p-4"><p className="text-[8px] font-black uppercase text-cyan-800">Cross-sell</p><p className="mt-2 text-3xl font-black text-slate-950">{byType('cross-sell').length}</p></div><div className="rounded-[20px] bg-blue-50 p-4"><p className="text-[8px] font-black uppercase text-blue-800">Upsell</p><p className="mt-2 text-3xl font-black text-slate-950">{byType('upsell').length}</p></div><div className="rounded-[20px] bg-emerald-50 p-4"><p className="text-[8px] font-black uppercase text-emerald-800">Renewal</p><p className="mt-2 text-3xl font-black text-slate-950">{byType('renewal').length}</p></div><div className="rounded-[20px] bg-violet-50 p-4"><p className="text-[8px] font-black uppercase text-violet-800">Referral</p><p className="mt-2 text-3xl font-black text-slate-950">{byType('referral').length}</p></div></div></TwinPanel><TwinPanel eyebrow="Eligibility discipline" title="Chemins à sécuriser" icon={Target} tone="amber"><div className="space-y-3">{priorityPaths.slice(0,6).map((path)=><div key={path.code} className="rounded-[20px] border border-slate-200 bg-slate-50 p-3"><div className="flex items-center justify-between"><p className="text-xs font-black text-slate-950">{path.code}</p><TwinTag tone={path.priorityScore >= 80 ? 'rose' : 'violet'}>{path.priorityScore}</TwinTag></div><p className="mt-2 text-[10px] font-semibold text-slate-700">{path.eligibilityRules.length} règle(s) · {path.triggerSignals.length} signal(aux)</p></div>)}</div></TwinPanel><Link href="/revenue-command-os/digital-twin/seasonality" className="flex items-center justify-between rounded-[26px] border border-cyan-200 bg-cyan-50 p-5 text-cyan-950"><div><p className="text-[9px] font-black uppercase">Timing commercial</p><p className="mt-1 text-sm font-black">Vérifier les fenêtres saisonnières</p></div><RefreshCcw size={22}/></Link></div>
    </div> : <TwinEmpty title="Aucun chemin de croissance" description="Le modèle est sain mais ne contient aucun chemin. Ajoutez uniquement des relations fondées sur des signaux et règles d’éligibilité documentés."/>}
    <TwinSafetyBanner detail="Un chemin de croissance ne lance aucune relance, offre ou communication; il structure uniquement une possibilité gouvernée."/>
  </div>
}
