'use client'

import Link from 'next/link'
import { Boxes, CircleDollarSign, Edit3, Gauge, PackagePlus, Plus, Target, UsersRound } from 'lucide-react'
import { useDigitalTwin } from '../../DigitalTwinContext'
import { offerRecord } from '../twin-entity-mappers'
import type { TwinRouteProps } from '../twin-experience-types'
import { TwinEmpty, TwinPanel, TwinRouteMasthead, TwinSafetyBanner, TwinStat, TwinStatus, TwinTag, twinExperienceStyles } from '../TwinExperiencePrimitives'

export default function OffersServicesExperience({ openEditor }: TwinRouteProps) {
  const { twin } = useDigitalTwin()
  const active = twin.offers.filter((item) => item.status === 'active')
  const conditional = twin.offers.filter((item) => item.availability === 'conditional')
  const unavailable = twin.offers.filter((item) => item.availability === 'unavailable')
  const withoutPrice = twin.offers.filter((item) => !twin.priceRules.some((rule) => rule.offerCode === item.code))

  return <div className={`${twinExperienceStyles.routeShell} space-y-5`} data-twin-route-id="MZ25-TWIN-OFFERS-SERVICES">
    <TwinRouteMasthead eyebrow="Offer architecture" title="Offres & services" subtitle="Relier chaque problème client à une proposition de valeur, un format de livraison, un segment, un territoire, une capacité et une règle de prix vérifiables." concept="Offer Architecture Portfolio" icon={Boxes} mode={twin.storageMode} freshness={twin.generatedAt} authority="Portefeuille commercial · aucune promesse externe" primary={{ label: 'Nouvelle offre', onClick: () => openEditor('offer') }} secondary={{ label: 'Prix & marges', href: '/revenue-command-os/digital-twin/pricing-margins' }}>
      <div className="grid grid-cols-3 gap-2"><TwinStat label="Actives" value={active.length} tone="emerald"/><TwinStat label="Conditionnelles" value={conditional.length} tone={conditional.length ? 'amber' : 'emerald'}/><TwinStat label="Sans prix" value={withoutPrice.length} tone={withoutPrice.length ? 'rose' : 'emerald'}/></div>
    </TwinRouteMasthead>

    <section className="grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><TwinStat label="Portefeuille" value={twin.offers.length} note="Offres structurées" tone="cyan"/><TwinStat label="Segments reliés" value={new Set(twin.offers.flatMap((item) => item.targetSegmentCodes)).size} note="Cibles uniques" tone="blue"/><TwinStat label="Capacités requises" value={new Set(twin.offers.flatMap((item) => item.requiredCapacityCodes)).size} note="Gates opérationnels" tone="violet"/><TwinStat label="Indisponibles" value={unavailable.length} note="Ne doivent pas être promises" tone={unavailable.length ? 'rose' : 'emerald'}/></section>

    {twin.offers.length ? <div className="grid gap-5 xl:grid-cols-[1.25fr_.75fr]">
      <section className="space-y-4">{twin.offers.map((offer) => {
        const price = twin.priceRules.find((rule) => rule.offerCode === offer.code)
        const unit = twin.businessUnits.find((item) => item.code === offer.businessUnitCode)
        return <article key={offer.code} className="rounded-[30px] border border-slate-200 bg-white p-5 shadow-[0_18px_50px_rgba(15,23,42,.055)]">
          <div className="grid gap-5 lg:grid-cols-[1fr_220px]"><div><div className="flex flex-wrap items-center gap-2"><TwinStatus status={offer.status}/><TwinStatus status={offer.availability}/><TwinTag tone="cyan">{offer.family}</TwinTag></div><div className="mt-4 flex items-start justify-between gap-4"><div><p className="text-[9px] font-black uppercase tracking-[.12em] text-slate-600">{offer.code} · {unit?.name || offer.businessUnitCode}</p><h2 className="mt-1 text-xl font-black text-slate-950">{offer.commercialName}</h2><p className="mt-2 text-xs font-bold text-slate-700">{offer.customerProblem}</p></div><button onClick={() => openEditor('offer', offerRecord(offer))} aria-label={`Modifier ${offer.commercialName}`} className="grid h-10 w-10 shrink-0 place-items-center rounded-xl border border-slate-200 text-slate-700 hover:border-cyan-300 hover:text-cyan-800"><Edit3 size={16}/></button></div><div className="mt-4 rounded-[22px] border border-cyan-100 bg-cyan-50 p-4"><p className="text-[9px] font-black uppercase text-cyan-800">Proposition de valeur</p><p className="mt-2 text-[11px] font-semibold leading-5 text-slate-800">{offer.valueProposition}</p></div><div className="mt-4 flex flex-wrap gap-2">{offer.targetSegmentCodes.map((code) => <TwinTag key={code} tone="blue"><UsersRound size={10} className="mr-1"/>{code}</TwinTag>)}{offer.requiredCapacityCodes.map((code) => <TwinTag key={code} tone="violet"><Gauge size={10} className="mr-1"/>{code}</TwinTag>)}</div></div>
          <aside className="rounded-[24px] bg-slate-950 p-4 text-white"><p className="text-[9px] font-black uppercase tracking-[.12em] text-cyan-200">Commercial readiness</p><div className="mt-4 space-y-4"><div><p className="text-[8px] font-black uppercase text-slate-400">Cycle</p><p className="mt-1 text-xl font-black text-white">{offer.salesCycleDays} j</p></div><div><p className="text-[8px] font-black uppercase text-slate-400">Price book</p><p className="mt-1 text-sm font-black text-white">{price ? price.priceBook : 'Non défini'}</p></div><div><p className="text-[8px] font-black uppercase text-slate-400">Territoires</p><p className="mt-1 text-xl font-black text-white">{offer.territoryCodes.length}</p></div></div>{price ? <Link href="/revenue-command-os/digital-twin/pricing-margins" className="mt-5 inline-flex w-full items-center justify-center gap-2 rounded-xl bg-white px-3 py-2.5 text-[10px] font-black text-slate-950"><CircleDollarSign size={14}/> Gouvernance prix</Link> : <div className="mt-5 rounded-xl border border-rose-300/30 bg-rose-300/10 p-3 text-[10px] font-black text-rose-100">Prix non calculé — règle requise</div>}</aside></div>
        </article>
      })}</section>
      <div className="space-y-5"><TwinPanel eyebrow="Portfolio pressure" title="Points de vigilance" icon={Target} tone="rose"><div className="space-y-3"><div className="rounded-[20px] border border-amber-200 bg-amber-50 p-4"><p className="text-sm font-black text-slate-950">{withoutPrice.length} offre(s) sans price book</p><p className="mt-1 text-[11px] font-semibold text-slate-700">Le moteur ne doit pas produire de recommandation tarifaire sans règle persistée.</p></div><div className="rounded-[20px] border border-rose-200 bg-rose-50 p-4"><p className="text-sm font-black text-slate-950">{unavailable.length} offre(s) indisponible(s)</p><p className="mt-1 text-[11px] font-semibold text-slate-700">Elles restent visibles mais ne doivent pas être présentées comme livrables.</p></div></div></TwinPanel><TwinPanel eyebrow="Packaging" title="Créer de la valeur combinée" icon={PackagePlus} tone="violet"><p className="text-sm font-semibold leading-6 text-slate-700">Les bundles et relations d’offres structurent l’entrée, la montée en gamme, le cross-sell et la rétention.</p><Link href="/revenue-command-os/digital-twin/bundles-combinations" className="mt-4 inline-flex w-full items-center justify-center gap-2 rounded-2xl bg-violet-600 px-4 py-3 text-xs font-black text-white"><Plus size={15}/> Ouvrir le studio de bundles</Link></TwinPanel></div>
    </div> : <TwinEmpty title="Aucune offre structurée" description="Le modèle est sain mais ne contient aucune proposition commerciale. Créez une offre en conservant un problème client, une valeur, une capacité et une disponibilité explicites."/>}
    <TwinSafetyBanner detail="Une offre visible dans le jumeau n’autorise ni devis, ni communication, ni engagement commercial externe."/>
  </div>
}
