'use client'
import Link from 'next/link'
import { usePathname } from 'next/navigation'
import RevenueOsIcon from '../../_components/RevenueOsIcon'
import { REVENUE_COMMAND_KERNEL_SECTIONS } from '@/lib/revenue-command-os/command-kernel/constants'
import { useCommandKernel } from './CommandKernelContext'
export default function CommandKernelFrame({children}:{children:any}){const path=usePathname();const{data,loading,error,refresh}=useCommandKernel();return <div className="min-h-screen bg-[#f5f8fc] text-slate-950"><div className="border-b border-slate-200 bg-white"><div className="mx-auto max-w-[1680px] px-6 py-6"><div className="flex flex-wrap items-start justify-between gap-5"><div><div className="mb-2 flex items-center gap-2 text-xs font-semibold uppercase tracking-[0.22em] text-blue-700"><RevenueOsIcon name="Command" className="h-4 w-4"/>Revenue Command OS · MZ09</div><h1 className="text-3xl font-semibold tracking-tight">Commandes 3000 · Bibliothèque stratégique complète</h1><p className="mt-2 max-w-4xl text-sm leading-6 text-slate-600">Routage déterministe, éligibilité, graphes, déclencheurs, simulation, versions, permissions et rollback et bibliothèque cumulative de 3 000 commandes, taxonomie canonique exacte, santé, fraîcheur, performance, suppression et couverture et actions externes verrouillées.</p></div><div className="flex items-center gap-3"><div className="rounded-2xl border border-amber-200 bg-amber-50 px-4 py-3"><div className="text-[11px] font-bold uppercase tracking-wider text-amber-800">Posture active</div><div className="mt-1 text-sm font-semibold text-amber-950">Shadow · Actions externes désactivées</div></div><button onClick={()=>void refresh()} className="rounded-xl border border-slate-300 bg-white px-4 py-3 text-sm font-semibold hover:bg-slate-50">Actualiser</button></div></div><div className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-7"><Metric label="Commandes 3000" value={data?.counters.commands3000??'—'}/><Metric label="Finales MZ09" value={data?.counters.mz09Final1000??'—'}/><Metric label="Registre total" value={data?.counters.commands??'—'}/><Metric label="Familles" value={data?.counters.families??'—'}/><Metric label="Déclencheurs" value={data?.counters.triggers??'—'}/><Metric label="Graphes" value={data?.counters.graphs??'—'}/><Metric label="Préparation" value={data?`${data.readiness.overall}%`:'—'}/></div></div></div><div className="border-b border-slate-200 bg-white/90"><nav className="mx-auto flex max-w-[1680px] gap-1 overflow-x-auto px-5 py-2">{REVENUE_COMMAND_KERNEL_SECTIONS.map(s=><Link key={s.key} href={s.href} className={`whitespace-nowrap rounded-lg px-3 py-2 text-xs font-semibold ${path===s.href?'bg-slate-950 text-white':'text-slate-600 hover:bg-slate-100'}`}>{s.label}</Link>)}</nav></div><main className="mx-auto max-w-[1680px] px-6 py-7">{loading?<Panel>Chargement du noyau gouverné…</Panel>:error?<Panel><span className="text-red-700">{error}</span></Panel>:children}</main></div>}
function Metric({label,value}:{label:string;value:any}){return <div className="rounded-2xl border border-slate-200 bg-white px-4 py-3"><div className="text-[11px] font-semibold uppercase tracking-wider text-slate-500">{label}</div><div className="mt-1 text-xl font-semibold">{value}</div></div>}
function Panel({children}:{children:any}){return <div className="rounded-3xl border border-slate-200 bg-white p-8 shadow-sm">{children}</div>}
