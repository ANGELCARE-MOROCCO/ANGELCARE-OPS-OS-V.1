'use client'
import {useEffect,useState} from 'react'
type Health={provider:string;available:boolean;model:string;message:string;checkedAt:string}
export function AiRuntimePanel(){const [health,setHealth]=useState<Health|null>(null);const [usage,setUsage]=useState<any>(null);useEffect(()=>{Promise.all([fetch('/api/revenue-command-os/ai/health',{cache:'no-store'}).then(r=>r.json()),fetch('/api/revenue-command-os/ai/usage',{cache:'no-store'}).then(r=>r.json())]).then(([h,u])=>{setHealth(h.data||null);setUsage(u.data||null)}).catch(()=>undefined)},[]);return <section className="rounded-3xl border border-slate-200 bg-white p-6 shadow-sm"><div className="flex flex-wrap items-center justify-between gap-4"><div><p className="text-xs font-semibold uppercase tracking-[.2em] text-blue-700">Runtime IA MZ10.1</p><h2 className="mt-2 text-xl font-semibold">Gemini · passerelle fournisseur neutre</h2><p className="mt-1 text-sm text-slate-500">Clé serveur, sorties structurées, quotas, fallback et audit. Actions externes désactivées.</p></div><span className={`rounded-full px-4 py-2 text-sm font-semibold ${health?.available?'bg-emerald-50 text-emerald-800':'bg-amber-50 text-amber-800'}`}>{health?.available?'Disponible':'Vérification requise'}</span></div><div className="mt-5 grid gap-3 sm:grid-cols-2 lg:grid-cols-5"><Cell label="Fournisseur" value={health?.provider||'gemini'}/><Cell label="Modèle" value={health?.model||'configuré via env'}/><Cell label="Mode" value="Shadow"/><Cell label="Quota du jour" value={usage?`${usage.requests} requêtes`:'—'}/><Cell label="Actions externes" value="0"/></div><p className="mt-4 text-xs text-slate-500">{health?.message||'Le panneau ne déclenche pas de requête Gemini live automatiquement.'}</p></section>}
function Cell({label,value}:{label:string;value:string}){return <div className="rounded-2xl bg-slate-50 p-4"><p className="text-xs text-slate-500">{label}</p><p className="mt-1 truncate text-sm font-semibold text-slate-900">{value}</p></div>}
