'use client'
import { Activity, Fingerprint, ScrollText, ShieldCheck, UserRoundCheck } from 'lucide-react'
import type { SovereignHeroProps } from '../hero-types'
import { AngelCareHeroBrand, HeroActionRail, HeroAuthorityBadge, HeroExecutiveBrief, HeroMetricGrid, HeroTruthBadge, HeroWarning } from '../HeroPrimitives'
import styles from '../SovereignHeroes.module.css'

export default function AuditHero({ state, posture, authority, summary, metrics = [], actions = [], freshness, warning }: SovereignHeroProps) {
 const rows=['Acteur','Cause','Action','Preuve']
 return <section className={`${styles.hero} min-h-[400px] bg-[linear-gradient(135deg,#f8fafc_0%,#ffffff_52%,#f1f5f9_100%)] p-6 sm:p-8 xl:p-10`} data-hero-surface="light" data-hero-id="MZ22-HERO-17-AUDIT"><div className={`absolute inset-0 opacity-45 ${styles.fineGrid}`}/><div className={`${styles.redTriangle} right-3 bottom-7`}/><div className="relative grid gap-8 xl:grid-cols-[1fr_520px] xl:items-center"><div><div className="flex flex-wrap gap-2"><HeroTruthBadge state={state}/><HeroAuthorityBadge>{authority}</HeroAuthorityBadge><span className="rounded-full border border-slate-300 bg-white px-3 py-2 text-[9px] font-black uppercase tracking-[.12em] text-slate-700">{posture}</span></div><div className="mt-5"><AngelCareHeroBrand /></div><p className="mt-7 text-[10px] font-black uppercase tracking-[.22em] text-slate-600">Causal Ledger</p><h1 className="mt-2 text-4xl font-black tracking-[-.06em] text-slate-950 sm:text-6xl">Audit</h1><p className="mt-4 max-w-2xl text-sm font-semibold leading-7 text-slate-600">Exposer les décisions, acteurs, causes, actions et preuves dans une chronologie institutionnelle précise et immuable.</p><div className="mt-6"><HeroExecutiveBrief>{summary}</HeroExecutiveBrief></div>{freshness ? <p className="mt-3 text-[9px] font-bold uppercase tracking-[.12em] text-slate-400">Évidence récente · {freshness}</p> : null}<HeroWarning>{warning}</HeroWarning><div className="mt-5"><HeroActionRail actions={actions}/></div></div><aside className="relative rounded-[34px] border border-slate-300 bg-white p-5 shadow-[0_24px_70px_rgba(15,23,42,.09)]"><div className="flex items-center justify-between"><div><p className="text-[9px] font-black uppercase tracking-[.16em] text-slate-600">Immutable trace</p><p className="mt-1 text-lg font-black text-slate-950">Chaîne causale</p></div><Fingerprint className="text-slate-700" size={24}/></div><div className="relative mt-5 space-y-2 pl-5"><span className="absolute bottom-2 left-[7px] top-2 w-px bg-slate-300"/>{rows.map((row,index)=><div key={row} className="relative grid grid-cols-[1fr_auto] items-center rounded-2xl border border-slate-200 bg-slate-50 p-3"><span className="absolute -left-[20px] top-1/2 h-3 w-3 -translate-y-1/2 rounded-full border-2 border-white bg-blue-600 shadow"/><div><p className="text-xs font-black text-slate-900">{row}</p><p className="mt-1 font-mono text-[8px] text-slate-400">trace.segment.{index+1}</p></div><span className="text-[8px] font-black uppercase text-emerald-700">Vérifié</span></div>)}</div><div className="mt-4"><HeroMetricGrid metrics={metrics.slice(0,4)}/></div></aside></div></section>
}
