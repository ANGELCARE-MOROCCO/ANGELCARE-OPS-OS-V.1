'use client'
import { BrainCircuit, CheckCircle2, GitCompareArrows, LockKeyhole, Scale, ShieldCheck } from 'lucide-react'
import type { SovereignHeroProps } from '../hero-types'
import { AngelCareHeroBrand, HeroActionRail, HeroAuthorityBadge, HeroExecutiveBrief, HeroMetricGrid, HeroTruthBadge, HeroWarning } from '../HeroPrimitives'
import styles from '../SovereignHeroes.module.css'

export default function StrategiesHero({ state, posture, authority, summary, metrics = [], actions = [], freshness, warning }: SovereignHeroProps) {
 const layers=['Hypothèse','Preuves','Contradictions','Certification']
 return <section className={`${styles.hero} min-h-[420px] bg-[linear-gradient(135deg,#0c1024_0%,#25124d_55%,#18214f_100%)] p-6 text-white sm:p-8 xl:p-10`} data-hero-id="MZ22-HERO-06-STRATEGIES"><div className={`absolute inset-0 opacity-20 ${styles.fineGrid}`}/><div className={`${styles.redTriangle} -right-4 top-7`}/><div className="relative grid gap-8 xl:grid-cols-[1fr_520px] xl:items-center"><div><div className="flex flex-wrap gap-2"><HeroTruthBadge state={state}/><HeroAuthorityBadge inverted>{authority}</HeroAuthorityBadge><span className="rounded-full border border-violet-200/20 bg-violet-300/10 px-3 py-2 text-[9px] font-black uppercase tracking-[.12em] text-violet-100">{posture}</span></div><div className="mt-5"><AngelCareHeroBrand inverted /></div><p className="mt-7 text-[10px] font-black uppercase tracking-[.22em] text-violet-200">Strategic Truth Laboratory</p><h1 className="mt-2 text-4xl font-black tracking-[-.06em] sm:text-6xl">Stratégies</h1><p className="mt-4 max-w-3xl text-sm font-semibold leading-7 text-slate-300">Générer, confronter et qualifier des stratégies concurrentes par la preuve, la contradiction et l’éligibilité au Conseil.</p><div className="mt-6"><HeroExecutiveBrief dark>{summary}</HeroExecutiveBrief></div>{freshness ? <p className="mt-3 text-[9px] font-bold uppercase tracking-[.12em] text-slate-400">Dossier analytique · {freshness}</p> : null}<HeroWarning dark>{warning}</HeroWarning><div className="mt-5"><HeroActionRail actions={actions} dark /></div></div><aside className="relative min-h-[320px] rounded-[34px] border border-violet-200/15 bg-white/[.06] p-5 backdrop-blur-xl"><div className="flex items-center justify-between"><div><p className="text-[9px] font-black uppercase tracking-[.16em] text-violet-200">Reasoning stack</p><p className="mt-1 text-lg font-black">Chaîne de certification</p></div><BrainCircuit className="text-violet-200" size={25}/></div><div className="mt-5 space-y-2">{layers.map((layer,index)=><div key={layer} className={`relative flex items-center gap-3 rounded-2xl border p-3 ${index===2?'border-amber-300/25 bg-amber-300/[.07]':'border-white/10 bg-white/[.045]'}`}><span className="grid h-9 w-9 place-items-center rounded-xl bg-violet-300/10 text-violet-200">{index===0?<BrainCircuit size={16}/>:index===1?<CheckCircle2 size={16}/>:index===2?<GitCompareArrows size={16}/>:<ShieldCheck size={16}/>}</span><div><p className="text-xs font-black">{layer}</p><p className="mt-1 text-[8px] uppercase tracking-[.1em] text-slate-400">Niveau {index+1}</p></div>{index<layers.length-1?<span className="ml-auto h-px w-12 bg-gradient-to-r from-violet-300/40 to-transparent"/>:null}</div>)}</div><div className="mt-4"><HeroMetricGrid metrics={metrics.slice(0,4)} dark /></div></aside></div></section>
}
