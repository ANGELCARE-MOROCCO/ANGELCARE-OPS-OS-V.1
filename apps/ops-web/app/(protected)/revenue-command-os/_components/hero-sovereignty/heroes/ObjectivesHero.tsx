'use client'
import { CalendarClock, Crosshair, Flag, Target, TrendingUp } from 'lucide-react'
import type { SovereignHeroProps } from '../hero-types'
import { AngelCareHeroBrand, HeroActionRail, HeroAuthorityBadge, HeroExecutiveBrief, HeroMetricGrid, HeroTruthBadge, HeroWarning } from '../HeroPrimitives'
import styles from '../SovereignHeroes.module.css'

export default function ObjectivesHero({ state, posture, authority, summary, metrics = [], actions = [], freshness, warning }: SovereignHeroProps) {
  return <section className={`${styles.hero} min-h-[390px] bg-white p-6 sm:p-8 xl:p-10`} data-hero-surface="light" data-hero-id="MZ22-HERO-04-OBJECTIVES"><div className="absolute inset-y-0 right-0 w-[46%] bg-[linear-gradient(135deg,#eff6ff,#f8fafc)]"/><div className={`${styles.redTriangle} right-6 top-10`} /><div className="relative grid gap-8 xl:grid-cols-[1fr_520px] xl:items-center"><div><div className="flex flex-wrap gap-2"><HeroTruthBadge state={state}/><HeroAuthorityBadge>{authority}</HeroAuthorityBadge><span className="rounded-full border border-blue-200 bg-blue-50 px-3 py-2 text-[9px] font-black uppercase tracking-[.12em] text-blue-800">{posture}</span></div><div className="mt-5"><AngelCareHeroBrand /></div><p className="mt-7 text-[10px] font-black uppercase tracking-[.22em] text-blue-700">Revenue Mandate Deck</p><h1 className="mt-2 text-4xl font-black tracking-[-.06em] text-slate-950 sm:text-6xl">Objectifs</h1><p className="mt-4 max-w-2xl text-sm font-semibold leading-7 text-slate-600">Créer, hiérarchiser et gouverner des mandats revenus lisibles avant toute stratégie, compilation ou exécution.</p><div className="mt-6"><HeroExecutiveBrief>{summary}</HeroExecutiveBrief></div>{freshness ? <p className="mt-3 text-[9px] font-bold uppercase tracking-[.12em] text-slate-400">Mandat vérifié · {freshness}</p> : null}<HeroWarning>{warning}</HeroWarning><div className="mt-5"><HeroActionRail actions={actions}/></div></div><div className="relative rounded-[34px] border border-blue-200 bg-white p-6 shadow-[0_24px_70px_rgba(37,99,235,.10)]"><div className="absolute right-5 top-5 grid h-14 w-14 place-items-center rounded-full border-8 border-blue-50 text-blue-700"><Crosshair size={24}/></div><p className="text-[9px] font-black uppercase tracking-[.16em] text-blue-700">Target architecture</p><div className="mt-6 space-y-3">{[['Mandat','Définition'],['Horizon','Temporalité'],['Marché','Périmètre'],['Autorité','Validation']].map(([label,note],index)=><div key={label} className="grid grid-cols-[42px_1fr_auto] items-center gap-3 rounded-2xl border border-slate-200 bg-slate-50/60 p-3"><span className={`grid h-10 w-10 place-items-center rounded-xl ${index===0?'bg-rose-600 text-white':'bg-white text-blue-700 shadow-sm'}`}>{index===0?<Target size={17}/>:index===1?<CalendarClock size={17}/>:index===2?<TrendingUp size={17}/>:<Flag size={17}/>}</span><div><p className="text-xs font-black text-slate-900">{label}</p><p className="mt-1 text-[9px] text-slate-500">{note}</p></div><span className="text-[9px] font-black uppercase text-slate-400">0{index+1}</span></div>)}</div><div className="mt-4"><HeroMetricGrid metrics={metrics.slice(0,4)}/></div></div></div></section>
}
