'use client'
import { LockKeyhole, Network, PlayCircle, ShieldCheck, Workflow, Zap } from 'lucide-react'
import type { SovereignHeroProps } from '../hero-types'
import { AngelCareHeroBrand, HeroActionRail, HeroAuthorityBadge, HeroExecutiveBrief, HeroMetricGrid, HeroTruthBadge, HeroWarning } from '../HeroPrimitives'
import styles from '../SovereignHeroes.module.css'

export default function ExecutionHero({ state, posture, authority, summary, metrics = [], actions = [], freshness, warning }: SovereignHeroProps) {
 return <section className={`${styles.hero} min-h-[420px] bg-[linear-gradient(135deg,#06172a_0%,#0c3150_48%,#0d4b45_100%)] p-6 text-white sm:p-8 xl:p-10`} data-hero-surface="dark" data-hero-id="MZ22-HERO-11-EXECUTION"><div className={`absolute inset-0 opacity-20 ${styles.fineGrid}`}/><div className={`${styles.redTriangle} right-3 bottom-7`}/><div className="relative grid gap-8 xl:grid-cols-[1fr_530px] xl:items-center"><div><div className="flex flex-wrap gap-2"><HeroTruthBadge state={state}/><HeroAuthorityBadge inverted>{authority}</HeroAuthorityBadge><span className="rounded-full border border-emerald-200/20 bg-emerald-300/10 px-3 py-2 text-[9px] font-black uppercase tracking-[.12em] text-emerald-100">{posture}</span></div><div className="mt-5"><AngelCareHeroBrand inverted /></div><p className="mt-7 text-[10px] font-black uppercase tracking-[.22em] text-emerald-200">Execution Control Grid</p><h1 className="mt-2 text-4xl font-black tracking-[-.06em] sm:text-6xl">Exécution</h1><p className="mt-4 max-w-3xl text-sm font-semibold leading-7 text-slate-300">Orchestrer la propagation revenue à haute cadence tout en maintenant les frontières internes, les approbations et le verrou des effets externes.</p><div className="mt-6"><HeroExecutiveBrief dark>{summary}</HeroExecutiveBrief></div>{freshness ? <p className="mt-3 text-[9px] font-bold uppercase tracking-[.12em] text-slate-400">Propagation · {freshness}</p> : null}<HeroWarning dark>{warning}</HeroWarning><div className="mt-5"><HeroActionRail actions={actions} dark /></div></div><aside className="rounded-[34px] border border-white/10 bg-white/[.055] p-5 backdrop-blur-xl"><div className="flex items-center justify-between"><div><p className="text-[9px] font-black uppercase tracking-[.16em] text-emerald-200">Propagation matrix</p><p className="mt-1 text-lg font-black">Nœuds internes sous contrôle</p></div><Workflow className="text-emerald-200" size={24}/></div><div className="mt-5 grid grid-cols-5 gap-2">{Array.from({length:20},(_,index)=><span key={index} className={`grid aspect-square place-items-center rounded-lg border ${index===7||index===13?'border-amber-300/25 bg-amber-300/10 text-amber-200':index>15?'border-rose-300/20 bg-rose-300/[.07] text-rose-200':'border-emerald-300/15 bg-emerald-300/[.06] text-emerald-200'}`}>{index===19?<LockKeyhole size={13}/>:index%4===0?<Network size={13}/>:<span className="h-1.5 w-1.5 rounded-full bg-current"/>}</span>)}</div><div className="mt-4 flex items-center gap-3 rounded-2xl border border-rose-300/15 bg-rose-400/[.08] p-3 text-[9px] font-black text-rose-100"><LockKeyhole size={15}/> External-effect perimeter verrouillé</div><div className="mt-4"><HeroMetricGrid metrics={metrics.slice(0,4)} dark /></div></aside></div></section>
}
