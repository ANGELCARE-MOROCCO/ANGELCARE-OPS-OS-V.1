'use client'
import { Boxes, CheckCircle2, GitBranch, PackageCheck, Settings2, Workflow } from 'lucide-react'
import type { SovereignHeroProps } from '../hero-types'
import { AngelCareHeroBrand, HeroActionRail, HeroAuthorityBadge, HeroExecutiveBrief, HeroMetricGrid, HeroTruthBadge, HeroWarning } from '../HeroPrimitives'
import styles from '../SovereignHeroes.module.css'

export default function CompilationHero({ state, posture, authority, summary, metrics = [], actions = [], freshness, warning }: SovereignHeroProps) {
 const stages=[['Stratégie',GitBranch],['Assembler',Settings2],['Valider',CheckCircle2],['Package',PackageCheck]] as const
 return <section className={`${styles.hero} min-h-[410px] bg-[linear-gradient(125deg,#f8fafc_0%,#eef5ff_50%,#f5f3ff_100%)] p-6 sm:p-8 xl:p-10`} data-hero-surface="light" data-hero-id="MZ22-HERO-10-COMPILATION"><div className={`absolute inset-0 opacity-45 ${styles.fineGrid}`}/><div className={`${styles.redTriangle} -right-4 top-8`}/><div className="relative grid gap-8 xl:grid-cols-[.95fr_1.05fr] xl:items-center"><div><div className="flex flex-wrap gap-2"><HeroTruthBadge state={state}/><HeroAuthorityBadge>{authority}</HeroAuthorityBadge><span className="rounded-full border border-violet-200 bg-violet-50 px-3 py-2 text-[9px] font-black uppercase tracking-[.12em] text-violet-800">{posture}</span></div><div className="mt-5"><AngelCareHeroBrand /></div><p className="mt-7 text-[10px] font-black uppercase tracking-[.22em] text-violet-700">Mission Forge</p><h1 className="mt-2 text-4xl font-black tracking-[-.06em] text-slate-950 sm:text-6xl">Compilation</h1><p className="mt-4 max-w-2xl text-sm font-semibold leading-7 text-slate-600">Transformer une stratégie approuvée en programmes, missions et tâches déterministes, vérifiables et prêts à la propagation.</p><div className="mt-6"><HeroExecutiveBrief>{summary}</HeroExecutiveBrief></div>{freshness ? <p className="mt-3 text-[9px] font-bold uppercase tracking-[.12em] text-slate-400">Compilation · {freshness}</p> : null}<HeroWarning>{warning}</HeroWarning><div className="mt-5"><HeroActionRail actions={actions}/></div></div><aside className="rounded-[34px] border border-violet-200 bg-white p-5 shadow-[0_24px_70px_rgba(109,40,217,.09)]"><div className="flex items-center justify-between"><div><p className="text-[9px] font-black uppercase tracking-[.16em] text-violet-700">Assembly line</p><p className="mt-1 text-lg font-black text-slate-950">Stratégie → package exécutable</p></div><Boxes className="text-violet-700" size={24}/></div><div className="mt-6 grid grid-cols-4 gap-2">{stages.map(([label,Icon],index)=><div key={label} className="relative rounded-[22px] border border-slate-200 bg-slate-50 p-3 text-center"><span className={`mx-auto grid h-10 w-10 place-items-center rounded-xl ${index===0?'bg-slate-950 text-white':index===3?'bg-emerald-100 text-emerald-700':'bg-violet-100 text-violet-700'}`}><Icon size={17}/></span><p className="mt-3 text-[9px] font-black text-slate-800">{label}</p>{index<3?<span className="absolute -right-2 top-7 z-10 h-px w-4 bg-violet-300"/>:null}</div>)}</div><div className="mt-5"><HeroMetricGrid metrics={metrics.slice(0,4)}/></div></aside></div></section>
}
