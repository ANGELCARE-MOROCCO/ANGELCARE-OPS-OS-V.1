import { requireMarketplacePageContext } from '@/angelcare-marketplace/auth/context';import { PartnerCommand } from '@/angelcare-marketplace/partner-os/components/PartnerCommand';import { listPlans,listSubscriptions,listTenants,partnerSummary } from '@/angelcare-marketplace/partner-os/repository'
export default async function Page(){await requireMarketplacePageContext('marketplace.partner_os.admin.view');const [summary,tenants,plans,subscriptions]=await Promise.all([partnerSummary(),listTenants(),listPlans(),listSubscriptions()]);return <PartnerCommand summary={summary} tenants={tenants} plans={plans} subscriptions={subscriptions}/>}
