import { requireMarketplacePageContext } from '@/angelcare-marketplace/auth/context';import { MarketplaceCommand } from '@/angelcare-marketplace/marketplace-core/components/MarketplaceCommand';import { catalogSummary,listCatalogItems,listQuoteBaskets,listSuppliers } from '@/angelcare-marketplace/marketplace-core/repository'
export default async function Page(){const context=await requireMarketplacePageContext('marketplace.catalog.view');const [summary,items,suppliers,baskets]=await Promise.all([catalogSummary(),listCatalogItems(),listSuppliers(),listQuoteBaskets(context)]);return <MarketplaceCommand summary={summary} items={items} suppliers={suppliers} baskets={baskets}/>}
