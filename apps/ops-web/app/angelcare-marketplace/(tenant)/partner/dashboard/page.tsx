import { redirect } from 'next/navigation';import { requireMarketplacePageContext } from '@/angelcare-marketplace/auth/context';import { TenantWorkspace } from '@/angelcare-marketplace/partner-os/components/TenantWorkspace';import { getTenantWorkspace } from '@/angelcare-marketplace/partner-os/repository'
export default async function Page(){const context=await requireMarketplacePageContext('marketplace.partner_os.tenant.access');if(!context.tenantId)redirect('/angelcare-marketplace/access-denied');return <TenantWorkspace data={await getTenantWorkspace({tenantId:context.tenantId,context})}/>}
