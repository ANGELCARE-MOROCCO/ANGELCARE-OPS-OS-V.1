import { fail,ok } from "@/lib/revenue-command-center/canonical-server"
import { revenueAccessFailure } from "@/lib/revenue-command-center/api-access"
import { cleanString } from "@/lib/revenue-command-center/canonical-server"
import { normalizeRiskPayload,partnershipContext,recordPartnershipEvent,updateRow } from "@/lib/revenue-command-center/partnership-enterprise/server"
export async function POST(request:Request){try{const {access,supabase}=await partnershipContext("revenue.partnerships.risks"),body=await request.json(),row=normalizeRiskPayload(body);if(!row.partnership_id)return fail("partnershipId requis.",400);const result=await supabase.from("revenue_partnership_risks").insert({...row,created_by:(access.user as any).id||null,updated_by:(access.user as any).id||null}).select("*").single();if(result.error)return fail(result.error);await recordPartnershipEvent(supabase,{partnershipId:row.partnership_id,eventType:"partnership_risk_created",title:`Risque déclaré : ${row.title}`,payload:body,result:{id:result.data.id},actorId:(access.user as any).id||null,severity:"warning"});return ok({risk:result.data})}catch(error){const access=revenueAccessFailure(error);return access?fail(access.message,access.status):fail(error)}}
export async function PATCH(request:Request){try{const {access,supabase}=await partnershipContext("revenue.partnerships.risks"),body=await request.json(),id=String(body.riskId||"");if(!id)return fail("riskId requis.",400);const data=await updateRow(supabase,"revenue_partnership_risks",id,{status:cleanString(body.status,"resolved"),resolution:cleanString(body.resolution),resolved_by:(access.user as any).id||null,resolved_at:new Date().toISOString(),updated_by:(access.user as any).id||null});return ok({risk:data})}catch(error){const access=revenueAccessFailure(error);return access?fail(access.message,access.status):fail(error)}}
