import { fail,ok } from "@/lib/revenue-command-center/canonical-server";import { executionContext,optionalRows } from "@/lib/revenue-command-center/execution-enterprise/server";import { revenueAccessFailure } from "@/lib/revenue-command-center/api-access";export async function GET(){try{const {supabase}=await executionContext("revenue.tasks.workload");const rows=await optionalRows(supabase,"revenue_task_workload_view","*",q=>q.order("overdue_tasks",{ascending:false}));return ok({workload:rows.rows,schemaAvailable:rows.available})}catch(error){const access=revenueAccessFailure(error);return access?fail(access.message,access.status):fail(error)}}
