import { fail, ok, cleanNumber, cleanString } from "@/lib/revenue-command-center/canonical-server"
import { revenueAccessFailure } from "@/lib/revenue-command-center/api-access"
import { b2cContext, recordB2CEvent } from "@/lib/revenue-command-center/b2c-enterprise/server"
export async function POST(request:Request){try{const {access,supabase}=await b2cContext("revenue.b2c.evidence"),body=await request.json(),caseId=cleanString(body.caseId||body.b2cCaseId);if(!caseId)return fail("caseId requis.",400);const row={b2c_case_id:caseId,evidence_type:cleanString(body.evidenceType),reference:cleanString(body.reference),sensitivity:cleanString(body.sensitivity,"standard"),verification_status:"unverified",created_by:(access.user as any).id||null};if(!row.evidence_type||!row.reference)return fail("Type et référence requis.",400);const result=await supabase.from("revenue_b2c_evidence").insert(row).select("*").single();if(result.error)return fail(result.error);await recordB2CEvent(supabase,{caseId,eventType:"b2c_evidence_recorded",title:`Preuve ajoutée : ${row.evidence_type}`,payload:{sensitivity:row.sensitivity},result:{id:result.data.id},actorId:(access.user as any).id||null});return ok({evidence:result.data})}catch(error){const access=revenueAccessFailure(error);return access?fail(access.message,access.status):fail(error)}}
