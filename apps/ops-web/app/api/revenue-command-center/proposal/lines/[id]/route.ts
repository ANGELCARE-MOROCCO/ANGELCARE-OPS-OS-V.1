import { fail,ok } from "@/lib/revenue-command-center/canonical-server"
import { revenueAccessFailure } from "@/lib/revenue-command-center/api-access"
import { normalizeLinePayload,proposalContext } from "@/lib/revenue-command-center/proposal-enterprise/server"
export async function PATCH(request:Request,{params}:{params:Promise<{id:string}>}){try{const {access,supabase}=await proposalContext("revenue.proposals.manage"),{id}=await params,body=await request.json(),row=normalizeLinePayload(body);const update={label:row.label,description:row.description,quantity:row.quantity,unit_price:row.unit_price,gross_value:row.gross_value,discount_value:row.discount_value,net_value:row.net_value,estimated_cost:row.estimated_cost,gross_margin:row.gross_margin,optional:row.optional,internal_only:row.internal_only,sort_order:row.sort_order,updated_by:(access.user as any).id||null};const {data,error}=await supabase.from("revenue_proposal_line_items").update(update).eq("id",id).is("proposal_version_id",null).select("*").single();if(error)return fail(error);return ok({line:data})}catch(error){const access=revenueAccessFailure(error);return access?fail(access.message,access.status):fail(error)}}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){try{const {supabase}=await proposalContext("revenue.proposals.manage"),{id}=await params,{error}=await supabase.from("revenue_proposal_line_items").delete().eq("id",id).is("proposal_version_id",null);if(error)return fail(error);return ok({deleted:true})}catch(error){const access=revenueAccessFailure(error);return access?fail(access.message,access.status):fail(error)}}
