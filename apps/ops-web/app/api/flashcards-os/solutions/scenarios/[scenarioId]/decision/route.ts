import { NextResponse } from 'next/server'
import { assertFlashcardsApiAccess } from '@/lib/flashcards-os/server/access'
import { actorFromUser, decideScenario } from '@/lib/flashcards-os/solutions/server/repository'
export async function POST(request:Request,{params}:{params:Promise<{scenarioId:string}>}){const access=await assertFlashcardsApiAccess('flashcards_os.approve_solution_scenarios');if(!access.ok)return NextResponse.json({error:access.message},{status:access.status});try{const {scenarioId}=await params;const body=await request.json();const decision=body.decision==='rejected'?'rejected':'selected';return NextResponse.json(await decideScenario(scenarioId,decision,String(body.note||''),actorFromUser(access.user)))}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Scenario decision failed.'},{status:400})}}
