import { NextResponse } from 'next/server'
import { assertFlashcardsApiAccess } from '@/lib/flashcards-os/server/access'
import { actorFromUser, mergeSolutionScenarios } from '@/lib/flashcards-os/solutions/server/repository'
export async function POST(request:Request,{params}:{params:Promise<{requestId:string}>}){const access=await assertFlashcardsApiAccess('flashcards_os.edit_solution_scenarios');if(!access.ok)return NextResponse.json({error:access.message},{status:access.status});try{const {requestId}=await params;const body=await request.json();return NextResponse.json(await mergeSolutionScenarios(requestId,Array.isArray(body.scenarioIds)?body.scenarioIds.map(String):[],String(body.name||'Merged solution'),actorFromUser(access.user)),{status:201})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Scenario merge failed.'},{status:400})}}
