import { NextResponse } from 'next/server'
import { assertFlashcardsApiAccess } from '@/lib/flashcards-os/server/access'
import { vaultDownload } from '@/lib/flashcards-os/production/server/storage-client'
export async function GET(request:Request,{params}:{params:Promise<{objectKey:string}>}){const access=await assertFlashcardsApiAccess('flashcards_os.download_deliverables');if(!access.ok)return NextResponse.json({error:access.message},{status:access.status});try{const {objectKey}=await params;const upstream=await vaultDownload(decodeURIComponent(objectKey),request.headers.get('range'));const headers=new Headers();for(const key of ['content-type','content-length','content-range','accept-ranges','content-disposition','etag']){const value=upstream.headers.get(key);if(value)headers.set(key,value)}return new Response(upstream.body,{status:upstream.status,headers})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Download failed.'},{status:502})}}
