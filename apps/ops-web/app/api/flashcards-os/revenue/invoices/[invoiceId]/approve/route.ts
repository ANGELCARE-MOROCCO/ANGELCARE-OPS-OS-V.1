import { NextResponse } from 'next/server'
import { assertFlashcardsApiAccess } from '@/lib/flashcards-os/server/access'
import { actorFromUser, approveDocument } from '@/lib/flashcards-os/revenue/server/repository'
export async function POST(request:Request,{params}:{params:Promise<{invoiceId:string}>}){const access=await assertFlashcardsApiAccess('flashcards_os.approve_invoices');if(!access.ok)return NextResponse.json({error:access.message},{status:access.status});try{const {invoiceId}=await params;const body=await request.json();return NextResponse.json(await approveDocument('invoice',invoiceId,body.approved!==false,String(body.note||''),actorFromUser(access.user)))}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Invoice approval failed.'},{status:400})}}
