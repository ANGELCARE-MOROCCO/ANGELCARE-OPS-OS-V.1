import { NextResponse } from 'next/server'
import { assertFlashcardsApiAccess } from '@/lib/flashcards-os/server/access'
import { actorFromUser, changeOpportunityStage } from '@/lib/flashcards-os/revenue/server/repository'
export async function POST(request:Request,{params}:{params:Promise<{opportunityId:string}>}){const access=await assertFlashcardsApiAccess('flashcards_os.manage_crm_activities');if(!access.ok)return NextResponse.json({error:access.message},{status:access.status});try{const {opportunityId}=await params;const body=await request.json();return NextResponse.json(await changeOpportunityStage(opportunityId,body.stage,body.probability,actorFromUser(access.user)))}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Stage transition failed.'},{status:400})}}
