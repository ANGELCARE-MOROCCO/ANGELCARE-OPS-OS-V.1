import { NextResponse } from 'next/server'
import { assertFlashcardsApiAccess } from '@/lib/flashcards-os/server/access'
import { actorFromUser, publishCatalogueScenarios } from '@/lib/flashcards-os/catalogue-composer/repository'
export async function POST(request:Request,{params}:{params:Promise<{requestId:string}>}){const {requestId}=await params;const body=await request.json();const universe=body.universe==='b2b'?'b2b':'b2c';const permission=universe==='b2b'?'flashcards_os.publish_b2b_sellables':'flashcards_os.publish_b2c_sellables';const access=await assertFlashcardsApiAccess(permission);if(!access.ok)return NextResponse.json({error:access.message},{status:access.status});try{return NextResponse.json(await publishCatalogueScenarios(requestId,Array.isArray(body.scenarioIds)?body.scenarioIds.map(String):[],universe,actorFromUser(access.user)),{status:201})}catch(error){return NextResponse.json({error:error instanceof Error?error.message:'Catalogue publication failed.'},{status:400})}}
