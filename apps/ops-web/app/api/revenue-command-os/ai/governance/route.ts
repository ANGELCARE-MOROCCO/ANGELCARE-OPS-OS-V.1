import { governRoute } from '@/lib/runtime/governor/route'
import { NextRequest,NextResponse } from 'next/server'
import { getCurrentUser } from '@/lib/getUser'
import { createServiceClient } from '@/lib/supabase/server'
import { aiActorOf,apiError } from '@/lib/revenue-command-os/ai/api-access'
import { estimateAiCostUsd,preflightGovernedAiRequest } from '@/lib/ai-provider-control/governor'
export const runtime='nodejs';export const dynamic='force-dynamic'
async function moduleSummary(){const supabase=(await createServiceClient()) as any;const weekStart=new Date();weekStart.setHours(0,0,0,0);weekStart.setDate(weekStart.getDate()-((weekStart.getDay()+6)%7));const dayStart=new Date();dayStart.setHours(0,0,0,0);const[quota,policies,schedules,requests,usage,reuse]=await Promise.all([supabase.from('ai_provider_quota_policies').select('*').eq('scope_type','module').eq('scope_key','revenue_os').maybeSingle(),supabase.from('ai_provider_command_policies').select('*').eq('module_key','revenue_os').order('workspace_key'),supabase.from('ai_provider_command_schedules').select('*').eq('module_key','revenue_os').order('next_run_at',{ascending:true}),supabase.from('ai_provider_governed_requests').select('*').eq('module_key','revenue_os').order('created_at',{ascending:false}).limit(100),supabase.from('ai_provider_usage_ledger').select('*').eq('module_key','revenue_os').gte('occurred_at',weekStart.toISOString()).order('occurred_at',{ascending:false}).limit(2000),supabase.from('ai_provider_reuse_events').select('*').eq('module_key','revenue_os').gte('created_at',weekStart.toISOString()).order('created_at',{ascending:false}).limit(1000)]);for(const result of[quota,policies,schedules,requests,usage,reuse])if(result.error){const message=String(result.error.message||'');if(message.includes('does not exist')||message.includes('schema cache'))throw new Error('AI_PROVIDER_SOVEREIGNTY_PHASE5_REQUIRED');throw new Error(message)}const usageRows=Array.isArray(usage.data)?usage.data:[];const reuseRows=Array.isArray(reuse.data)?reuse.data:[];const todayRows=usageRows.filter((row:any)=>new Date(row.occurred_at)>=dayStart);const sum=(rows:any[],key:string)=>rows.reduce((total,row)=>total+Number(row[key]||0),0);const requestRows=Array.isArray(requests.data)?requests.data:[];const scheduleRows=Array.isArray(schedules.data)?schedules.data:[];return{generatedAt:new Date().toISOString(),quota:quota.data||null,blockingEnabled:false,commandPolicies:policies.data||[],schedules:scheduleRows,recentRequests:requestRows,rollups:{todayRequests:sum(todayRows,'request_count'),weekRequests:sum(usageRows,'request_count'),todayInputTokens:sum(todayRows,'input_tokens'),weekInputTokens:sum(usageRows,'input_tokens'),todayOutputTokens:sum(todayRows,'output_tokens'),weekOutputTokens:sum(usageRows,'output_tokens'),todayCostUsd:sum(todayRows,'estimated_cost_usd'),weekCostUsd:sum(usageRows,'estimated_cost_usd'),cacheHits:requestRows.filter((row:any)=>row.decision==='REUSE_CACHED').length,joinedRequests:requestRows.filter((row:any)=>row.decision==='JOIN_IN_FLIGHT').length,technicalFailures:requestRows.filter((row:any)=>row.status==='failed').length,avoidedRequests:sum(reuseRows,'avoided_requests'),avoidedTokens:sum(reuseRows,'avoided_input_tokens')+sum(reuseRows,'avoided_output_tokens'),avoidedCostUsd:sum(reuseRows,'avoided_cost_usd'),activeSchedules:scheduleRows.filter((row:any)=>row.enabled&&row.status==='active').length}}}
async function GET__angelcareGovernedImpl(){const user=await getCurrentUser();if(!user)return apiError('UNAUTHENTICATED','Authentification requise.',401);try{return NextResponse.json({ok:true,data:await moduleSummary(),mode:'live',externalActions:true},{headers:{'Cache-Control':'no-store'}})}catch(error){return apiError('AI_CONTROL_UNAVAILABLE',error instanceof Error?error.message:'AI control unavailable',503)}}
async function POST__angelcareGovernedImpl(request:NextRequest){const user=await getCurrentUser();if(!user)return apiError('UNAUTHENTICATED','Authentification requise.',401);try{const body=await request.json() as Record<string,unknown>;const actor=aiActorOf(user,body);const estimatedInputTokens=Math.max(0,Number(body.estimatedInputTokens||0));const estimatedOutputTokens=Math.max(0,Number(body.estimatedOutputTokens||0));const preflight=await preflightGovernedAiRequest({moduleKey:'revenue_os',workspaceKey:String(body.workspaceKey||'*'),capability:String(body.capability||'general') as any,commandCode:String(body.commandCode||body.capability||'REVENUE_AI_GENERAL'),requestedModel:String(body.requestedModel||process.env.GEMINI_PRIMARY_MODEL||'gemini-2.5-flash'),promptVersion:String(body.promptVersion||'')||null,sourceRevision:String(body.sourceRevision||'')||null,requestPayload:body.requestPayload||{},triggerType:String(body.triggerType||'manual') as any,scheduleKey:String(body.scheduleKey||'')||null,actorId:actor.id,estimatedRequests:Math.max(1,Number(body.estimatedRequests||1)),estimatedInputTokens,estimatedOutputTokens,estimatedCostUsd:Math.max(0,Number(body.estimatedCostUsd??estimateAiCostUsd(estimatedInputTokens,estimatedOutputTokens))),forceRefresh:Boolean(body.forceRefresh)});return NextResponse.json({ok:true,data:{...preflight,eligible:true,blocking:false,originalDecision:preflight.decision,decision:['REUSE_CACHED','JOIN_IN_FLIGHT'].includes(String(preflight.decision))?preflight.decision:'EXECUTE_NEW',reason:'TRUSTED_OPERATOR_LIVE'},mode:'live',externalActions:true},{headers:{'Cache-Control':'no-store'}})}catch(error){return apiError('AI_PREFLIGHT_TECHNICAL_FAILURE',error instanceof Error?error.message:'Preflight failed',400)}}

export const GET = governRoute(
  {
    workloadClass: 'ai',
    operation: 'GET:/api/revenue-command-os/ai/governance',
  },
  GET__angelcareGovernedImpl,
)

export const POST = governRoute(
  {
    workloadClass: 'ai',
    operation: 'POST:/api/revenue-command-os/ai/governance',
  },
  POST__angelcareGovernedImpl,
)
