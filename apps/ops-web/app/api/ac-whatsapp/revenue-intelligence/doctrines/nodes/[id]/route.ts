import { NextRequest } from 'next/server'
import { acContext, audit, fail, ok } from '@/lib/ac-whatsapp/server'
export async function PATCH(request:NextRequest,{params}:{params:Promise<{id:string}>}){const context=await acContext(request,'ac-whatsapp.automation.manage');if('error' in context)return context.error;const {id}=await params;const body=await request.json().catch(()=>({}));const allowed=['title','customer_type','service_line','journey_stage','intent_family','trigger_terms','trigger_regex','objective','tone_profile','emotional_strategy','commercial_intensity','action_type','response_guidance','response_variants','qualification_questions','objection_class','proof_options','next_actions','escalation_rules','exclusions','forbidden_claims','success_signals','failure_signals','cross_sell','follow_up_rules','priority','maturity_weight','status'];const patch:any={updated_by:context.user.id,updated_at:new Date().toISOString()};for(const key of allowed)if(Object.prototype.hasOwnProperty.call(body,key))patch[key]=body[key];const before=await context.supabase.from('ac_whatsapp_ri_doctrine_nodes').select('*').eq('id',id).maybeSingle();if(before.error)return fail(before.error.message,500);if(!before.data)return fail('DOCTRINE_NODE_NOT_FOUND',404);const updated=await context.supabase.from('ac_whatsapp_ri_doctrine_nodes').update(patch).eq('id',id).select('*').single();if(updated.error)return fail(updated.error.message,500);await audit(context,{action:'revenue.doctrine_node.update',entityType:'ri_doctrine_node',entityId:id,previousState:before.data,newState:updated.data,reason:String(body.reason||'Doctrine editor update')});return ok(updated.data)}
