import { apiError,apiOk } from '@/lib/homeservice-design/server/api'; import { requireHomeServiceApi } from '@/lib/homeservice-design/server/auth'; import { searchServiceDesign } from '@/lib/homeservice-design/server/repository'; export async function GET(r:Request){try{await requireHomeServiceApi('homeservice_design.view');const q=new URL(r.url).searchParams.get('q')||'';return apiOk(await searchServiceDesign(q))}catch(e){return apiError(e)}}
