import { apiError,apiOk,jsonBody } from '@/lib/homeservice-design/server/api'; import { decisionPermission,requireHomeServiceApi } from '@/lib/homeservice-design/server/auth'; import { decideApproval } from '@/lib/homeservice-design/server/repository'; import type { HsdDecision } from '@/types/homeservice-design'; export async function POST(r:Request,{params}:{params:Promise<{id:string}>}){try{const body=await jsonBody(r) as {decision:HsdDecision};const u=await requireHomeServiceApi(decisionPermission(body.decision));const {id}=await params;const x=await decideApproval(id,body.decision,body,u);return apiOk(x.data,200,x.correlationId)}catch(e){return apiError(e)}}
