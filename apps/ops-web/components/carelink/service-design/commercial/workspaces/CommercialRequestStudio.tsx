'use client';import{useState}from'react';import{CUSTOMER_SEGMENTS,OBJECTIVES,INTENTS}from'@/lib/homeservice-commercial/options';import{Action,Hero,Rule,Surface}from'../CommercialUI'
export function CommercialRequestStudio(){const[u,setU]=useState<'b2c'|'b2b'>('b2c'),[count,setCount]=useState(3);return <div className="space-y-6"><Hero eyebrow="Offer Request Studio" title="Brief commercial contrôlé" description="Une source technique approuvée, un marché, des contraintes et jusqu’à dix alternatives. Toute proposition repasse ensuite par le calcul déterministe." actions={<Action href="/carelink-ops/service-design/offers/new" tone="emerald">Créer une demande réelle</Action>}/><div className="grid gap-6 xl:grid-cols-[1.4fr_.8fr]"><Surface title="Architecture de la demande" subtitle="Les sélections structurées remplacent le brief libre."><div className="grid gap-5 md:grid-cols-2"><label className="space-y-2"><span className="text-xs font-black">Univers</span><div className="grid grid-cols-2 gap-2">{(['b2c','b2b']as const).map(x=><button key={x} onClick={()=>setU(x)} className={`rounded-2xl border px-4 py-3 text-sm font-black ${u===x?'border-blue-600 bg-blue-600 text-white':'border-slate-200'}`}>{x.toUpperCase()}</button>)}</div></label><label className="space-y-2"><span className="text-xs font-black">Plan technique approuvé</span><select className="w-full rounded-2xl border border-slate-200 px-4 py-3 text-sm font-bold"><option>Sélection UMZ2 requise</option></select></label><label className="space-y-2"><span className="text-xs font-black">Segment</span><select className="w-full rounded-2xl border border-slate-200 px-4 py-3 text-sm font-bold">{CUSTOMER_SEGMENTS.filter(x=>x.code.startsWith(u)).map(x=><option key={x.code}>{x.label}</option>)}</select></label><label className="space-y-2"><span className="text-xs font-black">Objectif</span><select className="w-full rounded-2xl border border-slate-200 px-4 py-3 text-sm font-bold">{OBJECTIVES.map(x=><option key={x.code}>{x.label}</option>)}</select></label><label className="space-y-2 md:col-span-2"><span className="flex justify-between text-xs font-black"><span>Scénarios</span><span>{count}/10</span></span><input type="range" min="1" max="10" value={count} onChange={e=>setCount(+e.target.value)} className="w-full"/></label></div><div className="mt-6 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{INTENTS.slice(0,count).map(x=><div key={x} className="rounded-2xl border border-slate-200 bg-slate-50 p-4 text-xs font-black uppercase tracking-[.12em]">{x.replaceAll('_',' ')}</div>)}</div></Surface><Surface title="Garde-fous" subtitle="La demande ne devient générable qu’après validation."><div className="space-y-3"><Rule title="Source technique approuvée" detail="Version UMZ2 approuvée et non supersédée." tone="emerald"/><Rule title="Coût absent ≠ zéro" detail="Une absence reste visible et bloque la publication." tone="amber"/><Rule title="IA sans autorité financière" detail="OpenRouter Free compose, le serveur calcule."/><Rule title="Aucune mission CARELINK" detail="UMZ3 crée des références, pas des missions." tone="rose"/></div></Surface></div></div>}
