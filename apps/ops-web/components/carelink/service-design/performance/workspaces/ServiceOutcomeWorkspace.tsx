import{performanceData}from'../load'
import{PerformanceWorkspace,Panel,EmptyState,MetricCard,Badge,SignalList,CaseList,DecisionRunway,HealthMatrix,ReadinessRail,WarningBanner}from'../PerformanceUI'
export default async function ServiceOutcomeWorkspace(){
 const data=await performanceData()
 return <PerformanceWorkspace data={data} eyebrow="Résultats service" title="Service Outcome Intelligence Studio" description="Mesurer les résultats opérationnels propres à chaque catégorie sans inventer de promesse thérapeutique." variant="studio" primary={<><Panel title="Matrice de résultats par catégorie" subtitle="Sécurité, couverture, engagement, routine et handover selon le service."><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{['Couverture sûre','Routine maintenue','Programme réalisé','Handover confirmé','Objectif client','Rebooking'].map((x,i)=><article key={x} className="rounded-[24px] border border-slate-200 bg-white p-5"><p className="text-[10px] font-black uppercase tracking-[.2em] text-slate-400">Dimension {i+1}</p><h3 className="mt-2 font-black text-slate-950">{x}</h3><p className="mt-2 text-xs font-semibold leading-5 text-slate-500">Définition configurable par catégorie; aucune mesure n’est inventée avant preuve.</p></article>)}</div></Panel></>} aside={<><><Panel title="Autorité humaine" subtitle="Les décisions restent séparées de l’analyse."><div className="space-y-3"><div className="rounded-xl bg-slate-950 p-4 text-white"><p className="text-xs font-black">OpenRouter Free · consultatif</p><p className="mt-2 text-[11px] font-semibold text-slate-300">Aucune approbation, suspension, jugement RH ou certification.</p></div><div className="rounded-xl border border-emerald-200 bg-emerald-50 p-4"><p className="text-xs font-black text-emerald-900">CARELINK · vérité terrain</p><p className="mt-2 text-[11px] font-semibold text-emerald-700">Missions, présence, rapports, incidents et paiements restent souverains.</p></div></div></Panel><Panel title="Readiness" subtitle="Evidence before confidence."><ReadinessRail data={data}/></Panel></></>}/>
}
