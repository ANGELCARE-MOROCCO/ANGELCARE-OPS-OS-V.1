'use client'

import { ArrowRight, CalendarRange, Clock3, PackageCheck, Sparkles } from 'lucide-react'
import type { CategoryExperienceBlueprint, CategoryExperiencePreset } from '@/types/homeservice-category-experience'
import { cx } from '../FactoryUI'

export function PresetGallery({ blueprint, selectedCode, onSelect }: { blueprint: CategoryExperienceBlueprint; selectedCode: string; onSelect: (preset: CategoryExperiencePreset) => void }) {
  return <section className="rounded-[30px] border border-slate-200 bg-white p-5 shadow-[0_18px_55px_rgba(15,23,42,.06)] sm:p-6"><div className="flex flex-wrap items-end justify-between gap-4"><div><p className="text-[10px] font-black uppercase tracking-[.22em] text-blue-600">Étape 1 · Choisir un scénario</p><h2 className="mt-1 text-2xl font-black tracking-[-.04em] text-slate-950">Configurations prêtes pour {blueprint.categoryName}</h2><p className="mt-2 max-w-3xl text-xs font-semibold leading-5 text-slate-500">Le scénario préremplit tout le dossier. Vous pouvez ajuster des sélections, mais aucune rédaction n’est demandée.</p></div><span className="rounded-full bg-emerald-50 px-4 py-2 text-[10px] font-black uppercase tracking-[.14em] text-emerald-700">{blueprint.presets.length} scénarios complets</span></div>
    <div className="mt-5 grid gap-4 md:grid-cols-2 xl:grid-cols-3">{blueprint.presets.map((preset) => { const active = selectedCode === preset.code; return <button key={preset.code} type="button" onClick={() => onSelect(preset)} className={cx('group rounded-[24px] border p-4 text-left transition', active ? 'border-slate-950 bg-slate-950 text-white shadow-[0_20px_45px_rgba(15,23,42,.24)]' : 'border-slate-200 bg-slate-50 hover:-translate-y-0.5 hover:border-slate-400 hover:bg-white')}><div className="flex items-start justify-between gap-3"><span className={cx('rounded-full px-2.5 py-1 text-[9px] font-black uppercase tracking-[.12em]', active ? 'bg-white/15 text-white' : 'bg-blue-50 text-blue-700')}>{preset.badge}</span>{active ? <PackageCheck size={18} /> : <ArrowRight size={18} className="text-slate-400 transition group-hover:translate-x-1" />}</div><h3 className="mt-4 text-base font-black">{preset.name}</h3><p className={cx('mt-2 text-[11px] font-semibold leading-5', active ? 'text-slate-300' : 'text-slate-500')}>{preset.description}</p><div className={cx('mt-4 flex flex-wrap gap-2 text-[9px] font-black uppercase tracking-[.1em]', active ? 'text-slate-300' : 'text-slate-500')}><span className="inline-flex items-center gap-1"><CalendarRange size={12} />{preset.defaultDayCount} jour(s)</span><span className="inline-flex items-center gap-1"><Clock3 size={12} />{preset.defaultStartTime}–{preset.defaultEndTime}</span><span className="inline-flex items-center gap-1"><Sparkles size={12} />{preset.scenarioCount} options</span></div></button> })}</div>
  </section>
}
