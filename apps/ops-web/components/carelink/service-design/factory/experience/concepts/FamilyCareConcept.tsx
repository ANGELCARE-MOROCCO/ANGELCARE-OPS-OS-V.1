import { HeartHandshake, Boxes, ShieldCheck } from 'lucide-react'
import type { ConceptLayoutProps } from '../types'

export function FamilyCareConcept({ blueprint, renderSection }: ConceptLayoutProps) {
  return <div className="space-y-5"><div className="grid gap-4 lg:grid-cols-[.75fr_1.25fr]"><aside className="rounded-[28px] border border-rose-200 bg-gradient-to-br from-rose-50 via-white to-amber-50 p-5"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-rose-600 text-white"><HeartHandshake size={22} /></div><h3 className="mt-5 text-2xl font-black tracking-[-.04em] text-slate-950">Architecture familiale</h3><p className="mt-2 text-xs font-semibold leading-5 text-slate-600">Le rythme de la maison, la présence parentale et la transmission structurent le service.</p><div className="mt-5 space-y-3"><div className="flex gap-3 rounded-2xl bg-white p-3"><Boxes className="text-rose-600" size={17} /><span className="text-xs font-bold text-slate-700">Routines du domicile</span></div><div className="flex gap-3 rounded-2xl bg-white p-3"><ShieldCheck className="text-emerald-600" size={17} /><span className="text-xs font-bold text-slate-700">Sécurité et remise autorisée</span></div></div></aside><div>{renderSection(blueprint.sections[0])}</div></div>{blueprint.sections.slice(1).map((section) => <div key={section.code}>{renderSection(section)}</div>)}</div>
}
