import { ChevronRight, FileText } from 'lucide-react'
import { Badge, Hero, Money, RevenuePage, Section, styles } from './RevenuePrimitives'
type Row={id:string;number:string;customer:string;status:string;total:number;href:string;detail:string}
export default function DocumentList({eyebrow,title,description,label,rows,newHref}:{eyebrow:string;title:string;description:string;label:string;rows:Row[];newHref?:string}){return <RevenuePage><Hero eyebrow={eyebrow} title={title} description={description} value={String(rows.length)} valueLabel={label}/><Section title="Controlled registry" action={newHref?<a className={styles.button} href={newHref}>Create</a>:undefined}><table className={styles.registry}><thead><tr><th>Number</th><th>Customer</th><th>Status</th><th>Detail</th><th>Total</th><th></th></tr></thead><tbody>{rows.map(row=><tr key={row.id}><td><FileText size={13}/> {row.number}</td><td>{row.customer}</td><td><Badge tone={row.status==='paid'||row.status==='completed'?'green':row.status==='cancelled'||row.status==='rejected'?'red':'blue'}>{row.status}</Badge></td><td>{row.detail}</td><td><Money value={row.total}/></td><td><a className={styles.link} href={row.href}>Open <ChevronRight size={12}/></a></td></tr>)}</tbody></table></Section></RevenuePage>}
