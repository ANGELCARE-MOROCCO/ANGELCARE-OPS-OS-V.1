import fs from 'fs'; const changed=fs.readFileSync('MZ7_PATCH_FILE_LIST.txt','utf8').trim().split(/\r?\n/).filter(Boolean); for(const file of changed){if(/(^|\/)app\/api\//.test(file)||/supabase|migration|schema|database/i.test(file)) throw new Error(`Backend/database file in MZ7 patch: ${file}`)} console.log('PASS — MZ7 introduces no API, database, Supabase or migration architecture');
