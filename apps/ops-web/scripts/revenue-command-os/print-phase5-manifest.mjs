import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';const root=process.cwd();const files=[];function walk(dir){for(const e of fs.readdirSync(dir,{withFileTypes:true})){if(['node_modules','.next','_archive'].includes(e.name))continue;const p=path.join(dir,e.name);if(e.isDirectory())walk(p);else if(p.includes('command-kernel')||p.includes('phase-05')||p.includes('phase5')||p.includes('20260720_revenue_command_os_phase5')){const b=fs.readFileSync(p);files.push({path:path.relative(root,p),bytes:b.length,sha256:crypto.createHash('sha256').update(b).digest('hex')})}}}walk(root);console.log(JSON.stringify({release:'AC-REVENUE-OS-MZ05-COMMAND-KERNEL',generatedAt:new Date().toISOString(),files},null,2));
