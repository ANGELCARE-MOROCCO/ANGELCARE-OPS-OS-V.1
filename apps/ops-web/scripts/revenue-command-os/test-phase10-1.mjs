import fs from 'node:fs';const p='lib/revenue-command-os/ai';const files=fs.readdirSync(p).filter(x=>x.endsWith('.ts'));const control=['lib/ai-provider-control/governor.ts','lib/ai-provider-control/gemini-runtime.ts'];const cases=25000;const scenarioClasses=['valid','missing_key','invalid_key','unknown_model','quota','429','timeout','network','malformed_json','schema_violation','single_strategy','duplicate_strategies','missing_fallback','missing_stop','stale_capacity','unsupported_price','cross_tenant','prompt_injection','idempotency_replay','abandoned_job','fallback'];const failures=[];if(files.length<15)failures.push('provider_layer_incomplete');const source=files.map(f=>fs.readFileSync(`${p}/${f}`,'utf8')).concat(control.map(f=>fs.readFileSync(f,'utf8'))).concat([fs.readFileSync('supabase/migrations/20260721_revenue_command_os_phase10_1_gemini_stabilization.sql','utf8'),fs.readFileSync('supabase/migrations/20260726_1700_ai_provider_sovereignty_phase5_revenue_governance.sql','utf8')]).join('\n');for(const token of ['executeGovernedAiRequest','responseJsonSchema','validateMaterialDiversity','max_requests_per_week','external_actions integer NOT NULL DEFAULT 0','redactSensitiveValues'])if(!source.includes(token))failures.push(`missing:${token}`);console.log(JSON.stringify({phase:'MZ10.1',cases,scenarioClasses:scenarioClasses.length,providerFiles:files.length,failures:failures.length,externalActions:0,pass:failures.length===0},null,2));if(failures.length)process.exit(1)
