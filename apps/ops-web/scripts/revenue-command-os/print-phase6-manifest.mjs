import fs from 'node:fs';import path from 'node:path';import crypto from 'node:crypto';
const root=process.cwd();const files=[];function walk(dir){for(const e of fs.readdirSync(dir,{withFileTypes:true})){if(['node_modules','.next','.git'].includes(e.name))continue;const full=path.join(dir,e.name);if(e.isDirectory())walk(full);else if(full.includes('golden-300')||full.includes('phase-06')||e.name.includes('phase6')||e.name.includes('MZ06'))files.push(full)}}walk(root);const result=files.sort().map(full=>({path:path.relative(root,full),bytes:fs.statSync(full).size,sha256:crypto.createHash('sha256').update(fs.readFileSync(full)).digest('hex')}));console.log(JSON.stringify({release:'MZ06',files:result.length,artifacts:result},null,2));
