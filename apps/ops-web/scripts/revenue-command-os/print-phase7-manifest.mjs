import fs from 'node:fs';import path from 'node:path';const root=process.cwd();const files=[];function w(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){if(['node_modules','.next','.git'].includes(e.name))continue;const f=path.join(d,e.name);e.isDirectory()?w(f):files.push(path.relative(root,f))}}w(root);console.log(JSON.stringify({release:'MZ07',files:files.length,paths:files.sort()},null,2));
