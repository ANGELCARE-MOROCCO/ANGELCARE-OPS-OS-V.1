import fs from 'fs'; const cssFile='components/market-os/content-command/release/mz7-release.module.css'; const css=fs.readFileSync(cssFile,'utf8'); const declared=new Set([...css.matchAll(/\.([A-Za-z_][A-Za-z0-9_-]*)/g)].map(m=>m[1])); const files=['components/market-os/content-command/release/release-ui.tsx','components/market-os/content-command/headquarters/ValidationWorkspace.tsx','components/market-os/content-command/headquarters/DistributionWorkspace.tsx','components/market-os/content-command/content-publishing-page.tsx']; let count=0; for(const file of files){const s=fs.readFileSync(file,'utf8'); for(const match of s.matchAll(/styles\.([A-Za-z_][A-Za-z0-9_]*)/g)){count++; if(!declared.has(match[1])) throw new Error(`Missing CSS module class ${match[1]} in ${file}`)}} console.log(`PASS — ${count} MZ7 CSS-module references resolve`);
