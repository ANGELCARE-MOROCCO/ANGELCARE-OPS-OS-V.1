import React from 'react'
import {ScreenPlaceholder} from '../_shared/ScreenPlaceholder'

export function OfflineScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Synchronisation"
      title="Centre offline"
      body="La file locale, les retries et les éléments en attente de sync seront intégrés à la phase 9."
    />
  )
}
