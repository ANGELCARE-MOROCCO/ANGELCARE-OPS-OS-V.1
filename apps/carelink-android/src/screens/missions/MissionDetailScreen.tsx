import React from 'react'
import {ScreenPlaceholder} from '../_shared/ScreenPlaceholder'

export function MissionDetailScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Mission"
      title="Détail opérationnel"
      body="Cette vue accueillera le cycle de mission complet, les actions, la présence, le rapport et les preuves terrain."
    />
  )
}
