import styles from './CommunicationCommand.module.css'
function fmt(d?:string|null){if(!d)return'—';return new Intl.DateTimeFormat('fr-FR',{dateStyle:'medium',timeStyle:'medium'}).format(new Date(d))}
export default function CommunicationAudit({events}:{events:Array<Record<string,any>>}){return <section className={styles.auditPanel}><div className={styles.panelHead}><div><p className={styles.eyebrow}>Communication Forensics</p><h2>Chronologie institutionnelle</h2><p>Actions de messagerie, changement de responsabilité, audiences et mutations du nouveau cockpit sont conservés dans le journal AngelCare 360 existant.</p></div><div className={styles.panelMeta}>{events.length} événement(s)</div></div><div className={styles.timeline}>{events.map(e=><div key={String(e.id)} className={styles.timelineItem}><span className={styles.timelineTime}>{fmt(e.created_at)}</span><div className={styles.timelineBody}><strong>{String(e.action||'action')} · {String(e.entity_type||'communication')}</strong><p>{String(e.actor_role||'acteur')} · {String(e.severity||'info')} · {e.entity_id?String(e.entity_id):'sans entité'}</p></div></div>)}</div>{!events.length?<div className={styles.empty}><div><strong>Aucun événement d’audit</strong><p>Le journal communication ne contient aucun enregistrement dans la fenêtre chargée.</p></div></div>:null}</section>}
