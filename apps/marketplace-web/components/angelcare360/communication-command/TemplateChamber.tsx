
import styles from './CommunicationCommand.module.css'
import TemplateRenderStudio from './TemplateRenderStudio'
import type { SanilaTemplate, SanilaTemplateVersion } from '@/types/angelcare360/communication-command'
function fmt(d?:string|null){if(!d)return'—';return new Intl.DateTimeFormat('fr-FR',{dateStyle:'medium',timeStyle:'short'}).format(new Date(d))}
export default function TemplateChamber({template,versions}:{template:SanilaTemplate;versions:SanilaTemplateVersion[]}){return <div className={styles.stack}><section className={styles.sectionPanel}><div className={styles.panelHead}><div><p className={styles.eyebrow}>Institutional Template Chamber</p><h2>{template.label}</h2><p>{template.template_key} · {template.template_type} · {template.channel} · {template.language_code}</p></div><span className={styles.pill}>{template.status}</span></div><div className={styles.sectionBody}><div className={styles.grid2}><div className={styles.channelCard}><strong>Audience & canal</strong><span>{template.audience_type} · {template.channel}</span></div><div className={styles.channelCard}><strong>Version</strong><span>{versions.length} version(s) · dernière v{template.latest_version||'—'}</span></div></div><div className={styles.channelCard} style={{marginTop:12}}><strong>{template.subject_template||'Sans objet'}</strong><span>{template.body_template}</span></div><div className={styles.channelCard} style={{marginTop:12}}><strong>Variables déclarées</strong><span>{Object.keys(template.variables_schema_json||{}).length?JSON.stringify(template.variables_schema_json):'Aucune variable déclarée'}</span></div></div></section><div className={styles.grid2}><TemplateRenderStudio templateKey={template.template_key} variablesSchema={template.variables_schema_json}/><section className={styles.sectionPanel}><div className={styles.panelHead}><div><p className={styles.eyebrow}>Version Chronicle</p><h2>Historique éditorial</h2></div></div><div className={styles.timeline}>{versions.map(v=><div key={v.id} className={styles.timelineItem}><span className={styles.timelineTime}>v{v.version_number}<br/>{fmt(v.created_at)}</span><div className={styles.timelineBody}><strong>{v.status} · {v.subject_template||'Sans objet'}</strong><p>{v.body_template}</p></div></div>)}</div></section></div></div>}
