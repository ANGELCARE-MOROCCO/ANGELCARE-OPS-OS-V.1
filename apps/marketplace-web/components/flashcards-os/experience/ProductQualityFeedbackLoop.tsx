import type { ProductQualitySignal } from '@/lib/flashcards-os/experience/types'
import { Metric, Status, money } from './ExperiencePrimitives'
import styles from './experience-os.module.css'
export default function ProductQualityFeedbackLoop({signals}:{signals:ProductQualitySignal[]}){return <div className={styles.page}><section className={styles.hero}><span className={styles.eyebrow}>Product Quality Feedback Loop</span><h1 className={styles.title}>Customer evidence becomes product intelligence — linked back to opportunity, design and quality review.</h1><div className={styles.heroRail}><Metric label="New signals" value={signals.filter(s=>s.status==='new').length} detail="Awaiting triage"/><Metric label="Product action" value={signals.filter(s=>s.status==='product_action_required').length} detail="Portfolio intervention"/><Metric label="Customer impact" value={signals.reduce((s,i)=>s+i.customerImpact,0)} detail="Affected customers"/><Metric label="Revenue impact" value={money(signals.reduce((s,i)=>s+i.revenueImpactDh,0))} detail="Commercial exposure"/></div></section><section className={styles.panel}><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Signal</th><th>Product lineage</th><th>Impact</th><th>Recommended action</th><th>Linked action</th><th>Status</th></tr></thead><tbody>{signals.map(item=><tr key={item.id}><td><strong>{item.code}</strong><br/>{item.title}<br/><small>{item.detail}</small></td><td>Collection {item.collectionId||'—'}<br/>Release {item.releaseId||'—'}</td><td>{item.occurrenceCount} occurrence(s)<br/>{item.customerImpact} customer(s)</td><td>{item.recommendedAction}</td><td>{item.linkedOpportunityId||item.linkedDesignId||item.linkedReviewId||'Unlinked'}</td><td><Status value={item.status}/></td></tr>)}</tbody></table></div></section></div>}
