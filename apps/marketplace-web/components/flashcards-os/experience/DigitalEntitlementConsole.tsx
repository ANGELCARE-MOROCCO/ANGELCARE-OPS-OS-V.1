import type { DigitalEntitlement, FulfilmentPlan } from '@/lib/flashcards-os/experience/types'
import { Metric, Status } from './ExperiencePrimitives'
import styles from './experience-os.module.css'
export default function DigitalEntitlementConsole({entitlements,plans}:{entitlements:DigitalEntitlement[];plans:FulfilmentPlan[]}){return <div className={styles.page}><section className={styles.hero}><span className={styles.eyebrow}>Digital delivery</span><h1 className={styles.title}>Release-bound access, controlled downloads, expiry, replacement and revocation.</h1><div className={styles.heroRail}><Metric label="Active" value={entitlements.filter(e=>e.status==='active').length} detail="Authorised product access"/><Metric label="Suspended" value={entitlements.filter(e=>e.status==='suspended').length} detail="Human review required"/><Metric label="Downloads" value={entitlements.reduce((s,e)=>s+e.downloadCount,0)} detail="Audited entitlement access"/><Metric label="Expired" value={entitlements.filter(e=>e.status==='expired').length} detail="No longer deliverable"/></div></section><section className={styles.panel}><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Entitlement</th><th>Customer</th><th>Release</th><th>Deliverable</th><th>Access</th><th>Expiry</th><th>Status</th></tr></thead><tbody>{entitlements.map(item=>{const plan=plans.find(p=>p.id===item.fulfilmentPlanId);return <tr key={item.id}><td><strong>{item.code}</strong><br/><small>Token hint {item.accessTokenHint}</small></td><td>{plan?.customerName||item.customerId}</td><td>{item.releaseId}</td><td>{item.deliverableId}</td><td>{item.downloadCount}{item.downloadLimit===null?'':` / ${item.downloadLimit}`}</td><td>{item.expiresAt||'Open'}</td><td><Status value={item.status}/></td></tr>})}</tbody></table></div></section></div>}
