import type { FulfilmentPlan, PhysicalWorkOrder, Shipment, DigitalEntitlement, FulfilmentException } from '@/lib/flashcards-os/experience/types'
import { Status } from './ExperiencePrimitives'
import styles from './experience-os.module.css'
export default function FulfilmentPlanDossier({plan,workOrders,shipments,entitlements,exceptions}:{plan:FulfilmentPlan;workOrders:PhysicalWorkOrder[];shipments:Shipment[];entitlements:DigitalEntitlement[];exceptions:FulfilmentException[]}){return <div className={styles.page}><section className={styles.hero}><span className={styles.eyebrow}>{plan.code} · {plan.universe.toUpperCase()}</span><h1 className={styles.title}>{plan.customerName}</h1><p className={styles.subtitle}>Order {plan.orderNumber} · {plan.mode} · owner {plan.owner}</p><div className={styles.actions}><Status value={plan.status}/><Status value={plan.priority}/></div></section><section className={styles.grid2}><div className={styles.panel}><div className={styles.panelHeader}><div><h2>Commitment lines</h2><p>Allocated, picked, packed, dispatched and delivered quantities.</p></div></div><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Line</th><th>Qty</th><th>Picked</th><th>Packed</th><th>Dispatched</th><th>Delivered</th><th>Status</th></tr></thead><tbody>{plan.items.map(item=><tr key={item.id}><td><strong>{item.description}</strong><br/><small>{item.releaseIds.join(', ')||'Release required'}</small></td><td>{item.quantity}</td><td>{item.pickedQuantity}</td><td>{item.packedQuantity}</td><td>{item.dispatchedQuantity}</td><td>{item.deliveredQuantity}</td><td><Status value={item.status}/></td></tr>)}</tbody></table></div></div><div className={styles.panel}><div className={styles.panelHeader}><div><h2>Execution lineage</h2><p>Work orders, shipments and digital access.</p></div></div><div className={styles.list}>{workOrders.map(item=><div className={styles.card} key={item.id}><div><strong>{item.code}</strong><p>{item.zone} · pick {item.pickProgress}% · pack {item.packProgress}%</p></div><Status value={item.status}/></div>)}{shipments.map(item=><div className={styles.card} key={item.id}><div><strong>{item.code}</strong><p>{item.carrier} · {item.trackingNumber||'No tracking yet'}</p></div><Status value={item.status}/></div>)}{entitlements.map(item=><div className={styles.card} key={item.id}><div><strong>{item.code}</strong><p>Release {item.releaseId} · {item.downloadCount} access(es)</p></div><Status value={item.status}/></div>)}{exceptions.map(item=><div className={styles.card} key={item.id}><div><strong>{item.type}</strong><p>{item.detail}</p></div><Status value={item.status}/></div>)}</div></div></section></div>}
