import type { ReconciliationRun } from '@/lib/flashcards-os/experience/types'
import { ReconciliationPanel } from './ExperienceActionPanels'
import { Metric, Status } from './ExperiencePrimitives'
import styles from './experience-os.module.css'
export default function ReconciliationControlRoom({runs}:{runs:ReconciliationRun[]}){const latest=runs[0];return <div className={styles.page}><section className={styles.hero}><span className={styles.eyebrow}>Deterministic Reconciliation</span><h1 className={styles.title}>Orders, invoices, payments, customer balances, Vault checksums and digital entitlements must agree before management trusts the dashboard.</h1><div className={styles.heroRail}><Metric label="Runs" value={runs.length} detail="Auditable reconciliation executions"/><Metric label="Latest checked" value={latest?.checkedRecords||0} detail="Governed records"/><Metric label="Findings" value={latest?.findingCount||0} detail="Mismatch evidence"/><Metric label="Blocking" value={latest?.blockingCount||0} detail="Critical integrity defects"/></div></section><section className={styles.grid2}><div className={styles.panel}><div className={styles.panelHeader}><div><h2>Run complete reconciliation</h2><p>Calculations are performed by deterministic services.</p></div></div><ReconciliationPanel/></div><div className={styles.panel}><div className={styles.panelHeader}><div><h2>Recent runs</h2><p>Evidence and blocking status.</p></div></div><div className={styles.list}>{runs.map(run=><div className={styles.card} key={run.id}><div><strong>{run.code}</strong><p>{run.scope.join(', ')} · {run.checkedRecords} checked · {run.findingCount} findings</p></div><Status value={run.status}/></div>)}</div></div></section>{latest?.findings.length?<section className={styles.panel}><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Domain</th><th>Record</th><th>Expected</th><th>Actual</th><th>Severity</th><th>Status</th></tr></thead><tbody>{latest.findings.map(f=><tr key={f.id}><td>{f.domain}</td><td>{f.recordType}<br/><small>{f.recordId}</small></td><td>{f.expected}</td><td>{f.actual}</td><td><Status value={f.severity}/></td><td><Status value={f.status}/></td></tr>)}</tbody></table></div></section>:null}</div>}
