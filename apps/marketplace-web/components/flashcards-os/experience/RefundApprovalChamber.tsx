import type { RefundRequest } from '@/lib/flashcards-os/experience/types'
import { Metric, Status, money } from './ExperiencePrimitives'
import styles from './experience-os.module.css'
export default function RefundApprovalChamber({refunds}:{refunds:RefundRequest[]}){return <div className={styles.page}><section className={styles.hero}><span className={styles.eyebrow}>Refund authority</span><h1 className={styles.title}>Eligible paid value, approval authority and completion evidence remain deterministic and human-governed.</h1><div className={styles.heroRail}><Metric label="Pending" value={refunds.filter(r=>r.status==='approval_required').length} detail="Finance or executive decision"/><Metric label="Exposure" value={money(refunds.filter(r=>!['rejected','cancelled','completed'].includes(r.status)).reduce((s,r)=>s+r.requestedAmountDh,0))} detail="Requested value"/><Metric label="Approved" value={money(refunds.reduce((s,r)=>s+r.approvedAmountDh,0))} detail="Human-authorised value"/><Metric label="Completed" value={refunds.filter(r=>r.status==='completed').length} detail="Payout evidence recorded"/></div></section><section className={styles.panel}><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Request</th><th>Customer</th><th>Invoice</th><th>Requested</th><th>Eligible</th><th>Approved</th><th>Status</th></tr></thead><tbody>{refunds.map(item=><tr key={item.id}><td><strong>{item.code}</strong><br/><small>{item.reason}</small></td><td>{item.customerName}</td><td>{item.invoiceId}</td><td>{money(item.requestedAmountDh)}</td><td>{money(item.eligibleAmountDh)}</td><td>{money(item.approvedAmountDh)}</td><td><Status value={item.status}/></td></tr>)}</tbody></table></div></section></div>}
