import type { PhysicalWorkOrder, FulfilmentPlan } from '@/lib/flashcards-os/experience/types'
import { Metric, Status } from './ExperiencePrimitives'
import styles from './experience-os.module.css'
export default function PhysicalFulfilmentBoard({workOrders,plans}:{workOrders:PhysicalWorkOrder[];plans:FulfilmentPlan[]}){return <div className={styles.page}><section className={styles.hero}><span className={styles.eyebrow}>Physical operations</span><h1 className={styles.title}>Pick, pack, quality hold and dispatch readiness — managed as operational obligations, not decorative cards.</h1><div className={styles.heroRail}><Metric label="Ready" value={workOrders.filter(x=>x.status==='ready').length} detail="Awaiting assignment"/><Metric label="Picking" value={workOrders.filter(x=>x.status==='picking').length} detail="Active item preparation"/><Metric label="Packing" value={workOrders.filter(x=>x.status==='packing').length} detail="Packaging and manifests"/><Metric label="QA holds" value={workOrders.filter(x=>x.status==='qa_hold').length} detail="Cannot dispatch"/></div></section><section className={styles.panel}><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Work order</th><th>Customer</th><th>Zone</th><th>Pick</th><th>Pack</th><th>Quality</th><th>Status</th></tr></thead><tbody>{workOrders.map(work=>{const plan=plans.find(p=>p.id===work.fulfilmentPlanId);return <tr key={work.id}><td>{work.code}</td><td>{plan?.customerName||work.fulfilmentPlanId}</td><td>{work.zone}</td><td><div className={styles.progress}><span style={{width:`${work.pickProgress}%`}}/></div>{work.pickProgress}%</td><td><div className={styles.progress}><span style={{width:`${work.packProgress}%`}}/></div>{work.packProgress}%</td><td><Status value={work.qualityStatus}/></td><td><Status value={work.status}/></td></tr>})}</tbody></table></div></section></div>}
