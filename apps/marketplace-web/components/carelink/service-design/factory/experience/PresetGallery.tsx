'use client'

import { ArrowRight, CalendarRange, Clock3, PackageCheck, Sparkles, WandSparkles } from 'lucide-react'
import type { CategoryExperienceBlueprint, CategoryExperiencePreset } from '@/types/homeservice-category-experience'
import { cx } from '../FactoryUI'

export function PresetGallery({ blueprint, selectedCode, onSelect }: { blueprint: CategoryExperienceBlueprint; selectedCode: string; onSelect: (preset: CategoryExperiencePreset) => void }) {
  return <section className="overflow-hidden rounded-[30px] border border-slate-200 bg-white shadow-[0_18px_55px_rgba(15,23,42,.06)]"><header className="relative overflow-hidden border-b border-slate-100 bg-gradient-to-r from-white via-blue-50/55 to-cyan-50/50 p-5 sm:p-6"><div className="absolute -right-12 -top-16 h-44 w-44 rounded-full border-[28px] border-blue-500/[0.06]" /><div className="relative flex flex-wrap items-end justify-between gap-4"><div><p className="text-[10px] font-black uppercase tracking-[.22em] text-blue-600">Étape 1 · Scenario Library</p><h2 className="mt-1 text-2xl font-black tracking-[-.04em] text-slate-950">Expériences prêtes pour {blueprint.categoryName}</h2><p className="mt-2 max-w-3xl text-xs font-semibold leading-5 text-slate-500">Chaque scénario configure le dossier complet. L’opérateur choisit l’expérience, puis ne modifie que les différences réelles.</p></div><span className="inline-flex items-center gap-2 rounded-full border border-emerald-200 bg-emerald-50 px-4 py-2 text-[10px] font-black uppercase tracking-[.14em] text-emerald-700"><WandSparkles size={13} />{blueprint.presets.length} scénarios complets</span></div></header>
    <div className="grid gap-4 p-5 md:grid-cols-2 xl:grid-cols-3 sm:p-6">{blueprint.presets.map((preset, index) => { const active = selectedCode === preset.code; return <button key={preset.code} type="button" onClick={() => onSelect(preset)} className={cx('group relative overflow-hidden rounded-[25px] border p-4 text-left transition duration-200 hover:-translate-y-1', active ? 'border-slate-950 bg-slate-950 text-white shadow-[0_24px_58px_rgba(15,23,42,.26)]' : 'border-slate-200 bg-slate-50/70 hover:border-blue-200 hover:bg-white hover:shadow-lg')}><div className={cx('absolute inset-x-0 top-0 h-1 bg-gradient-to-r', index % 3 === 0 ? 'from-blue-600 to-cyan-400' : index % 3 === 1 ? 'from-violet-600 to-fuchsia-400' : 'from-emerald-600 to-teal-400')} /><div className="flex items-start justify-between gap-3"><span className={cx('rounded-full px-2.5 py-1 text-[9px] font-black uppercase tracking-[.12em]', active ? 'bg-white/15 text-white' : 'bg-blue-50 text-blue-700')}>{preset.badge}</span>{active ? <PackageCheck size={18} className="text-emerald-300" /> : <ArrowRight size={18} className="text-slate-400 transition group-hover:translate-x-1 group-hover:text-blue-600" />}</div><h3 className="mt-4 text-base font-black tracking-[-.025em]">{preset.name}</h3><p className={cx('mt-2 min-h-14 text-[11px] font-semibold leading-5', active ? 'text-slate-300' : 'text-slate-500')}>{preset.description}</p><div className={cx('mt-4 grid grid-cols-3 gap-2 text-[9px] font-black uppercase tracking-[.08em]', active ? 'text-slate-300' : 'text-slate-500')}><span className={cx('rounded-xl p-2', active ? 'bg-white/5' : 'bg-white')}><CalendarRange size={12} className="mb-1" />{preset.defaultDayCount} j</span><span className={cx('rounded-xl p-2', active ? 'bg-white/5' : 'bg-white')}><Clock3 size={12} className="mb-1" />{preset.defaultStartTime}</span><span className={cx('rounded-xl p-2', active ? 'bg-white/5' : 'bg-white')}><Sparkles size={12} className="mb-1" />{preset.scenarioCount} alt.</span></div></button> })}</div>
  </section>
}
