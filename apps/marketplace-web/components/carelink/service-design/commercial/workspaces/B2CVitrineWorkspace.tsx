import { ArrowRight, HeartHandshake, PackageCheck, Sparkles } from 'lucide-react'
import { Action, Empty, Hero, State, Surface } from '../CommercialUI'
import { commercialData } from '../load'

export async function B2CVitrineWorkspace() {
  const d = await commercialData()
  const items = d.sellables.filter(x => x.universe === 'b2c')
  return <div className="space-y-6">
    <Hero eyebrow="ANGELCARE · B2C Vitrine Release Studio" title="Prévisualisez l’expérience familiale avant publication." description="Chaque référence traduit une promesse, un format, une durée, une préparation et un prix en une présentation immédiatement compréhensible pour les familles." actions={<Action tone="emerald" href="/carelink-ops/service-design/offers/new">Créer une référence B2C</Action>}/>
    <Surface title="Family Experience Portfolio" subtitle="Références approuvées et gouvernées, présentées comme de vrais produits de service.">
      {items.length?<div className="grid gap-5 md:grid-cols-2 2xl:grid-cols-3">{items.map((x,index)=><a key={x.id} href={x.code.startsWith('HSFS-')?`/carelink-ops/service-design/factory/sellables/${x.id}`:`/carelink-ops/service-design/vitrine/${x.id}`} className="group relative overflow-hidden rounded-[30px] border border-slate-200 bg-white shadow-[0_16px_44px_rgba(15,23,42,.06)] transition hover:-translate-y-1 hover:border-blue-200 hover:shadow-[0_28px_72px_rgba(37,99,235,.12)]"><div className={`h-1.5 bg-gradient-to-r ${index%3===0?'from-blue-600 to-cyan-400':index%3===1?'from-violet-600 to-fuchsia-400':'from-emerald-600 to-teal-400'}`}/><div className="p-5"><div className="flex items-start justify-between gap-3"><div className="grid h-11 w-11 place-items-center rounded-[18px] bg-slate-950 text-white"><HeartHandshake size={18}/></div><State value={x.status}/></div><p className="mt-5 text-[9px] font-black uppercase tracking-[.17em] text-blue-600">{x.code} · B2C</p><h3 className="mt-2 text-2xl font-black tracking-[-.045em] text-slate-950">{x.commercialName}</h3><p className="mt-3 min-h-14 text-sm font-semibold leading-6 text-slate-600">{x.promise||'Promesse à finaliser.'}</p><div className="mt-5 grid grid-cols-2 gap-3"><div className="rounded-2xl bg-slate-50 p-3"><p className="text-[9px] font-black uppercase text-slate-400">À partir de</p><p className="mt-1 text-xl font-black text-slate-950">{x.startingPrice==null?'Sur devis':`${x.startingPrice.toLocaleString('fr-FR')} Dh`}</p></div><div className="rounded-2xl bg-blue-50 p-3"><p className="text-[9px] font-black uppercase text-blue-600">Version</p><p className="mt-1 text-xl font-black text-blue-950">v{x.activeVersion}</p></div></div><div className="mt-5 flex items-center justify-between border-t border-slate-100 pt-4"><span className="flex items-center gap-2 text-[10px] font-black uppercase text-slate-500"><PackageCheck size={14}/>Prévisualiser</span><ArrowRight size={16} className="text-blue-600 transition group-hover:translate-x-1"/></div></div></a>)}</div>:<Empty title="Aucune référence B2C" detail="Publiez une offre approuvée pour alimenter cette vitrine. Aucun produit fictif n’est affiché."/>}
    </Surface>
  </div>
}
