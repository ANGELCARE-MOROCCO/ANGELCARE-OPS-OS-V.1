import{HandoffHero,MissionTree,Surface}from'../HandoffUI';import{handoffData}from'../load'
export async function MissionCalendarWorkspace(){const d=await handoffData(),dates=d.dates.slice(0,12).map(x=>({sequence:x.sequence,serviceDate:x.serviceDate,startTime:x.startTime,endTime:x.endTime,status:x.status}));return <div className="space-y-6"><HandoffHero eyebrow="Calendrier d’exécution" title="Mission Calendar Composer" description="Les dates approuvées deviennent des unités opérationnelles exactes. Décalages tolérés, exclusions, heures différentes et conflits sont visibles avant la construction CARELINK." seal="1 date approuvée = 1 sous-mission"/><div className="grid gap-6 xl:grid-cols-[.95fr_1.05fr]"><Surface title="Runway des dates" subtitle="Fenêtres exactes, durée et état de confirmation."><div className="space-y-3">{dates.length?dates.map(x=><div key={`${x.sequence}-${x.serviceDate}`} className="grid grid-cols-[70px_1fr_auto] items-center gap-4 rounded-2xl border border-slate-200 p-4"><div className="grid h-12 w-12 place-items-center rounded-2xl bg-blue-50 text-sm font-black text-blue-800">J{x.sequence}</div><div><p className="font-black text-slate-950">{x.serviceDate}</p><p className="text-xs font-semibold text-slate-500">{x.startTime} → {x.endTime}</p></div><span className="text-[10px] font-black uppercase text-emerald-700">Confirmée</span></div>):<div className="rounded-2xl border border-dashed border-slate-200 p-8 text-center text-sm font-semibold text-slate-500">Aucune date réelle chargée.</div>}</div></Surface><MissionTree dates={dates}/></div></div>}
