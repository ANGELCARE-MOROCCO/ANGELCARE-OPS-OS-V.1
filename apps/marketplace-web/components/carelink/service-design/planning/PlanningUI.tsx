'use client'
import type { ReactNode } from 'react'
import { CheckCircle2, Clock3, LockKeyhole, ShieldAlert } from 'lucide-react'
import { StudioAction, StudioEmpty, StudioHero, StudioMetric, StudioSurface, sd2030 } from '../studio2030'

export const pc = sd2030
export function StageHeader({ eyebrow, title, description, actions }: { eyebrow: string; title: string; description: string; actions?: ReactNode }) { return <StudioHero eyebrow={eyebrow} title={title} description={description} actions={actions} chips={<><span className="rounded-full border border-white/10 bg-white/5 px-3 py-1.5 text-[9px] font-black uppercase tracking-[.14em] text-cyan-200">Deterministic timeline</span><span className="rounded-full border border-white/10 bg-white/5 px-3 py-1.5 text-[9px] font-black uppercase tracking-[.14em] text-emerald-200">Human authority</span></>} /> }
export function Surface({ title, subtitle, children, action, className }: { title: string; subtitle?: string; children: ReactNode; action?: ReactNode; className?: string }) { return <StudioSurface title={title} subtitle={subtitle} action={action} className={className}>{children}</StudioSurface> }
export function Stat({ label, value, detail, tone = 'blue' }: { label: string; value: ReactNode; detail: string; tone?: 'blue' | 'emerald' | 'amber' | 'rose' | 'violet' }) { return <StudioMetric label={label} value={value} detail={detail} tone={tone} icon={<Clock3 size={18} />} /> }
export function Action({ children, href, tone = 'blue' }: { children: ReactNode; href?: string; tone?: 'blue' | 'emerald' | 'slate' | 'rose' }) { return <StudioAction href={href} tone={tone}>{children}</StudioAction> }
export function Status({ value }: { value: string }) { const ok = ['valid', 'approved', 'ready', 'generated'].includes(value), bad = ['blocked', 'failed', 'rejected'].includes(value); return <span className={pc('inline-flex items-center gap-1.5 rounded-full border px-2.5 py-1 text-[10px] font-black uppercase tracking-[.14em]', ok ? 'border-emerald-200 bg-emerald-50 text-emerald-700' : bad ? 'border-rose-200 bg-rose-50 text-rose-700' : 'border-amber-200 bg-amber-50 text-amber-800')}>{ok ? <CheckCircle2 size={13} /> : bad ? <ShieldAlert size={13} /> : <Clock3 size={13} />} {value.replaceAll('_', ' ')}</span> }
export function Blocker({ title, detail }: { title: string; detail: string }) { return <div className="flex gap-3 rounded-[22px] border border-rose-200 bg-rose-50 p-4"><ShieldAlert className="mt-0.5 text-rose-700" size={18} /><div><p className="text-sm font-black text-rose-900">{title}</p><p className="mt-1 text-xs font-semibold leading-5 text-rose-700">{detail}</p></div></div> }
export function Empty({ title, detail, action }: { title: string; detail: string; action?: ReactNode }) { return <StudioEmpty title={title} detail={detail} action={action} icon={<LockKeyhole size={21} />} /> }
