import fs from 'node:fs'
const files={login:'app/traininghub/login/page.tsx',partnerLogin:'app/traininghub/partner/login/page.tsx',guard:'components/traininghub/auth/TrainingHubPartnerRouteGuard.tsx',entry:'app/api/traininghub/auth/entry-route/route.ts',hardDelete:'app/api/traininghub/internal/partners/[organizationId]/hard-delete/route.ts',modal:'components/traininghub/internal/ExistingPartnerSyncedModal.tsx'}
const read=(file)=>fs.existsSync(file)?fs.readFileSync(file,'utf8'):''; const text=Object.fromEntries(Object.entries(files).map(([k,file])=>[k,read(file)]))
const checks=[['canonical /traininghub/login exists',fs.existsSync(files.login)],['canonical login uses Supabase signInWithPassword',text.login.includes('signInWithPassword')],['canonical login calls entry-route',text.login.includes('/api/traininghub/auth/entry-route')],['partner login redirects to canonical login',text.partnerLogin.includes("redirect('/traininghub/login')")],['route guard uses unified entry-route',text.guard.includes('/api/traininghub/auth/entry-route')],['route guard sends missing session to /traininghub/login',text.guard.includes('/traininghub/login?error=session_required')],['entry-route returns /traininghub for internal admin',text.entry.includes("isInternal ? '/traininghub'")],['hard-delete validates bearer session',text.hardDelete.includes('authorization')&&text.hardDelete.includes('getUser(token)')],['hard-delete checks internal admin access',text.hardDelete.includes('TrainingHub internal admin access required')],['modal sends Authorization bearer',!fs.existsSync(files.modal)||text.modal.includes('Authorization: `Bearer ${modalSession.access_token}`')],['modal uses credentials include',!fs.existsSync(files.modal)||text.modal.includes("credentials: 'include'")]]
console.table(checks.map(([name,pass])=>({name,pass})))
if(checks.some(([,pass])=>!pass)){console.error('TrainingHub single login role router verification FAILED.');process.exit(1)}
console.log('TrainingHub single login role router verification PASSED.')
