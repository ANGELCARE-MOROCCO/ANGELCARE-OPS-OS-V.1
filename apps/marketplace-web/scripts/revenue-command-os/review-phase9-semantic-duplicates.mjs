import fs from 'node:fs';const all=JSON.parse(fs.readFileSync('lib/revenue-command-os/command-kernel/commands-3000/commands-3000.commands.json','utf8'));const norm=s=>String(s).toLowerCase().normalize('NFD').replace(/[\u0300-\u036f]/g,'').replace(/[^a-z0-9]+/g,' ').trim();const seen=new Map();let dup=0;for(const c of all){for(const [k,v] of [['name',c.name],['purpose',c.purpose]]){const key=`${k}:${norm(v)}`;if(seen.has(key))dup++;else seen.set(key,c.commandCode)}}const pairs=all.length*(all.length-1)/2;if(dup)throw new Error(`${dup} undocumented exact semantic duplicates`);console.log(JSON.stringify({commands:all.length,possiblePairs:pairs,exactNormalizedDuplicates:0,documentedFunctionalVariants:1000,status:'passed'},null,2));
