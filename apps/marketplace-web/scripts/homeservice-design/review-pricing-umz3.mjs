import fs from'node:fs';const s=fs.readFileSync('lib/homeservice-commercial/server/pricing-engine.ts','utf8');let p=0,f=0;const c=(n,v)=>{console.log(`${v?'PASS':'FAIL'}  ${n}`);v?p++:f++};for(const [n,v] of [['server deterministic calculation',s.includes('calculateCommercialTruth')],['missing costs are explicit',s.includes('missingCostCodes')&&s.includes('MISSING_COSTS')],['missing costs do not become zero authority',s.includes("marginStatus:Calculation['marginStatus']=!known?'unknown'")],['tax calculation exists',s.includes('taxTotal=money')],['discount total exists',s.includes('discountTotal=money')],['gross margin exists',s.includes('grossMargin=known')],['margin floor is enforced',s.includes("'approval_required'")&&s.includes("'blocked'")],['invalid total rejected',s.includes('INVALID_TOTAL')]])c(n,v);console.log(`\n${p}/${p+f} pricing-integrity checks passed.`);if(f)process.exit(1)
