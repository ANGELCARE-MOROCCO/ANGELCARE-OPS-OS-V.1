import fs from'node:fs';const s=fs.readFileSync('lib/homeservice-commercial/server/bundle-engine.ts','utf8');let p=0,f=0;const c=(n,v)=>{console.log(`${v?'PASS':'FAIL'}  ${n}`);v?p++:f++};for(const [n,v] of [['duplicate service detection',s.includes('DUPLICATE_SERVICE')],['B2C/B2B conflict detection',s.includes('UNIVERSE_CONFLICT')],['configured incompatibility detection',s.includes('SERVICE_INCOMPATIBLE')],['time overlap detection',s.includes('TIME_OVERLAP')],['safety blocker detection',s.includes('SAFETY_BLOCKER')],['shared staffing detection',s.includes('SHARED_STAFFING')],['shared transport detection',s.includes('SHARED_TRANSPORT')],['shared materials detection',s.includes('SHARED_MATERIALS')],['missing costs remain explicit',s.includes('missingCosts')]])c(n,v);console.log(`\n${p}/${p+f} bundle-compatibility checks passed.`);if(f)process.exit(1)
