import fs from'node:fs';const sql=fs.readFileSync('supabase/migrations/20260801_homeservice_design_os_ultra_mega_zip3_commercial.sql','utf8'),repo=fs.readFileSync('lib/homeservice-commercial/server/repository.ts','utf8');let p=0,f=0;const c=(n,v)=>{console.log(`${v?'PASS':'FAIL'}  ${n}`);v?p++:f++};for(const[n,v]of[['sellable requires one source',sql.includes("check((offer_id is not null)::int+(bundle_id is not null)::int=1)")],['sellable requires technical plan version',sql.includes('technical_plan_version_id uuid not null references public.hsd_technical_plan_versions(id)')],['approved source required',repo.includes("eq('status','approved').single()")],['version snapshot created',repo.includes("from('hsd_sellable_versions').insert")],['price snapshot created',repo.includes("from('hsd_sellable_prices').insert")],['publication lineage table',sql.includes('hsd_sellable_publications')],['status lineage table',sql.includes('hsd_sellable_status_events')],['commercial documents versioned',sql.includes('hsd_sellable_document_versions')],['published content immutable',sql.includes('Published sellable content is immutable')]])c(n,v);console.log(`\n${p}/${p+f} sellable-version-integrity checks passed.`);if(f)process.exit(1)
