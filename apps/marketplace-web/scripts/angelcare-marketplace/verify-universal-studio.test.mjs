import test from 'node:test'
import assert from 'node:assert/strict'
import fs from 'node:fs'
import path from 'node:path'

const root=process.cwd()
const read=(rel)=>fs.readFileSync(path.join(root,rel),'utf8')
const exists=(rel)=>fs.existsSync(path.join(root,rel))
const studio='angelcare-marketplace/studio-universal'
const source=(rel)=>read(`${studio}/${rel}`)

const expectedFiles=[
 'types.ts','manifest.ts','block-contracts.ts','capability-registry.ts','design.tsx','puck-config.tsx','puck-bridge.ts','import-compiler.ts','capture.ts','repository.ts','database-error.ts','picker-data.ts','StudioPublishedRenderer.tsx','developer-contract.ts',
 'components/UniversalExperienceStudio.tsx','components/UniversalImportWorkbench.tsx','components/PuckOperatorTools.tsx','components/StudioLayouts.tsx','components/StudioFields.tsx','components/StudioBlockRuntime.tsx','components/StudioDesignShell.tsx','components/studio-workspace.module.css','components/studio-runtime.module.css',
]

test('one official AngelCare Studio workspace and no VoltHub identity',()=>{const text=expectedFiles.map(source).join('\n');assert.match(source('manifest.ts'),/officialWorkspace:\s*true/);assert.match(source('manifest.ts'),/AngelCare Marketplace Studio/);assert.doesNotMatch(text,/VOLTHUB|VoltHub|volthub/);})
test('all canonical Studio source files exist',()=>{for(const file of expectedFiles)assert.equal(exists(`${studio}/${file}`),true,file)})
test('Puck 0.23 is the real editor dependency',()=>{const pkg=JSON.parse(read('package.json'));assert.equal(pkg.dependencies['@puckeditor/core'],'0.23.0');const ui=source('components/UniversalExperienceStudio.tsx');assert.match(ui,/\bPuck\b/);assert.match(ui,/<Puck config=/);assert.match(ui,/<Puck\.Preview/);assert.match(ui,/<Puck\.Components/);assert.match(ui,/<Puck\.Fields/)})
test('Puck lock closure is present',()=>{const lock=JSON.parse(read('package-lock.json'));assert.equal(lock.packages[''].dependencies['@puckeditor/core'],'0.23.0');assert.equal(lock.packages['node_modules/@puckeditor/core'].version,'0.23.0')})
test('five first-class structural primitives are present',()=>{const text=source('block-contracts.ts');for(const key of ['ac_section','ac_container','ac_columns','ac_grid','ac_stack'])assert.match(text,new RegExp(`['\"]${key}['\"]`))})
test('operator has undo redo copy paste duplicate move delete',()=>{const text=source('components/PuckOperatorTools.tsx');for(const token of ["history.back()","history.forward()","type:'duplicate'","type:'remove'","type:'reorder'","type:'insert'","type:'replace'"])assert.ok(text.includes(token),token)})
test('mounted Puck receives imported data immediately with history',()=>{const text=source('components/UniversalExperienceStudio.tsx');assert.match(text,/type:'setData'/);assert.match(text,/recordHistory:true/);assert.match(text,/await persist\(next,summary\)/)})
test('canonical persistence is Experience Core and not browser storage',()=>{const text=source('repository.ts')+source('puck-bridge.ts')+source('components/UniversalExperienceStudio.tsx');assert.match(text,/saveBlocks/);assert.match(text,/angelcare-puck-v1/);assert.doesNotMatch(text,/localStorage|sessionStorage/)})
test('Media Vault and real commerce pickers are wired',()=>{const text=source('picker-data.ts');assert.match(text,/angelcare_marketplace_media_assets/);assert.match(text,/angelcare_marketplace_catalog_categories/);assert.match(text,/angelcare_marketplace_homepage_collections/);assert.doesNotMatch(text,/demo|fake|seed/i)})
test('public runtime renders Studio structures and real commerce authority',()=>{const text=source('StudioPublishedRenderer.tsx');assert.match(text,/type\.startsWith\('ac_'\)/);assert.match(text,/searchDiscovery/);assert.match(text,/CatalogCard/);assert.match(text,/collection_rail/)})
test('Universal Import supports URL HTML and file',()=>{const text=source('components/UniversalImportWorkbench.tsx');assert.match(text,/type="file"/);assert.match(text,/mode==='url'/);assert.match(text,/mode==='file'/);assert.match(text,/HTML source/)})
test('Experience IR candidate contains fingerprints ownership fidelity and loss budget',()=>{const text=source('import-compiler.ts');for(const token of ['sourceFingerprint','documentFingerprint','ownership','fidelity','lossBudget','doubleConsumedNodes','unownedMeaningfulNodes'])assert.match(text,new RegExp(token))})
test('candidate comes before apply and review is explicit',()=>{const text=source('components/UniversalImportWorkbench.tsx');assert.match(text,/setCandidate\(next\)/);assert.match(text,/reviewAccepted/);assert.match(text,/J’ai examiné les pertes/);assert.match(text,/candidate\.safeToApply/)})
test('full-page replace defaults and duplicate source guard exist',()=>{const text=source('components/UniversalImportWorkbench.tsx')+source('import-compiler.ts');assert.match(text,/strategy==='full-page'\?'replace'/);assert.match(text,/existing\.has\(next\.sourceFingerprint\)/);assert.match(text,/Source déjà présente/)})
test('DOM ownership and double-consumption hard blocker exist',()=>{const text=source('import-compiler.ts');assert.match(text,/disposition:/);assert.match(text,/DOUBLE_CONSUMPTION/);assert.match(text,/ownerBlockId/)})
test('CSS source handling accounts for cascade specificity custom properties and responsive buckets',()=>{const text=source('import-compiler.ts')+source('css-fidelity.ts');for(const token of ['studioSpecificity','customProperties','@media','container','supports','unsupportedAtRules','computeStudioElementStyle'])assert.match(text,new RegExp(token,'i'))})
test('foreign code execution is blocked',()=>{const text=source('import-compiler.ts')+source('capture.ts');assert.match(text,/script,noscript/);assert.match(text,/name\.startsWith\('on'\)/);assert.match(text,/javascript:/);assert.doesNotMatch(text,/\beval\s*\(|new Function\s*\(/)})
test('URL capture includes SSRF/private network protection and limits',()=>{const text=source('capture.ts');for(const token of ['dns','blockedIpv4','blockedIpv6','localhost','redirect','timeout','MAX_HTML','MAX_CSS'])assert.match(text,new RegExp(token,'i'));assert.match(text,/réseau privé/i)})
test('precise DB error classifier replaces migration catch-all',()=>{const text=source('database-error.ts');for(const code of ['42P01','42703','42883','PGRST202','23505','23503','23502','22P02','42501'])assert.match(text,new RegExp(code));const legacy=read('angelcare-marketplace/experience-builder/repository.ts');assert.doesNotMatch(legacy,/message[^\n]+includes\(['\"]angelcare_marketplace_cms_/);assert.match(legacy,/marketplaceDatabaseError/)})
test('normal page creation generates route key server side and opens Studio',()=>{const repo=source('repository.ts'),index=source('components/StudioIndex.tsx');assert.match(repo,/stableRouteKey/);assert.match(repo,/L’URL \/\$\{slug\} existe déjà/);assert.doesNotMatch(index,/ROUTE KEY|route key/i);assert.match(index,/window\.location\.assign\(`\/angelcare-marketplace\/admin\/experience\/studio\//)})
test('publication remains governed instead of silently self-approving',()=>{const repo=source('repository.ts'),ui=source('components/UniversalExperienceStudio.tsx');assert.match(repo,/\['approved','scheduled'\]/);assert.doesNotMatch(repo,/submitted.*in_review.*approved.*published/);assert.match(ui,/Validation requise/)})
test('secure preview uses canonical Experience Core preview token',()=>{assert.equal(exists('app/api/angelcare-marketplace/cms/studio/pages/[pageId]/preview/route.ts'),true);const route=read('app/api/angelcare-marketplace/cms/studio/pages/[pageId]/preview/route.ts');assert.match(route,/createStudioPreview/);assert.match(route,/preview_token/)})
test('Studio public renderer is integrated into canonical PublicPageRenderer',()=>{const text=read('angelcare-marketplace/public-universe/components/PublicPageRenderer.tsx');assert.match(text,/StudioPublishedRenderer/);assert.match(text,/isStudioCmsBlock/)})
test('existing Experience Command exposes the official Studio',()=>{const text=read('angelcare-marketplace/experience-builder/components/ExperienceCommand.tsx');assert.match(text,/\/admin\/experience\/studio/);assert.match(text,/Marketplace Studio/)})
test('developer contract is machine readable and immutable invariants are explicit',()=>{const text=source('developer-contract.ts');assert.match(text,/foreignCodeExecution:\s*false/);assert.match(text,/fakeBusinessData:\s*false/);assert.match(text,/localStoragePersistence:\s*false/);assert.match(text,/candidateBeforeApply:\s*true/);assert.match(text,/noSilentLoss:\s*true/)})
test('golden import fixture covers responsive design and interactive HTML',()=>{const html=read('scripts/angelcare-marketplace/fixtures/universal-studio-golden.html'),css=read('scripts/angelcare-marketplace/fixtures/universal-studio-golden.css');assert.match(html,/<section class="hero">/);assert.match(html,/<details>/);assert.match(html,/<form>/);assert.match(css,/--golden-navy/);assert.match(css,/@media\(max-width:767px\)/)})
test('no SQL or migration is added by Universal Studio',()=>{const dir=path.join(root,'supabase/migrations');const added=fs.existsSync(dir)?fs.readdirSync(dir).filter(name=>/universal.*studio|puck/i.test(name)):[];assert.deepEqual(added,[])})
