const baseUrl=process.env.APP_BASE_URL||'http://localhost:3000';async function j(path,init){const r=await fetch(`${baseUrl}${path}`,init);const t=await r.text();try{return JSON.parse(t)}catch{throw new Error(`${path} did not return JSON: ${t.slice(0,120)}`)}}async function main(){const summary=await j('/api/saas-factory/supabase/summary');if(!summary||!summary.metrics)throw new Error('No summary metrics returned');console.log('source:',summary.sourceConfidence);console.log('resources:',summary.metrics.resources);console.log('liveScan:',summary.liveScan);if(summary.sourceConfidence!=='live')console.warn('Live scan is not active yet. Run database/20260602_saas_factory_supabase_live_introspection.sql in Supabase and verify env service role keys.');const scan=await j('/api/saas-factory/supabase/scan',{method:'POST'});console.log('scan ok:',scan.ok);console.log('Supabase live introspection smoke completed.')}main().catch(e=>{console.error(e);process.exit(1)})
