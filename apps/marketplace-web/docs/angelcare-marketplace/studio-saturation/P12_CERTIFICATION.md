# P12 Certification

- Existing Experience Core dependency graph reused.
- Tagged runtime reads implemented.
- Automatic invalidation wired to canonical mutation authorities.
- Admin preview bypasses public runtime caches.
- No SQL, no migration, no shadow dependency store.
