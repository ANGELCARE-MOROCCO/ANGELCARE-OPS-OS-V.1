# Acceptance

MZ13 is accepted when only approved immutable strategies compile; all nineteen mandatory output categories exist; approval conditions, capacity, assignments and dependency graph are enforced; conflicts are explainable; idempotency, recompile, partial recompile, delta and rollback exist; all objects trace to source strategy and approval; external actions remain zero; static, scenario, SQL and TypeScript gates pass.
