# Data dictionary

The migration adds 29 additive tables: two run/blueprint tables, twenty-two compiled object/control tables, version/delta/rollback/audit tables, and one Shadow propagation-package table. Every operational object retains tenant, run, strategy version, approval decision, compiler version, code, status, payload and timestamps.
