# Known limitations

MZ13 intentionally stops at a propagation-ready internal package. Live adapter execution belongs to MZ14. Staff assignment uses available user/role/capacity data; when no concrete eligible user is available, the compiler records a governed assignment proposal or blocker rather than inventing an owner. Account plans are created only for governed account IDs; otherwise segment/cohort plans are used.
