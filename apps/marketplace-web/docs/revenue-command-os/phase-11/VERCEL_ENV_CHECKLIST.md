# Vercel environment checklist MZ11

Reuse MZ10.1 Gemini variables. Add optional `REVENUE_OS_COUNCIL_MAX_CONCURRENT_AGENTS=2` and `REVENUE_OS_COUNCIL_ENABLED=true`. Keep execution mode `shadow` and external actions `false`.
