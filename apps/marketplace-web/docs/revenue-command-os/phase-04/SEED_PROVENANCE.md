# Seed provenance

The Phase 4 seed is a contract fixture for safe first rendering and validation. It models known AngelCare application domains and existing table names detected in the uploaded monorepo. Example signal records are synthetic operational scenarios, explicitly marked `seed` and `shadowOnly`; they are not represented as live customer facts.
