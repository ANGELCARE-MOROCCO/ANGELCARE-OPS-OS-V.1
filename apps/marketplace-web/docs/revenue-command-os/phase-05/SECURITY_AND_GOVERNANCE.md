# Security and governance

- Shadow-only runtime.
- External actions disabled.
- No secrets in command context.
- RLS on every MZ05 table.
- Direct anon/authenticated table access revoked.
- Server-side service-role access only.
- Approval classes do not grant external authority.
- Published versions are immutable.
- Tenant, actor, permission and tool boundaries are evaluated before planning.
