# Rollback

The installer creates a timestamped application backup and prints its location. Restore that backup with the generated rollback command. Apply `ROLLBACK.sql` manually in Supabase SQL Editor only when database rollback is intended. MZ01–MZ04 tables are not removed.
