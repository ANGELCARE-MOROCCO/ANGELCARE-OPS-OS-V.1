# MZ06 dependency map

MZ06 must register 300 deeply tested command objects through the MZ05 schema, taxonomy, versioning, eligibility, tool permissions, validators, approval classes, router and test harness. MZ06 must not bypass the registry with loose prompts.
