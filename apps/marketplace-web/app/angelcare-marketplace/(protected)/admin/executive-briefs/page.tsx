import { requireMarketplacePageContext } from '@/angelcare-marketplace/auth/context'
import { PageHeader, StatePanel, StatusChip } from '@/angelcare-marketplace/design-system/ui'
import { listExecutiveBriefs } from '@/angelcare-marketplace/sovereign-control/repository'
export default async function Page(){await requireMarketplacePageContext('marketplace.backoffice.briefs.view');const items=await listExecutiveBriefs();return <><PageHeader eyebrow="EXECUTIVE INTELLIGENCE" title="Briefs de direction" description="Synthèses versionnées, risques, décisions et propriétaires."/>{items.length?<div style={{display:'grid',gap:16}}>{items.map(item=><article key={item.id} style={{background:'#fff',border:'1px solid #dce4ee',borderRadius:20,padding:24}}><div style={{display:'flex',justifyContent:'space-between',gap:16}}><div><h2>{item.title}</h2><p>{item.summary}</p></div><StatusChip status={item.status}/></div></article>)}</div>:<StatePanel type="empty" title="Aucun brief publié" text="Les briefs apparaîtront après création et revue par les rôles autorisés."/>}</>}
