import { requireMarketplacePageContext } from '@/angelcare-marketplace/auth/context';import { CommercialCommand } from '@/angelcare-marketplace/commercial-pipeline/components/CommercialCommand';import { commercialSummary,listLeads,listOpportunities,listQuotes } from '@/angelcare-marketplace/commercial-pipeline/repository'
export default async function Page(){await requireMarketplacePageContext('marketplace.crm.view');const [summary,leads,opportunities,quotes]=await Promise.all([commercialSummary(),listLeads(),listOpportunities(),listQuotes()]);return <CommercialCommand summary={summary} leads={leads} opportunities={opportunities} quotes={quotes}/>}
