import { notFound } from 'next/navigation';import { PublicDevelopmentExperience } from '@/angelcare-marketplace/development-engine/components/PublicDevelopmentExperience';import { listDevelopmentActivities,listDevelopmentCategories,listDevelopmentKits } from '@/angelcare-marketplace/development-engine/repository'
export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!['fr','en','ar'].includes(locale))notFound();const [categories,activities,kits]=await Promise.all([listDevelopmentCategories(),listDevelopmentActivities({status:'published'}),listDevelopmentKits()]);return <PublicDevelopmentExperience locale={locale as 'fr'|'en'|'ar'} categories={categories} activities={activities} kits={kits}/>}
