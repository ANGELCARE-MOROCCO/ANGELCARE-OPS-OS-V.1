import { PublicEstablishmentsExperience } from '@/angelcare-marketplace/b2b-verticals/components/PublicEstablishmentsExperience'
import { publicVerticalSnapshot } from '@/angelcare-marketplace/b2b-verticals/repository'
import { getPublishedSurface } from '@/angelcare-marketplace/total-commerce-control/repository'
export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale:raw}=await params;const locale=raw==='ar'?'ar':raw==='en'?'en':'fr';const [snapshot,experience]=await Promise.all([publicVerticalSnapshot('establishment'),getPublishedSurface('establishments',{locale}).catch(()=>null)]);return <PublicEstablishmentsExperience locale={locale} mode="establishments" activePrograms={snapshot.activePrograms} organizations={snapshot.organizations} experience={experience}/>}
