import {notFound} from 'next/navigation';import {Storefront} from '@/angelcare-marketplace/catalog-discovery/components/Storefront';import {storefrontExperience} from '@/angelcare-marketplace/catalog-discovery/repository';import type {CatalogLocale} from '@/angelcare-marketplace/catalog-discovery/types';export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!['fr','en','ar'].includes(locale))notFound();return <Storefront experience={await storefrontExperience({locale:locale as CatalogLocale,key:'families'})}/>}