import { redirect } from 'next/navigation'
import Link from 'next/link'
import styles from '@/components/angelcare360/material-command/MaterialCommand.module.css'
import { MaterialCommandShell,SectionHero,formatMad } from '@/components/angelcare360/material-command/MaterialCommandShell'
import { getMaterialSnapshot } from '@/lib/angelcare360/server/inventory-material-command'
export default async function DepletionPage(){const s=await getMaterialSnapshot();if(!s)redirect('/angelcare-360-command-center');const out=s.items.filter(i=>i.currentStock<=0||i.health==='critical');const low=s.items.filter(i=>i.health==='pressure');const exceptions=s.items.filter(i=>i.health==='exception');const unassigned=s.items.filter(i=>!i.responsibleStaffId&&!['inactive','archived'].includes(i.status));const items=[...out,...exceptions,...low].filter((item,index,array)=>array.findIndex(candidate=>candidate.id===item.id)===index);return <MaterialCommandShell schoolName={s.schoolName} academicYearLabel={s.academicYearLabel} integrity={s.integrity} activePath="/angelcare-360-command-center/inventaire/stock-bas"><section className={styles.sectionPage}><SectionHero eyebrow="REPLENISHMENT & EXCEPTION COMMAND" title="Stock critique & exceptions" description="La Watchtower sépare rupture, seuil bas, dommage, perte et responsabilité. Elle n’invente ni prévision de consommation, ni commande fournisseur automatique."/><div className={styles.operatorStrip} style={{padding:0,marginBottom:18}}><div className={styles.operatorMetric} data-tone={low.length?'warning':'success'}><span>Sous seuil</span><strong>{low.length}</strong><small>réapprovisionnement à examiner</small></div><div className={styles.operatorMetric} data-tone={out.length?'danger':'success'}><span>Ruptures</span><strong>{out.length}</strong><small>stock nul / critique</small></div><div className={styles.operatorMetric} data-tone={s.totals.damaged?'danger':'success'}><span>Endommagé</span><strong>{s.totals.damaged}</strong><small>état persistant dommage</small></div><div className={styles.operatorMetric} data-tone={s.totals.lost?'danger':'success'}><span>Perdu</span><strong>{s.totals.lost}</strong><small>état persistant perte</small></div><div className={styles.operatorMetric} data-tone={unassigned.length?'warning':'success'}><span>Sans responsable</span><strong>{unassigned.length}</strong><small>gouvernance matérielle</small></div><div className={styles.operatorMetric}><span>Valeur exposée</span><strong>{formatMad(items.reduce((n,i)=>n+i.indicativeValue,0))}</strong><small>indicative, non comptable</small></div></div><div className={styles.grid3}><section className={styles.sectionPanel}><div className={styles.panelHead}><div><p className={styles.eyebrow}>RUPTURE</p><h2>Stock nul / critique</h2><p>Articles dont la disponibilité est directement compromise.</p></div><span className={styles.panelMeta}>{out.length}</span></div><div className={styles.categoryList}>{out.length?out.map(i=><Link className={styles.materialCard} data-health="critical" href={i.detailHref} key={i.id}><div className={styles.materialTop}><span className={styles.code}>{i.code}</span><span className={styles.pill} data-tone="danger">Rupture</span></div><strong>{i.label}</strong><p>{i.categoryLabel||'Sans catégorie'} · stock {i.currentStock} {i.unit}</p></Link>):<div className={styles.empty}><div><strong>Aucune rupture</strong><p>Aucun article actif n’est actuellement à stock nul.</p></div></div>}</div></section><section className={styles.sectionPanel}><div className={styles.panelHead}><div><p className={styles.eyebrow}>RÉAPPROVISIONNEMENT</p><h2>Sous seuil</h2><p>Le signal repose uniquement sur le seuil enregistré.</p></div><span className={styles.panelMeta}>{low.length}</span></div><div className={styles.categoryList}>{low.length?low.map(i=><Link className={styles.materialCard} data-health="pressure" href={i.detailHref} key={i.id}><div className={styles.materialTop}><span className={styles.code}>{i.code}</span><span className={styles.pill} data-tone="warning">Sous seuil</span></div><strong>{i.label}</strong><p>Stock {i.currentStock} · seuil {i.reorderLevel} · déficit {Math.max(0,i.reorderLevel-i.currentStock)}</p></Link>):<div className={styles.empty}><div><strong>Aucun seuil bas</strong><p>Les articles actifs restent au-dessus de leur seuil enregistré.</p></div></div>}</div></section><section className={styles.sectionPanel}><div className={styles.panelHead}><div><p className={styles.eyebrow}>EXCEPTIONS</p><h2>Dommage / perte</h2><p>État matériel persistant nécessitant une décision humaine.</p></div><span className={styles.panelMeta}>{exceptions.length}</span></div><div className={styles.categoryList}>{exceptions.length?exceptions.map(i=><Link className={styles.materialCard} data-health="exception" href={i.detailHref} key={i.id}><div className={styles.materialTop}><span className={styles.code}>{i.code}</span><span className={styles.pill} data-tone="danger">{i.status}</span></div><strong>{i.label}</strong><p>{i.categoryLabel||'Sans catégorie'} · {i.responsibleStaffName||'sans responsable'}</p></Link>):<div className={styles.empty}><div><strong>Aucune exception</strong><p>Aucun article n’est actuellement déclaré endommagé ou perdu.</p></div></div>}</div></section></div></section></MaterialCommandShell>}
