import { notFound,redirect } from 'next/navigation'
import styles from '@/components/angelcare360/material-command/MaterialCommand.module.css'
import { MaterialDossier } from '@/components/angelcare360/material-command/MaterialDossier'
import { MaterialCommandShell,SectionHero } from '@/components/angelcare360/material-command/MaterialCommandShell'
import { getMaterialIntegrityStatus,getMaterialItemDossier,listMaterialCategories,listMaterialStaff } from '@/lib/angelcare360/server/inventory-material-command'
import { getMaterialCommandContext } from '../../_utils'
export default async function ArticleDossierPage({params}:{params:Promise<{id:string}>}){const{id}=await params;const context=await getMaterialCommandContext();const[dossier,categories,staff,integrity]=await Promise.all([getMaterialItemDossier(id,{schoolId:context.school.id}),listMaterialCategories({schoolId:context.school.id}),listMaterialStaff({schoolId:context.school.id}),getMaterialIntegrityStatus()]);if(!dossier)notFound();return <MaterialCommandShell schoolName={context.school.name} academicYearLabel={context.academicYear?.label||null} integrity={integrity} activePath="/angelcare-360-command-center/inventaire/articles"><section className={styles.sectionPage}><SectionHero eyebrow="MATERIAL DOSSIER" title={dossier.item.label} description="Portrait matériel, position de stock, gouvernance, mouvements et preuve d’audit réunis dans un même dossier."/><MaterialDossier schoolId={context.school.id} dossier={dossier} categories={categories} staff={staff} integrity={integrity}/></section></MaterialCommandShell>}
