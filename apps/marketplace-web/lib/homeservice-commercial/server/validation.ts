import{SCENARIO_LIMIT}from '../constants'
export class ValidationError extends Error{status=422;code='COMMERCIAL_VALIDATION';constructor(message:string){super(message)}}
const req=(v:unknown,n:string)=>{const s=String(v||'').trim();if(!s)throw new ValidationError(`${n} est obligatoire.`);return s}
export function validateRequest(x:any){const scenarioCount=Number(x?.scenarioCount||1),universe=String(x?.universe||'');if(!Number.isInteger(scenarioCount)||scenarioCount<1||scenarioCount>SCENARIO_LIMIT)throw new ValidationError('Nombre de scénarios: 1 à 10.');if(!['b2c','b2b'].includes(universe))throw new ValidationError('Univers invalide.');return{title:req(x?.title,'Titre'),technicalPlanId:req(x?.technicalPlanId,'Plan technique'),technicalPlanVersionId:req(x?.technicalPlanVersionId,'Version technique'),universe:universe as'b2c'|'b2b',customerSegment:req(x?.customerSegment,'Segment'),commercialObjective:req(x?.commercialObjective,'Objectif'),scenarioCount,constraints:typeof x?.constraints==='object'?x.constraints:{}}}
export function transition(current:string,next:string){const a:Record<string,string[]>={draft:['composed','archived'],composed:['pricing_incomplete','review_required','correction_required'],pricing_incomplete:['composed','correction_required'],review_required:['correction_required','approval_requested'],correction_required:['composed','review_required'],approval_requested:['approved','rejected','correction_required'],approved:['published','superseded','archived'],published:['suspended','superseded','retired'],suspended:['published','retired'],superseded:['archived'],retired:['archived']};if(!(a[current]||[]).includes(next))throw new ValidationError(`Transition interdite: ${current} → ${next}.`)}
