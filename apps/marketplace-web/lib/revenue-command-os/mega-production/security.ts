export const PROHIBITED_SECRET_KEY=/token|secret|password|authorization|api[_-]?key|credential|private[_-]?key|cookie/i
export function scanSecrets(value:unknown,path='root'):string[]{const findings:string[]=[];if(Array.isArray(value))value.forEach((item,index)=>findings.push(...scanSecrets(item,`${path}[${index}]`)));else if(value&&typeof value==='object')for(const [key,item] of Object.entries(value as Record<string,unknown>)){if(PROHIBITED_SECRET_KEY.test(key)&&typeof item==='string'&&item&&item!=='[REDACTED]')findings.push(`${path}.${key}`);findings.push(...scanSecrets(item,`${path}.${key}`))}return findings}
export function assertTenant(expected:string,actual:string):void{if(!expected||expected!==actual)throw new Error('cross_tenant_access_denied')}
export function promptInjectionSignals(text:string):string[]{const patterns=[/ignore previous/i,/system prompt/i,/reveal.*secret/i,/bypass.*approval/i,/disable.*guard/i,/execute.*without approval/i];return patterns.filter(pattern=>pattern.test(text)).map(pattern=>pattern.source)}
