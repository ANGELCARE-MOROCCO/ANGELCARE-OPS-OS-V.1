import { executionConfig } from './config'
export async function requestJson(url:string,init:RequestInit,timeoutMs=executionConfig().timeoutMs){const controller=new AbortController();const timer=setTimeout(()=>controller.abort(),timeoutMs);try{const response=await fetch(url,{...init,signal:controller.signal,cache:'no-store'});const text=await response.text();let body:unknown={};try{body=text?JSON.parse(text):{}}catch{body={raw:text.slice(0,4000)}}return{ok:response.ok,status:response.status,headers:response.headers,body}}finally{clearTimeout(timer)}}
export async function googleAccessToken(){const clientId=process.env.GOOGLE_CLIENT_ID,clientSecret=process.env.GOOGLE_CLIENT_SECRET,refreshToken=process.env.GOOGLE_REFRESH_TOKEN;if(!clientId||!clientSecret||!refreshToken)throw new Error('GOOGLE_OAUTH_CREDENTIALS_MISSING');const r=await requestJson('https://oauth2.googleapis.com/token',{method:'POST',headers:{'content-type':'application/x-www-form-urlencoded'},body:new URLSearchParams({client_id:clientId,client_secret:clientSecret,refresh_token:refreshToken,grant_type:'refresh_token'})});if(!r.ok)throw new Error(`GOOGLE_TOKEN_${r.status}`);const token=(r.body as any)?.access_token;if(!token)throw new Error('GOOGLE_ACCESS_TOKEN_MISSING');return String(token)}
