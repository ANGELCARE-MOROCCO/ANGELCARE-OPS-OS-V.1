import crypto from 'node:crypto';import type {RevenueStrategy} from './types';import {validateStrategyPreflight} from './guardrails';
export function combineStrategies(a:RevenueStrategy,b:RevenueStrategy):RevenueStrategy{if(a.objectiveId!==b.objectiveId)throw new Error('OBJECTIVE_MISMATCH');const combined:RevenueStrategy={...a,id:crypto.randomUUID(),code:`${a.code}-HYBRID`,archetype:`hybrid:${a.archetype}+${b.archetype}`,thesis:`${a.thesis} En complément, ${b.thesis}`,channelMix:[...a.channelMix,...b.channelMix].filter((x,i,arr)=>arr.findIndex(y=>y.channel===x.channel&&y.purpose===x.purpose)===i),campaignSequence:[...a.campaignSequence,...b.campaignSequence],commandPortfolio:[...a.commandPortfolio,...b.commandPortfolio].filter((x,i,arr)=>arr.findIndex(y=>y.commandCode===x.commandCode&&y.version===x.version)===i),risks:[...a.risks,...b.risks],fallbackPlan:[...new Set([...a.fallbackPlan,...b.fallbackPlan])],stopConditions:[...new Set([...a.stopConditions,...b.stopConditions])],combinationLineage:[a.id,b.id],status:'combined_candidate',version:'1.0.0',parentVersion:undefined,createdAt:new Date().toISOString()};const result=validateStrategyPreflight(combined);if(!result.pass)throw new Error(`COMBINATION_INVALID:${result.errors.join('|')}`);return combined;}
