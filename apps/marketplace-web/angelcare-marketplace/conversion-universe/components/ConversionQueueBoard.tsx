import Link from 'next/link'
import { AlertTriangle, ArrowRight, BadgeCheck, Clock3, FileText, MapPin, ShieldCheck } from 'lucide-react'
import type { ConversionSession } from '../types'
import styles from '../conversion.module.css'

export function ConversionQueueBoard({ title, eyebrow, sessions, mode = 'sessions' }: { title: string; eyebrow: string; sessions: ConversionSession[]; mode?: 'sessions' | 'exceptions' | 'holds' | 'abandonment' }) {
  return <div className={styles.queueRoot} data-mode={mode}>
    <section className={styles.queueHero}><div><span>{eyebrow}</span><h1>{title}</h1><p>Chaque ligne conserve le contexte de l’offre, le territoire, le prix, la disponibilité, les consentements et le handover associé.</p></div><div><strong>{sessions.length}</strong><span>dossiers visibles</span></div></section>
    <div className={styles.queueToolbar}><div><button type="button">Toutes</button><button type="button">Action requise</button><button type="button">Prêtes</button><button type="button">Échecs</button></div><Link href="/angelcare-marketplace/admin/conversion">Retour au cockpit <ArrowRight size={16}/></Link></div>
    <section className={styles.queueGrid}>{sessions.map(session=><article key={session.id} data-status={session.status}><header><div><span>{session.journey.replaceAll('_',' ')}</span><h2>{session.item?.name||session.public_reference}</h2></div><b>{session.public_reference}</b></header><div className={styles.queueSignals}><span><MapPin size={14}/>{session.territory_id||'global'}</span><span><Clock3 size={14}/>{new Date(session.last_activity_at).toLocaleString('fr-FR')}</span><span><ShieldCheck size={14}/>{session.consents?.filter(consent=>consent.accepted).length||0} consentements</span></div><div className={styles.queueEvidence}><div><small>Prix</small><b>{session.priceSnapshot?.status||'non revérifié'}</b></div><div><small>Disponibilité</small><b>{String(session.availability_result.status||'non revérifiée')}</b></div><div><small>Statut</small><b>{session.status}</b></div><div><small>Résultat</small><b>{session.outcome?.public_reference||'—'}</b></div></div>{session.failure_message?<p className={styles.failureMessage}><AlertTriangle size={16}/>{session.failure_message}</p>:null}<footer><span>{session.status==='ready'?<BadgeCheck size={16}/>:session.status==='failed'?<AlertTriangle size={16}/>:<FileText size={16}/>} {session.status}</span><Link href={`/angelcare-marketplace/admin/conversion/sessions/${session.id}`}>Ouvrir le dossier <ArrowRight size={16}/></Link></footer></article>)}{!sessions.length?<div className={styles.queueEmpty}>Aucune session ne correspond à ce périmètre.</div>:null}</section>
  </div>
}
