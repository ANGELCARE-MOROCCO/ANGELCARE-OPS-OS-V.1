import type { CommercialAccount,CommercialLead } from '../types';import s from '../commercial.module.css'
export function LeadAccountCommand({leads,accounts}:{leads:CommercialLead[];accounts:CommercialAccount[]}){return <div className={s.shell}><section className={s.hero}><div className={s.eyebrow}>LEADS & ACCOUNTS</div><h1 className={s.title}>Identité commerciale, propriétaire et prochaine action</h1><p className={s.lead}>Aucun lead ou compte n’est réduit à une note libre : source, type, territoire, statut, owner et prochaine action restent exploitables.</p></section><section className={s.panel}><h2>Leads</h2><div className={s.rows}>{leads.map(x=><div className={s.row} key={x.id}><div><strong>{x.name}</strong><div className={s.subtle}>{x.organization_name||x.lead_type}</div></div><span>{x.source}</span><span>{x.status}</span><span>{x.next_action||'À définir'}</span><span>{x.public_reference}</span></div>)}{!leads.length&&<div className={s.empty}>Aucun lead réel.</div>}</div></section><section className={s.panel}><h2>Comptes</h2><div className={s.rows}>{accounts.map(x=><div className={s.row} key={x.id}><div><strong>{x.display_name}</strong><div className={s.subtle}>{x.legal_name}</div></div><span>{x.account_type}</span><span>{x.status}</span><span className={s.status}>{x.health_status}</span><span>{x.public_reference}</span></div>)}{!accounts.length&&<div className={s.empty}>Aucun compte réel.</div>}</div></section></div>}
