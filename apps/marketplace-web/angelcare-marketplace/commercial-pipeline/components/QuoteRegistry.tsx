import type { CommercialQuote } from '../types';import s from '../commercial.module.css'
export function QuoteRegistry({quotes}:{quotes:CommercialQuote[]}){return <div className={s.shell}><section className={s.hero}><div className={s.eyebrow}>QUOTE & PROOF COMMAND</div><h1 className={s.title}>Devis versionnés, approbation et preuve commerciale</h1><p className={s.lead}>Les remises, totaux, validités, statuts et approbations ne peuvent être contournés par une simple modification visuelle.</p></section><section className={s.panel}><div className={s.rows}>{quotes.map(q=><div className={s.row} key={q.id}><div><strong>{q.public_reference}</strong><div className={s.subtle}>Version {q.version}</div></div><span>{q.quote_status}</span><span>{q.grand_total} {q.currency_label}</span><span className={s.status}>{q.approval_status}</span><span>{q.valid_until||'Sans date'}</span></div>)}{!quotes.length&&<div className={s.empty}>Aucun devis réel.</div>}</div></section></div>}
