import type {StorefrontExperience} from '@/angelcare-marketplace/catalog-discovery/types'
import type {PublicExperienceDensityMode} from './types'

export interface StorefrontMerchandisingPlan {density:PublicExperienceDensityMode;inventoryLimit:number;featuredLimit:number;collectionLimit:number;strategies:Array<{slot:string;strategy:string;count:number;reason:string}>}
export function storefrontMerchandisingPlan(experience:StorefrontExperience,density:PublicExperienceDensityMode='hyper_commerce'):StorefrontMerchandisingPlan{const limits={premium:[12,6,3],commerce:[24,8,4],hyper_commerce:[36,12,6],festival:[48,16,8]}[density];const available=experience.items.filter(i=>i.availability_status!=='unavailable').length,featured=experience.featured.length;return{density,inventoryLimit:limits[0],featuredLimit:limits[1],collectionLimit:limits[2],strategies:[{slot:'featured_items',strategy:featured?'canonical_featured':'available_fallback',count:Math.min(featured||available,limits[1]),reason:featured?'Sélection featured canonique.':'Fallback vers offres disponibles.'},{slot:'inventory_items',strategy:'published_available',count:Math.min(available,limits[0]),reason:'Inventaire public canonique, borné par density mode.'},{slot:'collections',strategy:'canonical_collections',count:Math.min(experience.collections.length,limits[2]),reason:'Collections canoniques présentes.'},{slot:'facets',strategy:'live_facets',count:Object.keys(experience.facets).length,reason:'Facettes calculées par Catalog Discovery.'}]}}
