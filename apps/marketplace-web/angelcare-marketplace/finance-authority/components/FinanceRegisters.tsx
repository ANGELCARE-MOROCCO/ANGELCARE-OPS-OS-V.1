import { InvoiceReadinessDesk, MarginDecisionDesk, ReconciliationResolutionDesk } from './FinanceDecisionClients'
import styles from '../finance.module.css'
import type { InvoiceReadiness, MarginException, ReconciliationEvent, RevenueStream } from '../types'

const dh = (value: number) => `${new Intl.NumberFormat('fr-FR', { maximumFractionDigits: 2 }).format(value)} Dh`

export function RevenueAuthority({ items }: { items: RevenueStream[] }) {
  return <div className={styles.shell}><section className={styles.hero}><div><div className={styles.eyebrow}>REVENUE STREAM AUTHORITY</div><h1 className={styles.title}>Du quote au paid, sans perdre l’objet source.</h1><p className={styles.copy}>Chaque ligne conserve territoire, tenant, organisation, customer, price-book lineage, owner et événement de reconnaissance.</p></div><div className={styles.moneyPanel}><small>Total net observé</small><strong>{dh(items.reduce((sum, item) => sum + item.net_amount, 0))}</strong><span>{items.length} revenue stream(s)</span></div></section><section className={styles.panel}><div className={styles.panelBody}><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Stream</th><th>Source</th><th>Territoire</th><th>Statut</th><th>Gross</th><th>Discount</th><th>Commission</th><th>Payable</th><th>Net</th><th>Marge</th></tr></thead><tbody>{items.map((item) => <tr key={item.id}><td><strong>{item.public_reference}</strong><br/><small>{item.stream_type}</small></td><td>{item.source_object_type}<br/><small>{item.source_object_id}</small></td><td>{item.territory_id || 'Global'}</td><td><span className={styles.status}>{item.status}</span></td><td>{dh(item.gross_amount)}</td><td>{dh(item.discount_amount)}</td><td>{dh(item.commission_amount)}</td><td>{dh(item.provider_payable_amount)}</td><td><strong>{dh(item.net_amount)}</strong></td><td>{item.margin_rate.toFixed(1)}%</td></tr>)}</tbody></table></div></div></section></div>
}

export function MarginAuthority({ items, canDecide }: { items: MarginException[]; canDecide: boolean }) {
  return <div className={styles.shell}><section className={styles.hero}><div><div className={styles.eyebrow}>MARGIN EXCEPTION AUTHORITY</div><h1 className={styles.title}>Sous le floor, aucune vente sans décision séparée.</h1><p className={styles.copy}>La demande expose prix configuré, prix proposé, cost basis, discount, marge et justification.</p></div><div className={styles.moneyPanel}><small>Exceptions en attente</small><strong>{items.filter((item) => item.status === 'pending').length}</strong><span>Décision Finance indépendante requise</span></div></section><section className={styles.panel}><div className={styles.panelBody}><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Exception</th><th>Objet</th><th>Prix config.</th><th>Prix proposé</th><th>Cost basis</th><th>Floor</th><th>Marge proposée</th><th>Discount</th><th>Statut</th><th>Raison</th></tr></thead><tbody>{items.map((item) => <tr key={item.id}><td><strong>{item.public_reference}</strong></td><td>{item.source_object_type}<br/><small>{item.source_object_id}</small></td><td>{dh(item.configured_price)}</td><td>{dh(item.proposed_price)}</td><td>{dh(item.cost_basis)}</td><td>{item.floor_margin}%</td><td><span className={styles.status} data-risk={item.proposed_margin < item.floor_margin}>{item.proposed_margin}%</span></td><td>{item.discount_rate}%</td><td>{item.status}</td><td>{item.reason}</td></tr>)}</tbody></table></div></div></section><MarginDecisionDesk items={items} canDecide={canDecide}/></div>
}

export function InvoiceReadinessAuthority({ items, canEvaluate }: { items: InvoiceReadiness[]; canEvaluate: boolean }) {
  return <div className={styles.shell}><section className={styles.hero}><div><div className={styles.eyebrow}>INVOICE READINESS</div><h1 className={styles.title}>L’invoice-ready est calculé depuis livraison, validation et preuve.</h1><p className={styles.copy}>Les blockers restent visibles jusqu’à résolution et chaque readiness conserve son objet canonique.</p></div><div className={styles.moneyPanel}><small>Montant ready</small><strong>{dh(items.filter((item) => item.status === 'ready').reduce((sum, item) => sum + item.amount, 0))}</strong><span>{items.filter((item) => item.status !== 'ready').length} dossier(s) bloqué(s)</span></div></section><section className={styles.panel}><div className={styles.panelBody}><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Dossier</th><th>Source</th><th>Montant</th><th>Statut</th><th>Preuves manquantes</th><th>Blockers</th><th>Finance owner</th><th>Invoice ref.</th></tr></thead><tbody>{items.map((item) => <tr key={item.id}><td><strong>{item.public_reference}</strong></td><td>{item.source_object_type}<br/><small>{item.source_object_id}</small></td><td>{dh(item.amount)}</td><td><span className={styles.status} data-risk={item.status !== 'ready'}>{item.status}</span></td><td>{item.missing_evidence.join(' · ') || 'Aucune'}</td><td>{item.blockers.join(' · ') || 'Aucun'}</td><td>{item.finance_owner_id || 'À assigner'}</td><td>{item.invoiced_reference || '—'}</td></tr>)}</tbody></table></div></div></section><InvoiceReadinessDesk items={items} canEvaluate={canEvaluate}/></div>
}

export function ReconciliationAuthority({ items, canResolve }: { items: ReconciliationEvent[]; canResolve: boolean }) {
  return <div className={styles.shell}><section className={styles.hero}><div><div className={styles.eyebrow}>RECONCILIATION CONTROL</div><h1 className={styles.title}>Comparer l’attendu et l’observé, puis assigner l’écart.</h1><p className={styles.copy}>Les événements de rapprochement préservent source, cible, devise, différence, owner et résolution.</p></div><div className={styles.moneyPanel}><small>Différence ouverte</small><strong>{dh(items.filter((item) => item.status === 'exception').reduce((sum, item) => sum + Math.abs(item.difference_amount), 0))}</strong><span>{items.filter((item) => item.status === 'exception').length} exception(s)</span></div></section><section className={styles.panel}><div className={styles.panelBody}><div className={styles.tableWrap}><table className={styles.table}><thead><tr><th>Événement</th><th>Source</th><th>Cible</th><th>Attendu</th><th>Observé</th><th>Différence</th><th>Statut</th><th>Owner</th><th>Résolution</th></tr></thead><tbody>{items.map((item) => <tr key={item.id}><td><strong>{item.public_reference}</strong><br/><small>{item.event_type}</small></td><td>{item.source_system}<br/><small>{item.source_reference}</small></td><td>{item.target_system}<br/><small>{item.target_reference || '—'}</small></td><td>{dh(item.expected_amount)}</td><td>{dh(item.observed_amount)}</td><td><span className={styles.status} data-risk={item.difference_amount !== 0}>{dh(item.difference_amount)}</span></td><td>{item.status}</td><td>{item.owner_id || 'À assigner'}</td><td>{item.resolution_note || '—'}</td></tr>)}</tbody></table></div></div></section><ReconciliationResolutionDesk items={items} canResolve={canResolve}/></div>
}
