import type { TranslationCsvRow } from './types'
const COLUMNS:(keyof TranslationCsvRow)[]=['candidate_id','translation_key','domain','workflow','route','source_type','source_location','territory_code','tenant_reference','content_type','sensitivity','source_locale','source_text_fr','source_hash','source_version','context_description','translator_note','variables','character_limit','target_locale','current_translation','proposed_translation','translation_status','reviewer_note']
function safe(value:string){ let v=String(value??''); if(/^[=+\-@]/.test(v))v=`'${v}`; return /[",\n\r]/.test(v)?`"${v.replace(/"/g,'""')}"`:v }
export function toCsv(rows:TranslationCsvRow[]):string{ return '\uFEFF'+[COLUMNS.join(','),...rows.map(row=>COLUMNS.map(c=>safe(row[c])).join(','))].join('\r\n') }
export function parseCsv(text:string):Record<string,string>[]{ const src=text.replace(/^\uFEFF/,''); const rows:string[][]=[]; let row:string[]=[],cell='',quoted=false; for(let i=0;i<src.length;i++){ const ch=src[i]; if(quoted){ if(ch==='"'&&src[i+1]==='"'){cell+='"';i++}else if(ch==='"')quoted=false;else cell+=ch }else if(ch==='"')quoted=true;else if(ch===','){row.push(cell);cell=''}else if(ch==='\n'){row.push(cell.replace(/\r$/,''));rows.push(row);row=[];cell=''}else cell+=ch } row.push(cell); if(row.some(Boolean))rows.push(row); const headers=rows.shift()||[]; return rows.map(values=>Object.fromEntries(headers.map((h,i)=>[h,values[i]||'']))) }
export function requiredColumnsMissing(headers:string[]):string[]{ return COLUMNS.filter(c=>!headers.includes(c)) }
