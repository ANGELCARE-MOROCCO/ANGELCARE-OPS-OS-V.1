import styles from '../localization.module.css'
export function PlaceholderWorkspace({eyebrow,title,description,items}:{eyebrow:string;title:string;description:string;items:string[]}){return <div className={styles.page}><section className={styles.hero}><div className={styles.eyebrow}>{eyebrow}</div><h1>{title}</h1><p>{description}</p></section><section className={styles.cards}>{items.map(i=><article className={styles.command} key={i}><h3>{i}</h3><p className={styles.muted}>Contrôle persistant, permissionné, audité et lié à la source française.</p></article>)}</section></div>}
