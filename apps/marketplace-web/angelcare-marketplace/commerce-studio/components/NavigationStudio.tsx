'use client'

import { useMemo, useState } from 'react'
import type { DragEvent } from 'react'
import { ExternalLink, GripVertical, Menu, Plus, Smartphone } from 'lucide-react'
import styles from '../commerce-studio.module.css'
import type { MediaAsset, NavigationItemRecord, NavigationMenuRecord } from '../types'
import { apiRequest, CheckField, Field, ImmediateAction, SelectField, StudioForm, TextArea, useStudioMutation } from './StudioClient'

export function NavigationStudio({ initialMenus, media, mode = 'header' }: { initialMenus: NavigationMenuRecord[]; media: MediaAsset[]; mode?: string }) {
  const [menus, setMenus] = useState(initialMenus)
  const [menuId,setMenuId]=useState(initialMenus.find((menu)=>menu.locale==='fr')?.id||initialMenus[0]?.id||'')
  const [selectedId,setSelectedId]=useState<string>('new')
  const [draggedId,setDraggedId]=useState<string|null>(null)
  const mutation=useStudioMutation()
  const menu=useMemo(()=>menus.find((item)=>item.id===menuId)||null,[menus,menuId])
  const selected=menu?.items?.find((item)=>item.id===selectedId)||null
  async function reorder(targetId:string){
    if(!menu||!draggedId||draggedId===targetId)return
    const ordered=[...(menu.items||[])].sort((a,b)=>a.sort_order-b.sort_order)
    const from=ordered.findIndex((item)=>item.id===draggedId)
    const to=ordered.findIndex((item)=>item.id===targetId)
    if(from<0||to<0)return
    const [moved]=ordered.splice(from,1);ordered.splice(to,0,moved)
    setMenus((current)=>current.map((entry)=>entry.id===menu.id?{...entry,items:ordered.map((item,index)=>({...item,sort_order:index*10}))}:entry))
    await mutation.run(()=>apiRequest('/api/angelcare-marketplace/admin/navigation/reorder',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({ordered_ids:ordered.map((item)=>item.id)})}),'Ordre du menu publié immédiatement.')
    setDraggedId(null)
  }
  return <main className={styles.shell}>
    <section className={styles.workspaceHero} data-accent="navigation"><div><span>NAVIGATION STUDIO · {mode.toUpperCase()}</span><h1>Header, mega-menu, mobile et footer sous contrôle.</h1><p>Libellés trilingues, hiérarchie, routes, images promotionnelles et publication directe.</p></div><div className={styles.workspaceStats}><Menu size={27}/><strong>{menu?.items?.length||0}</strong><span>items dans le menu</span></div></section>
    <section className={styles.navigationLayout}>
      <aside className={styles.menuRegistry}><header><span>MENUS</span><button type="button" onClick={()=>setSelectedId('new-menu')}><Plus size={15}/></button></header>{menus.map((item)=><button type="button" key={item.id} data-selected={item.id===menuId} onClick={()=>{setMenuId(item.id);setSelectedId('new')}}><strong>{item.name}</strong><span>{item.locale} · {item.status}</span></button>)}</aside>
      <section className={styles.menuCanvas}><div className={styles.menuPreview}><div className={styles.brandPreview}>ANGELCARE <i/></div>{menu?.items?.filter((item)=>!item.parent_id&&item.status==='active').sort((a,b)=>a.sort_order-b.sort_order).map((item)=><button type="button" key={item.id} onClick={()=>setSelectedId(item.id)}>{item.label}<span>{menu.items.filter((child)=>child.parent_id===item.id).length||''}</span></button>)}<i/><span className={styles.previewDevice} aria-label="Aperçu mobile"><Smartphone size={16}/></span></div><div className={styles.menuTree}><header><div><span>MENU TREE</span><h2>{menu?.name||'Navigation publique'}</h2></div><button type="button" onClick={()=>setSelectedId('new')}><Plus size={16}/> Ajouter un item</button></header>{menu?.items?.sort((a,b)=>a.sort_order-b.sort_order).map((item)=><button type="button" key={item.id} data-selected={selectedId===item.id} draggable onDragStart={()=>setDraggedId(item.id)} onDragOver={(event:DragEvent<HTMLButtonElement>)=>event.preventDefault()} onDrop={()=>void reorder(item.id)} style={{paddingInlineStart:`${18+(item.parent_id?28:0)}px`}} onClick={()=>setSelectedId(item.id)}><GripVertical size={16}/><span><strong>{item.label}</strong><small>{item.href}</small></span><i>{item.desktop_visible?'DESKTOP':''} {item.mobile_visible?'MOBILE':''}</i></button>)}</div></section>
      <aside className={styles.navigationInspector}>{selectedId==='new-menu'?<><span>NEW MENU</span><h2>Créer un menu</h2><StudioForm resource="navigation-menus" onSaved={(record)=>{setMenus((current)=>[...current,{...record,menu_key:String(record.menu_key||''),name:String(record.name||''),locale:(record.locale==='en'||record.locale==='ar'?record.locale:'fr'),territory_id:record.territory_id?String(record.territory_id):null,status:String(record.status||'published'),items:[]}]);setMenuId(record.id);setSelectedId('new')}} submitLabel="Créer et publier"><Field name="name" label="Nom interne" required/><Field name="menu_key" label="Clé"/><SelectField name="locale" label="Locale" options={['fr','en','ar']}/><input type="hidden" name="status" value="published"/></StudioForm></>:<><span>{selected?'ITEM INSPECTOR':'NEW ITEM'}</span><h2>{selected?.label||'Ajouter une entrée'}</h2><StudioForm resource="navigation-items" id={selected?.id} extraPayload={{menu_id:menuId}} onSaved={(record)=>setMenus((current)=>current.map((entry)=>entry.id===menuId?{...entry,items:selected?entry.items.map((item)=>item.id===record.id?record as NavigationItemRecord:item):[...(entry.items||[]),record as NavigationItemRecord]}:entry))}><Field name="label_fr" label="Libellé FR" defaultValue={selected?.label_fr||selected?.label} required/><div className={styles.formGrid}><Field name="label_en" label="Libellé EN" defaultValue={selected?.label_en}/><Field name="label_ar" label="Libellé AR" defaultValue={selected?.label_ar}/></div><Field name="href" label="Route ou URL" defaultValue={selected?.href} required/><SelectField name="parent_id" label="Parent / groupe" defaultValue={selected?.parent_id} options={[{value:'',label:'Niveau principal'},...(menu?.items?.filter((item)=>!item.parent_id).map((item)=>({value:item.id,label:item.label}))||[])]}/><div className={styles.formGrid}><Field name="sort_order" label="Ordre" type="number" defaultValue={selected?.sort_order||0}/><Field name="icon_key" label="Icône" defaultValue={selected?.icon_key}/></div><SelectField name="image_asset_id" label="Image promotionnelle Media Library" defaultValue={selected?.image_asset_id} options={[{value:'',label:'Aucune image'},...media.filter((asset)=>asset.media_type==='image').map((asset)=>({value:asset.id,label:asset.file_name}))]}/><div className={styles.formGrid}><SelectField name="visibility" label="Audience" defaultValue={selected?.visibility||'public'} options={['public','family','organization','professional','authenticated']}/><SelectField name="status" label="État live" defaultValue={selected?.status||'active'} options={['active','hidden','archived']}/></div><div className={styles.formGrid}><CheckField name="desktop_visible" label="Visible desktop" defaultChecked={selected?.desktop_visible??true}/><CheckField name="mobile_visible" label="Visible mobile" defaultChecked={selected?.mobile_visible??true}/></div></StudioForm>{selected?<div className={styles.actionBar}><ImmediateAction resource="navigation-items" id={selected.id} action={selected.status==='active'?'unpublish':'publish'} label={selected.status==='active'?'Masquer':'Afficher'}/><a href={selected.href} target="_blank"><ExternalLink size={14}/> Tester la route</a></div>:null}<div className={styles.routeWarnings}><strong>VALIDATION AUTOMATIQUE</strong><TextArea name="warnings" label="" defaultValue="Route interne contrôlée · hiérarchie sans boucle · locale cohérente · visibilité desktop/mobile"/></div></>}</aside>
    </section>
  </main>
}
