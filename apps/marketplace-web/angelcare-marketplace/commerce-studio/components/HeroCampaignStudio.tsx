'use client'

import { useMemo, useState } from 'react'
import { Eye, ImagePlus, Monitor, Plus, Smartphone, Tablet } from 'lucide-react'
import styles from '../commerce-studio.module.css'
import type { CommerceRecord, MediaAsset } from '../types'
import { Field, ImmediateAction, SelectField, StudioForm, TextArea } from './StudioClient'

export function HeroCampaignStudio({ initialCampaigns, media }: { initialCampaigns: CommerceRecord[]; media: MediaAsset[] }) {
  const [campaigns,setCampaigns]=useState(initialCampaigns)
  const [selectedId,setSelectedId]=useState(initialCampaigns[0]?.id||'new')
  const [device,setDevice]=useState<'desktop'|'tablet'|'mobile'>('desktop')
  const selected=useMemo(()=>campaigns.find((campaign)=>campaign.id===selectedId)||null,[campaigns,selectedId])
  const previewUrl=selected?String(device==='mobile'?selected.mobile_asset_url||selected.desktop_asset_url:device==='tablet'?selected.tablet_asset_url||selected.desktop_asset_url:selected.desktop_asset_url||''):''
  return <main className={styles.shell}>
    <section className={styles.workspaceHero} data-accent="homepage"><div><span>HERO & CAMPAIGN STUDIO</span><h1>Campagnes responsive, ciblées et immédiatement live.</h1><p>Images Media Library, messages trilingues, CTA, audience, territoire, priorité et calendrier.</p></div><div className={styles.deviceSwitch}><button type="button" data-active={device==='desktop'} onClick={()=>setDevice('desktop')}><Monitor size={17}/></button><button type="button" data-active={device==='tablet'} onClick={()=>setDevice('tablet')}><Tablet size={17}/></button><button type="button" data-active={device==='mobile'} onClick={()=>setDevice('mobile')}><Smartphone size={17}/></button></div></section>
    <section className={styles.heroStudioLayout}>
      <aside className={styles.campaignRegistry}><header><span>CAMPAIGNS</span><button type="button" onClick={()=>setSelectedId('new')}><Plus size={15}/></button></header>{campaigns.map((campaign)=><button type="button" key={campaign.id} data-selected={campaign.id===selectedId} onClick={()=>setSelectedId(campaign.id)}><strong>{String(campaign.title)}</strong><span>{String(campaign.locale)} · {String(campaign.status)}</span></button>)}</aside>
      <section className={styles.heroLivePreview} data-device={device} style={{backgroundImage:previewUrl?`linear-gradient(90deg,rgba(3,24,46,.84),rgba(3,24,46,.12)),url(${previewUrl})`:undefined}}><span>{String(selected?.eyebrow||'ANGELCARE MARKETPLACE')}</span><h2>{String(selected?.title||'Nouvelle campagne Hero')}</h2><p>{String(selected?.subtitle||'Configurez une campagne complète depuis le panneau administrateur.')}</p><b>{String(selected?.primary_cta_label||'Action principale')}</b></section>
      <aside className={styles.campaignInspector}><span>{selected?'EDIT CAMPAIGN':'NEW CAMPAIGN'}</span><h2>{String(selected?.title||'Créer une campagne')}</h2><StudioForm resource="homepage-campaigns" id={selected?.id} onSaved={(record)=>{setCampaigns((current)=>selected?current.map((item)=>item.id===record.id?record:item):[record,...current]);setSelectedId(record.id)}} submitLabel={selected?'Enregistrer la campagne':'Créer et activer'}><div className={styles.formGrid}><SelectField name="locale" label="Locale" defaultValue={String(selected?.locale||'fr')} options={['fr','en','ar']}/><SelectField name="audience" label="Audience" defaultValue={String(selected?.audience||'all')} options={['all','family','organization','professional']}/></div><Field name="eyebrow" label="Eyebrow" defaultValue={String(selected?.eyebrow||'')}/><Field name="title" label="Titre" defaultValue={String(selected?.title||'')} required/><TextArea name="subtitle" label="Sous-titre" defaultValue={String(selected?.subtitle||'')}/><div className={styles.formGrid}><Field name="primary_cta_label" label="CTA principal" defaultValue={String(selected?.primary_cta_label||'Découvrir')} required/><Field name="primary_cta_href" label="Destination" defaultValue={String(selected?.primary_cta_href||'/angelcare-marketplace/fr/marketplace')} required/></div><div className={styles.formGrid}><Field name="secondary_cta_label" label="CTA secondaire" defaultValue={String(selected?.secondary_cta_label||'')}/><Field name="secondary_cta_href" label="Destination secondaire" defaultValue={String(selected?.secondary_cta_href||'')}/></div><SelectField name="desktop_asset_url" label="Image desktop" defaultValue={String(selected?.desktop_asset_url||'')} options={[{value:'',label:'Sélectionner dans Media Library'},...media.filter((asset)=>asset.media_type==='image').map((asset)=>({value:asset.desktop_url,label:asset.file_name}))]}/><div className={styles.formGrid}><SelectField name="tablet_asset_url" label="Image tablette" defaultValue={String(selected?.tablet_asset_url||'')} options={[{value:'',label:'Dérivée automatiquement'},...media.filter((asset)=>asset.media_type==='image').map((asset)=>({value:asset.tablet_url||asset.desktop_url,label:asset.file_name}))]}/><SelectField name="mobile_asset_url" label="Image mobile" defaultValue={String(selected?.mobile_asset_url||'')} options={[{value:'',label:'Dérivée automatiquement'},...media.filter((asset)=>asset.media_type==='image').map((asset)=>({value:asset.mobile_url||asset.desktop_url,label:asset.file_name}))]}/></div><div className={styles.formGrid}><Field name="priority" label="Priorité" type="number" defaultValue={Number(selected?.priority||100)}/><SelectField name="status" label="Statut" defaultValue={String(selected?.status||'active')} options={['draft','configured','scheduled','active','paused','expired','archived']}/></div><div className={styles.formGrid}><Field name="starts_at" label="Début" type="datetime-local"/><Field name="ends_at" label="Fin" type="datetime-local"/></div></StudioForm>{selected?<div className={styles.actionBar}><ImmediateAction resource="homepage-campaigns" id={selected.id} action={selected.status==='active'?'unpublish':'publish'} label={selected.status==='active'?'Mettre en pause':'Activer maintenant'}/><ImmediateAction resource="homepage-campaigns" id={selected.id} action="duplicate" label="Dupliquer"/><a href="/angelcare-marketplace/fr" target="_blank"><Eye size={14}/> Voir live</a></div>:<div className={styles.empty}><ImagePlus size={24}/> Sélectionnez les médias responsive et publiez immédiatement.</div>}</aside>
    </section>
  </main>
}
