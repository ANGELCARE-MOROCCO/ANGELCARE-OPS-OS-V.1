import type { DevelopmentKit } from '../types';import s from '../development.module.css'
export function KitPortfolio({kits}:{kits:DevelopmentKit[]}){return <div className={s.shell}><section className={s.hero}><div className={s.eyebrow}>KITS & BUNDLES</div><h1 className={s.title}>Portefeuille de kits gouverné</h1><p className={s.lead}>Du starter familial au kit institutionnel, chaque bundle reste lié aux usages, alternatives, spécifications et statuts réels.</p></section><section className={s.cards}>{kits.map(k=><article className={s.card} key={k.id}><span className={s.status}>{k.status}</span><h3>{k.name_fr}</h3><p className={s.subtle}>{k.description_fr||'Description à compléter'} · {k.item_count||0} éléments</p></article>)}{!kits.length&&<div className={s.empty}>Aucun kit réel enregistré.</div>}</section></div>}
