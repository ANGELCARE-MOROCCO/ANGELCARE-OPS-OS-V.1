import { requireMarketplaceApiContext } from '../auth/context'
import { apiFailure, apiSuccess, cleanOptionalText, cleanText, parseJsonObject, requestId, requireText } from '../server/request'
import { MarketplaceError } from '../server/errors'
import {
  archiveSymbol, archiveTemplate, createPage, createPreview, getPageDetail, instantiateTemplate, listCtas, listDependencyEdges, listExperienceMediaAssets,
  listMenus, listPages, listPreviewSessions, listPublicationJobs, listSymbolRevisions, listSymbols, listTemplates, publishSymbol, publishTemplate, restoreSymbolRevision, revokePreview, rollbackPage,
  saveBlockAsSymbol, saveBlocks, savePageAsTemplate, transitionPage, updatePage, updateSymbolFromBlock, updateTemplateFromPage,
} from './repository'
import type { CmsPageStatus } from './types'
import type { MarketplacePermission } from '../domain/types'

const pageStatuses = new Set<CmsPageStatus>(['draft','submitted','in_review','approved','scheduled','published','retired','archived'])
const transitionPermission: Record<CmsPageStatus, MarketplacePermission> = {
  draft: 'marketplace.cms.edit', submitted: 'marketplace.cms.submit', in_review: 'marketplace.cms.review', approved: 'marketplace.cms.approve', scheduled: 'marketplace.cms.schedule', published: 'marketplace.cms.publish', retired: 'marketplace.cms.publish', archived: 'marketplace.cms.archive',
}

export async function handlePages(request: Request) {
  const id = requestId(request)
  try {
    if (request.method === 'GET') { const context = await requireMarketplaceApiContext('marketplace.cms.view'); const url = new URL(request.url); return apiSuccess(await listPages({ status: url.searchParams.get('status') || undefined, locale: url.searchParams.get('locale') || undefined, territoryId: context.territoryId }), { requestId: id }) }
    const context = await requireMarketplaceApiContext('marketplace.cms.create'); const body = await parseJsonObject(request)
    return apiSuccess(await createPage({ routeKey: requireText(body.routeKey,'routeKey','Clé de route',120), locale: cleanText(body.locale || 'fr',5), territoryId: context.territoryId, title: requireText(body.title,'title','Titre',240), navigationLabel: cleanOptionalText(body.navigationLabel,120), slug: requireText(body.slug,'slug','Slug',180), description: cleanOptionalText(body.description,2000), sensitive: Boolean(body.sensitive), seoTitle: cleanOptionalText(body.seoTitle,240), seoDescription: cleanOptionalText(body.seoDescription,500), context, requestId: id }), { requestId: id, status: 201 })
  } catch (error) { return apiFailure(error,id) }
}

export async function handlePage(request: Request, pageId: string) {
  const id=requestId(request)
  try {
    if(request.method==='GET'){await requireMarketplaceApiContext('marketplace.cms.view');return apiSuccess(await getPageDetail(pageId),{requestId:id})}
    const context=await requireMarketplaceApiContext('marketplace.cms.edit');const body=await parseJsonObject(request)
    const allowed=['title','navigation_label','slug','description','seo_title','seo_description','canonical_url','translation_status','sensitive','scheduled_at'];const patch:Record<string,unknown>={};for(const key of allowed){if(key in body)patch[key]=body[key]}
    return apiSuccess(await updatePage({pageId,patch,expectedVersion:Number(body.expectedVersion)||undefined,changeSummary:requireText(body.changeSummary,'changeSummary','Résumé du changement',500),context,requestId:id}),{requestId:id})
  } catch(error){return apiFailure(error,id)}
}

export async function handleBlocks(request: Request,pageId:string){const id=requestId(request);try{const context=await requireMarketplaceApiContext('marketplace.cms.blocks.manage');const body=await parseJsonObject(request);if(!Array.isArray(body.blocks))throw new MarketplaceError('VALIDATION_ERROR','Une liste de blocs est requise.');const blocks=body.blocks.map((entry,index)=>{if(!entry||typeof entry!=='object'||Array.isArray(entry))throw new MarketplaceError('VALIDATION_ERROR',`Bloc ${index+1} invalide.`);const block=entry as Record<string,unknown>;return{id:cleanOptionalText(block.id,100)||undefined,blockKey:requireText(block.blockKey,'blockKey','Clé du bloc',120),blockType:requireText(block.blockType,'blockType','Type du bloc',60),sortOrder:Number(block.sortOrder??index),content:block.content&&typeof block.content==='object'&&!Array.isArray(block.content)?block.content as Record<string,unknown>:{},settings:block.settings&&typeof block.settings==='object'&&!Array.isArray(block.settings)?block.settings as Record<string,unknown>:{},status:cleanText(block.status||'active',20),audience:Array.isArray(block.audience)?block.audience.map(value=>cleanText(value,60)).filter(Boolean):[],parentBlockKey:cleanOptionalText(block.parentBlockKey,120)||null,slotKey:cleanText(block.slotKey||'default',60),schemaVersion:Number(block.schemaVersion||1)}});return apiSuccess(await saveBlocks({pageId,blocks,expectedVersion:Number(body.expectedVersion)||undefined,changeSummary:cleanOptionalText(body.changeSummary,500)||undefined,context,requestId:id}),{requestId:id})}catch(error){return apiFailure(error,id)}}

export async function handlePageTransition(request:Request,pageId:string){const id=requestId(request);try{const body=await parseJsonObject(request);const target=cleanText(body.target,30) as CmsPageStatus;if(!pageStatuses.has(target))throw new MarketplaceError('VALIDATION_ERROR','Statut cible invalide.');const context=await requireMarketplaceApiContext(transitionPermission[target]);return apiSuccess(await transitionPage({pageId,target,reason:requireText(body.reason,'reason','Motif',1000),context,requestId:id}),{requestId:id})}catch(error){return apiFailure(error,id)}}
export async function handlePageRollback(request:Request,pageId:string){const id=requestId(request);try{const context=await requireMarketplaceApiContext('marketplace.cms.rollback');const body=await parseJsonObject(request);return apiSuccess(await rollbackPage({pageId,versionNumber:Number(body.versionNumber),reason:requireText(body.reason,'reason','Motif',1000),context,requestId:id}),{requestId:id})}catch(error){return apiFailure(error,id)}}
export async function handleMenus(request:Request){const id=requestId(request);try{await requireMarketplaceApiContext('marketplace.cms.view');return apiSuccess(await listMenus(),{requestId:id})}catch(error){return apiFailure(error,id)}}
export async function handleCtas(request:Request){const id=requestId(request);try{await requireMarketplaceApiContext('marketplace.cms.view');return apiSuccess(await listCtas(),{requestId:id})}catch(error){return apiFailure(error,id)}}
export async function handlePublishing(request:Request){const id=requestId(request);try{await requireMarketplaceApiContext('marketplace.cms.view');return apiSuccess(await listPublicationJobs(),{requestId:id})}catch(error){return apiFailure(error,id)}}
export async function handlePreview(request:Request,pageId:string){const id=requestId(request);try{const context=await requireMarketplaceApiContext('marketplace.cms.preview');return apiSuccess(await createPreview({pageId,context,requestId:id}),{requestId:id,status:201})}catch(error){return apiFailure(error,id)}}
export async function handlePreviewSessions(request:Request){const id=requestId(request);try{const url=new URL(request.url);if(request.method==='GET'){await requireMarketplaceApiContext('marketplace.cms.preview');return apiSuccess(await listPreviewSessions(url.searchParams.get('pageId')||undefined),{requestId:id})}const context=await requireMarketplaceApiContext('marketplace.cms.preview');const body=await parseJsonObject(request);return apiSuccess(await revokePreview({previewId:requireText(body.previewId,'previewId','Preview',100),context,requestId:id}),{requestId:id})}catch(error){return apiFailure(error,id)}}
export async function handleExperienceMedia(request:Request){const id=requestId(request);try{await requireMarketplaceApiContext('marketplace.media.view');const url=new URL(request.url);return apiSuccess(await listExperienceMediaAssets(url.searchParams.get('q')||undefined),{requestId:id})}catch(error){return apiFailure(error,id)}}
export async function handleDependencies(request:Request){const id=requestId(request);try{await requireMarketplaceApiContext('marketplace.cms.view');const url=new URL(request.url);return apiSuccess(await listDependencyEdges({pageId:url.searchParams.get('pageId')||undefined,targetType:url.searchParams.get('targetType')||undefined,targetId:url.searchParams.get('targetId')||undefined}),{requestId:id})}catch(error){return apiFailure(error,id)}}
export async function handleTemplates(request:Request){const id=requestId(request);try{if(request.method==='GET'){await requireMarketplaceApiContext('marketplace.cms.view');return apiSuccess(await listTemplates(),{requestId:id})}const context=await requireMarketplaceApiContext('marketplace.cms.pages.manage');const body=await parseJsonObject(request);const action=cleanText(body.action||'create',40);if(action==='instantiate')return apiSuccess(await instantiateTemplate({templateId:requireText(body.templateId,'templateId','Template',100),routeKey:requireText(body.routeKey,'routeKey','Clé route',120),title:requireText(body.title,'title','Titre',240),slug:requireText(body.slug,'slug','Slug',180),locale:cleanText(body.locale||'fr',5),context,requestId:id}),{requestId:id,status:201});if(action==='update')return apiSuccess(await updateTemplateFromPage({templateId:requireText(body.templateId,'templateId','Template',100),pageId:requireText(body.pageId,'pageId','Page',100),changeSummary:requireText(body.changeSummary,'changeSummary','Résumé',500),publish:Boolean(body.publish),context,requestId:id}),{requestId:id});if(action==='publish')return apiSuccess(await publishTemplate({templateId:requireText(body.templateId,'templateId','Template',100),context,requestId:id}),{requestId:id});if(action==='archive')return apiSuccess(await archiveTemplate({templateId:requireText(body.templateId,'templateId','Template',100),context,requestId:id}),{requestId:id});return apiSuccess(await savePageAsTemplate({pageId:requireText(body.pageId,'pageId','Page',100),name:requireText(body.name,'name','Nom',180),category:cleanText(body.category||'page',80),description:cleanOptionalText(body.description,500)||undefined,context,requestId:id}),{requestId:id,status:201})}catch(error){return apiFailure(error,id)}}
export async function handleSymbols(request:Request){const id=requestId(request);try{const url=new URL(request.url);if(request.method==='GET'){await requireMarketplaceApiContext('marketplace.cms.view');const symbolId=url.searchParams.get('symbolId');return apiSuccess(symbolId?await listSymbolRevisions(symbolId):await listSymbols(),{requestId:id})}const context=await requireMarketplaceApiContext('marketplace.cms.blocks.manage');const body=await parseJsonObject(request);const action=cleanText(body.action||'create',40);if(action==='update')return apiSuccess(await updateSymbolFromBlock({symbolId:requireText(body.symbolId,'symbolId','Symbole',100),pageId:requireText(body.pageId,'pageId','Page',100),blockKey:requireText(body.blockKey,'blockKey','Bloc',120),changeSummary:requireText(body.changeSummary,'changeSummary','Résumé',500),publish:Boolean(body.publish),context,requestId:id}),{requestId:id});if(action==='publish')return apiSuccess(await publishSymbol({symbolId:requireText(body.symbolId,'symbolId','Symbole',100),context,requestId:id}),{requestId:id});if(action==='restore')return apiSuccess(await restoreSymbolRevision({symbolId:requireText(body.symbolId,'symbolId','Symbole',100),revisionNumber:Number(body.revisionNumber),publish:Boolean(body.publish),context,requestId:id}),{requestId:id});if(action==='archive')return apiSuccess(await archiveSymbol({symbolId:requireText(body.symbolId,'symbolId','Symbole',100),context,requestId:id}),{requestId:id});return apiSuccess(await saveBlockAsSymbol({pageId:requireText(body.pageId,'pageId','Page',100),blockKey:requireText(body.blockKey,'blockKey','Bloc',120),name:requireText(body.name,'name','Nom',180),description:cleanOptionalText(body.description,500)||undefined,context,requestId:id}),{requestId:id,status:201})}catch(error){return apiFailure(error,id)}}
