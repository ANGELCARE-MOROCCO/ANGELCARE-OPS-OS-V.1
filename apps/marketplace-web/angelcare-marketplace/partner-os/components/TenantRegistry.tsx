import type { PartnerTenant } from '../types';import s from '../partner.module.css'
export function TenantRegistry({tenants}:{tenants:PartnerTenant[]}){return <div className={s.shell}><section className={s.hero}><div className={s.eyebrow}>TENANT CONTROL</div><h1 className={s.title}>Registre des organisations clientes</h1><p className={s.lead}>Identité, type, territoire, statut, owner, onboarding et santé restent visibles et filtrables.</p></section><section className={s.panel}><div className={s.rows}>{tenants.map(t=><div className={s.row} key={t.id}><div><strong>{t.display_name}</strong><div className={s.subtle}>{t.legal_name} · {t.public_reference}</div></div><span>{t.tenant_type}</span><span>{t.onboarding_score}%</span><span className={s.status}>{t.status}</span><span>{t.health_status}</span></div>)}{!tenants.length&&<div className={s.empty}>Aucun tenant réel.</div>}</div></section></div>}
