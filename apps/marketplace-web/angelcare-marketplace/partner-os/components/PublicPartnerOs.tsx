import Link from 'next/link';import type { PartnerPlan } from '../types';import s from '../partner.module.css'
export function PublicPartnerOs({locale,plans}:{locale:'fr'|'en'|'ar';plans:PartnerPlan[]}){return <main className={s.shell} dir={locale==='ar'?'rtl':'ltr'}><section className={s.hero}><div className={s.eyebrow}>ANGELCARE PARTNER OS</div><h1 className={s.title}>{locale==='ar'?'نظام تشغيل للمؤسسات الشريكة':locale==='en'?'An operating system for partner organizations':'Le système d’exploitation des établissements partenaires'}</h1><p className={s.lead}>Modules, équipe, onboarding, abonnement et gouvernance dans un workspace sécurisé par tenant.</p><Link href={`/angelcare-marketplace/${locale}/partner-os/contact`} style={{display:'inline-flex',marginTop:18,padding:'12px 16px',borderRadius:12,background:'#fff',color:'#0b2c55',textDecoration:'none',fontWeight:850}}>Demander une démonstration</Link></section><section className={s.grid}>{plans.filter(p=>p.status==='published').map(p=><article className={s.card} key={p.id}><span className={s.status}>{p.billing_period}</span><h3>{p.name_fr}</h3><p className={s.subtle}>{p.description_fr}</p><div className={s.score}>{p.base_price==null?'Sur devis':`${p.base_price} ${p.currency_label}`}</div></article>)}{!plans.some(p=>p.status==='published')&&<div className={s.empty}>Les plans publics seront affichés après approbation.</div>}</section></main>}
