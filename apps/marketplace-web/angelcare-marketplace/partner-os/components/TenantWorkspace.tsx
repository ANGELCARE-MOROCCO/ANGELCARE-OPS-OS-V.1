import type { TenantWorkspaceData } from '../types';import s from '../partner.module.css'
export function TenantWorkspace({data}:{data:TenantWorkspaceData}){return <div className={s.shell}><section className={s.hero}><div className={s.eyebrow}>TENANT WORKSPACE</div><h1 className={s.title}>{data.tenant.display_name}</h1><p className={s.lead}>Espace isolé : abonnement, modules, membres, onboarding et usages du tenant courant uniquement.</p><div className={s.metrics}><div className={s.metric}><strong>{data.modules.filter(x=>x.status==='active').length}</strong><span>Modules actifs</span></div><div className={s.metric}><strong>{data.members}</strong><span>Membres</span></div><div className={s.metric}><strong>{data.tenant.onboarding_score}%</strong><span>Onboarding</span></div><div className={s.metric}><strong>{data.usageAlerts}</strong><span>Alertes usage</span></div></div></section><section className={s.grid}>{data.modules.map(m=><article className={s.card} key={m.id}><span className={s.status}>{m.status}</span><h3>{m.module_key}</h3><p className={s.subtle}>Configuration et limites gouvernées pour ce tenant.</p></article>)}{!data.modules.length&&<div className={s.empty}>Aucun module activé.</div>}</section><section className={s.panel}><h2>Runway onboarding</h2><div className={s.rows}>{data.onboarding.map(c=><div className={s.row} key={c.id}><strong>{c.title_fr}</strong><span>{c.required?'Obligatoire':'Recommandé'}</span><span>{c.owner_id?'Owner assigné':'Owner manquant'}</span><span className={s.status}>{c.status}</span><span>→</span></div>)}</div></section></div>}
