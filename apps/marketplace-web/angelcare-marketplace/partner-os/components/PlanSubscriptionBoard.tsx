import type { PartnerPlan,PartnerSubscription } from '../types';import s from '../partner.module.css'
export function PlanSubscriptionBoard({plans,subscriptions}:{plans:PartnerPlan[];subscriptions:PartnerSubscription[]}){return <div className={s.shell}><section className={s.hero}><div className={s.eyebrow}>PLANS & SUBSCRIPTIONS</div><h1 className={s.title}>Offre SaaS, modules et renouvellements</h1><p className={s.lead}>Chaque plan porte son prix, sa période, ses modules et son statut. Chaque abonnement conserve sa période et son mode de renouvellement.</p></section><section className={s.grid}>{plans.map(p=><article className={s.card} key={p.id}><span className={s.status}>{p.status}</span><h3>{p.name_fr}</h3><p className={s.subtle}>{p.description_fr}</p><div className={s.score}>{p.base_price==null?'Sur devis':`${p.base_price} ${p.currency_label}`}</div><p className={s.subtle}>{p.billing_period} · {p.module_count||0} modules</p></article>)}{!plans.length&&<div className={s.empty}>Aucun plan réel.</div>}</section><section className={s.panel}><h2>Abonnements</h2><div className={s.rows}>{subscriptions.map(x=><div className={s.row} key={x.id}><strong>{x.public_reference}</strong><span>{x.status}</span><span>{x.amount==null?'—':`${x.amount} ${x.currency_label}`}</span><span className={s.status}>{x.renewal_mode}</span><span>{x.current_period_ends_at||'—'}</span></div>)}{!subscriptions.length&&<div className={s.empty}>Aucun abonnement réel.</div>}</div></section></div>}
