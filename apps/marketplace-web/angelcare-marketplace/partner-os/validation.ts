import { MarketplaceError } from '../server/errors';import type { MarketplaceRequestContext } from '../domain/types';import type { TenantStatus } from './types'
const transitions:Record<TenantStatus,TenantStatus[]>={draft:['onboarding','archived'],onboarding:['trial','active','suspended','archived'],trial:['active','suspended','cancelled'],active:['suspended','cancelled'],suspended:['onboarding','trial','active','cancelled','archived'],cancelled:['archived'],archived:[]}
export function assertTenantTransition(current:TenantStatus,target:TenantStatus){if(!transitions[current].includes(target))throw new MarketplaceError('INVALID_STATE_TRANSITION',`Transition tenant ${current} → ${target} interdite.`)}
export function assertTenantScope(context:MarketplaceRequestContext,tenantId:string){if(context.roleKeys.includes('marketplace_admin')||context.actor.sourceRole==='ceo'||context.permissions.includes('marketplace.partner_os.admin.view'))return;if(!context.tenantId||context.tenantId!==tenantId)throw new MarketplaceError('SCOPE_MISMATCH','Ce tenant ne fait pas partie de votre périmètre.')}
export function assertActivationReady(input:{subscriptionStatus?:string;onboardingBlocked:number}){if(!['trial','active'].includes(input.subscriptionStatus||'')||input.onboardingBlocked>0)throw new MarketplaceError('DEPENDENCY_BLOCKED','L’activation exige un abonnement actif et un onboarding sans blocage obligatoire.')}
