import styles from '../operations.module.css';import type { OperationsMission } from '../types'
const lanes=[['staffing_required','À staffer'],['proposed','Propositions'],['awaiting_acceptance','Acceptation'],['accepted','Brief'],['in_progress','En cours']]
export function DispatchBoard({missions}:{missions:OperationsMission[]}){return <div className={styles.shell}><section className={styles.hero}><div><div className={styles.eyebrow}>DISPATCH CONTROL</div><h1 className={styles.title}>Affecter avec une preuve d’éligibilité explicable.</h1><p className={styles.copy}>Disponibilité, certifications, zones, charge, trajet et restrictions sont visibles avant toute décision.</p></div></section><section className={styles.dispatch}>{lanes.map(([status,label])=><div className={styles.lane} key={status}><div className={styles.laneHead}><strong>{label}</strong><span>{missions.filter(m=>m.status===status).length}</span></div>{missions.filter(m=>m.status===status).map(m=><article className={styles.missionCard} key={m.id}><h3>{m.title}</h3><div className={styles.missionMeta}><span>{m.service_type}</span><span>{m.operational_zone||'Zone à confirmer'}</span><span>{m.risk_level}</span></div><div className={styles.score}><small>Readiness</small><div className={styles.scoreBar}><span style={{width:m.assigned_provider_id?'88%':'42%'}}/></div></div></article>)}</div>)}</section></div>}
