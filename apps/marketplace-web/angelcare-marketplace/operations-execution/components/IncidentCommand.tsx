import styles from '../operations.module.css';import type { OperationsIncident } from '../types';export function IncidentCommand({incidents}:{incidents:OperationsIncident[]}){return <div className={styles.shell}><section className={styles.hero}><div><div className={styles.eyebrow}>INCIDENT & ESCALATION COMMAND</div><h1 className={styles.title}>Trier, répondre, corriger et valider.</h1><p className={styles.copy}>Les récits protégés restent séparés des rapports visibles aux familles et partenaires.</p></div></section><section className={styles.panel}><div className={styles.panelBody}>{incidents.map(i=><div className={styles.incident} key={i.id}><strong>{i.public_reference} · {i.severity}</strong><p>{i.category} · {i.status} · échéance {i.response_due_at||'à définir'}</p></div>)}</div></section></div>}