import styles from '../operations.module.css';import type { OperationsSummary } from '../types';export function DailyClosureBoard({summary}:{summary:OperationsSummary}){const blockers=[['Missions non affectées',summary.unassigned],['Acceptations manquantes',summary.awaitingAcceptance],['Check-in manquants',summary.checkInMissing],['Rapports en attente',summary.reportsPending],['Incidents ouverts',summary.incidentsOpen],['Validation en attente',summary.validationPending]];return <div className={styles.shell}><section className={styles.hero}><div><div className={styles.eyebrow}>DAILY CLOSURE</div><h1 className={styles.title}>La journée ne se ferme pas sur une obligation critique.</h1><p className={styles.copy}>Le supervisor sign-off demeure bloqué tant que les preuves et escalades nécessaires ne sont pas résolues.</p></div></section><section className={styles.closure}><div className={styles.blockers}>{blockers.map(([l,v])=><div className={styles.blocker} key={String(l)}><span>{l}</span><strong>{Number(v)}</strong></div>)}</div></section></div>}