# Rejection Check

Reject if certificates are decorative, attendance is fabricated, provider eligibility is manual-only, documents lack expiry, ineligible providers can be assigned, dispatch is static, check-in is inferred from page activity, reports overwrite history, incidents are generic notes, payable is toggled without proof, family data leaks, or the original MZ16–18 domains are replaced by generic placeholders.
