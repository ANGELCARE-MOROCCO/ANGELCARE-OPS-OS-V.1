# Rejection Check

Reject if scanner is regex-only, database/backend/runtime sources are absent, French is not canonical, stale detection fails, CSV lacks hash validation, placeholders can be corrupted, source is rewritten blindly, sensitive fallback occurs, RTL is cosmetic or coverage is fabricated.
