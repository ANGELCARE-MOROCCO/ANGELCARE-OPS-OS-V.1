# Acceptance Matrix

Contractual static verifier covers required source, routes, APIs, migration, rollback, permissions, module registration, RLS, visual primitives, RTL, responsive behavior and suppression scans. Installer additionally runs strict targeted TypeScript. Runtime acceptance requires live database and canonical-system tests.
