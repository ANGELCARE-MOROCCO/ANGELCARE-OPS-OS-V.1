# API Register

Customer account and journey reads; action completion; change request creation; recovery creation; notification acknowledgement; protected administrative list, summary, detail and transition endpoints. Every mutation is server-authorized and audit-linked.
