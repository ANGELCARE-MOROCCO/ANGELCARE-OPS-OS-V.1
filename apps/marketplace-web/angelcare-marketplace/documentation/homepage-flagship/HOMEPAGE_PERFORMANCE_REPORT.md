# Performance Contract

Hero assets are vector and responsive. Only the active hero receives high fetch priority; below-fold images are lazy loaded. Public data is resolved server-side. Interactive state is isolated to the storefront client component. No third-party carousel or heavy marketplace dependency is introduced.
