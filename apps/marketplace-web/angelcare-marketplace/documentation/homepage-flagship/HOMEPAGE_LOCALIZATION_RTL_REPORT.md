# Localization and RTL

French is canonical. Initial campaign, category, sellable-object and Partner OS plan content is supplied in FR/EN/AR. The root receives `lang` and `dir`; the storefront and global marketplace shell contain explicit RTL layout corrections. Missing business translations still fall back to French only within server mapping; sensitive Trust claims are never synthesized.
