# Migration Register

The additive migration extends localized catalogue fields and Partner OS plan translations; creates category, media, homepage orchestration, interaction and selection tables; seeds database-managed commercial content and responsive assets; enables RLS and indexes. It contains no DROP, TRUNCATE or destructive column operation.
