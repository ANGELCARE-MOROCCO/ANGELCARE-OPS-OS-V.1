# Scanner Source Registry

Required adapters: source AST, backend/API, registered database fields, controlled runtime render, communication templates, SEO metadata, content files. Export headers are optional but monitored. Database scans are allowlist-only and deny secret/private fields.
