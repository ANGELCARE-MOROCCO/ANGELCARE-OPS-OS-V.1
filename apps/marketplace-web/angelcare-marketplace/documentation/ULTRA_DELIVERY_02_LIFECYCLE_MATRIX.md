# Lifecycle Matrix

Development and catalog objects: `draft → review → approved → published`, with controlled suspension and archive. Opportunities: `new → discovery → qualified → solution → proposal → negotiation → verbal_commitment → won`, with explicit hold and loss branches. Tenants: `draft → onboarding → trial/active`, with suspension, cancellation and archive. Server validation rejects invalid transitions.
