# QA Evidence

Static verifier covers required files, AST compiler use, SQL structures, RLS/revokes, French source constraint, stale propagation, CSV UTF-8 and formula defense, placeholder parity and truthful coverage. Target TypeScript and runtime scans run after installation.
