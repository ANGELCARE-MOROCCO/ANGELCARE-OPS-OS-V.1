import Link from 'next/link'
import styles from '../academy.module.css'
import type { AcademyProgram } from '../types'
import type {PublishedSurfaceExperience} from '../../total-commerce-control/types'
import {PublicSurfaceSections} from '../../total-commerce-control/components/PublicSurfaceSections'
const val=(v:unknown,f='')=>typeof v==='string'&&v?v:f
export function PublicAcademyExperience({programs,locale,experience}:{programs:AcademyProgram[];locale:'fr'|'en'|'ar';experience?:PublishedSurfaceExperience|null}){const c=experience?.copy||{};return <main className={`${styles.shell} ${locale==='ar'?styles.rtl:''}`}><section className={styles.publicHero}><div className={styles.eyebrow}>{val(c.eyebrow,'ANGELCARE ACADEMY')}</div><h1 className={styles.title}>{val(c.title,'Des compétences vérifiées pour des services qui inspirent confiance.')}</h1><p className={styles.copy}>{val(c.lead,'Programmes professionnels, parcours B2B, évaluations et certifications gouvernées par des preuves — jamais par une simple attestation décorative.')}</p><div className={styles.publicGrid}>{programs.slice(0,6).map(p=><article className={styles.publicCard} key={p.id}><h2>{p.title_fr}</h2><p>{p.description_fr||'Programme de compétence ANGELCARE.'}</p><span>{p.status}</span></article>)}</div><Link href={val(c.primary_cta_href,`/angelcare-marketplace/${locale}/academy/request`)}>{val(c.primary_cta_label,'Demander un parcours Academy')} →</Link></section><PublicSurfaceSections experience={experience||null}/></main>}
