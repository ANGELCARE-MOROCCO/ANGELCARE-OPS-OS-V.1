import type { QuoteBasket } from '../types';import s from '../marketplace.module.css'
export function QuoteBasketBoard({baskets}:{baskets:QuoteBasket[]}){return <div className={s.shell}><section className={s.hero}><div className={s.eyebrow}>QUOTE BASKETS</div><h1 className={s.title}>Paniers de devis et configurations</h1><p className={s.lead}>Les offres sélectionnées, quantités, configurations et montants restent persistants jusqu’à la qualification commerciale.</p></section><section className={s.panel}><div className={s.table}>{baskets.map(b=><div className={s.row} key={b.id}><div><strong>{b.public_reference}</strong><div className={s.subtle}>{b.items?.length||0} lignes</div></div><span>{b.basket_status}</span><span>{b.grand_total} {b.currency_label}</span><span className={s.status}>{b.tenant_id?'Tenant':'Famille / public'}</span><span>{b.expires_at?'Expire':'Ouvert'}</span></div>)}{!baskets.length&&<div className={s.empty}>Aucun panier de devis réel.</div>}</div></section></div>}
