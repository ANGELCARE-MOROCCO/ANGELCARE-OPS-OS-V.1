import type { CatalogItem } from '../types';import s from '../marketplace.module.css'
export function CatalogRegistry({items}:{items:CatalogItem[]}){return <div className={s.shell}><section className={s.hero}><div className={s.eyebrow}>UNIFIED CATALOG</div><h1 className={s.title}>Catalogue commercial unifié</h1><p className={s.lead}>Chaque ligne expose sa nature, son prix, son territoire, sa disponibilité, son cycle de publication et son identité publique.</p></section><section className={s.panel}><div className={s.table}>{items.map(i=><div className={s.row} key={i.id}><div><strong>{i.name_fr}</strong><div className={s.subtle}>{i.public_reference}</div></div><span>{i.kind}</span><span>{i.price_mode==='quote_only'?'Sur devis':i.price_amount==null?'—':`${i.price_amount} ${i.currency_label}`}</span><span className={s.status}>{i.status}</span><span>{i.availability_status}</span></div>)}{!items.length&&<div className={s.empty}>Aucune offre réelle enregistrée.</div>}</div></section></div>}
