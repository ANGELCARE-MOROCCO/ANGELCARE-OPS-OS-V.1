import Link from 'next/link';import type { CatalogItem } from '../types';import s from '../marketplace.module.css'
export function PublicMarketplace({locale,items}:{locale:'fr'|'en'|'ar';items:CatalogItem[]}){return <main className={s.shell} dir={locale==='ar'?'rtl':'ltr'}><section className={s.hero}><div className={s.eyebrow}>ANGELCARE MARKETPLACE</div><h1 className={s.title}>{locale==='ar'?'حلول أنجل كير في سوق واحد':locale==='en'?'AngelCare solutions in one marketplace':'Toutes les solutions AngelCare dans un seul Marketplace'}</h1><p className={s.lead}>Services, produits, formations, audits, kits et modules SaaS disponibles selon votre territoire.</p></section><section className={s.grid}>{items.map(i=><article className={s.card} key={i.id}><span className={s.kind}>{i.kind}</span><h3>{i.name_fr}</h3><p className={s.subtle}>{i.short_description_fr}</p><div className={s.price}>{i.price_mode==='quote_only'?'Sur devis':i.price_amount==null?'Demander disponibilité':`${i.price_amount} ${i.currency_label}`}</div><Link className={s.button} href={`/angelcare-marketplace/${locale}/marketplace/${i.slug}`}>Voir l’offre</Link></article>)}{!items.length&&<div className={s.empty}>Aucune offre publiée n’est disponible dans ce territoire.</div>}</section></main>}
