import { listServiceBlueprints } from './blueprint-engine'
export type AIRecommendation={id:string; title:string; impact:'low'|'medium'|'high'|'massive'; confidence:number; action:string; serviceCodes:string[]; owner:string; expectedOutcome:string}
export function getAIServiceRecommendations():AIRecommendation[]{ const b=listServiceBlueprints(); return [
 {id:'AI-001',title:'Launch Autism Hybrid Premium Pack in Rabat/Casablanca',impact:'massive',confidence:91,action:'bundle school_support + therapy_coordination + certified staff + parent reporting',serviceCodes:['#S.H','#S.K'],owner:'CEO + Special Care Lead',expectedOutcome:'high differentiation and premium margin'},
 {id:'AI-002',title:'Create Postpartum 30-Day Elite Subscription',impact:'high',confidence:86,action:'combine newborn_support + mother_support + nurse_supervision + overnight option',serviceCodes:['#P.P'],owner:'Postpartum BU Lead',expectedOutcome:'recurring high-ticket revenue'},
 {id:'AI-003',title:'Scale Montessori Parascolaire B2B for Schools',impact:'high',confidence:82,action:'institution_contract + montessori_activity + academy_certification',serviceCodes:['#S.L','#A.A'],owner:'Academy Director',expectedOutcome:'institutional contracts and repeat demand'},
 {id:'AI-004',title:'Open Marrakech tourism family pilot',impact:'medium',confidence:74,action:'pilot bilingual home/event childcare with hotels and concierge partners',serviceCodes:['#H.S','#A.B'],owner:'Expansion Manager',expectedOutcome:'tourism-driven premium acquisition'},
 ]}
export function getDemandSignals(){return [{signal:'postpartum demand',trend:'+ strong premium potential',cities:['Rabat','Casablanca'],action:'package 30/60 days'},{signal:'special needs support',trend:'+ underserved and high willingness to pay',cities:['Rabat','Casablanca'],action:'certification pipeline'},{signal:'parascolaire ludique',trend:'+ school partnership opportunity',cities:['Casablanca','Rabat'],action:'B2B curriculum'}]}
export function getExecutiveAICommand(){ const recs=getAIServiceRecommendations(); return {massive:recs.filter(r=>r.impact==='massive').length,confidence:Math.round(recs.reduce((s,r)=>s+r.confidence,0)/recs.length),nextBestAction:recs[0],blueprints:listServiceBlueprints().length} }
