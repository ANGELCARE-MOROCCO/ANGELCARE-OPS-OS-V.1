export async function loadProductionState(key: string) { const res = await fetch(`/api/market-os/production-state?table=market_os_content_runtime_state&key=${encodeURIComponent(key)}`, { cache:'no-store' }); const json = await res.json(); return json?.data?.[0]?.payload ?? null; }
export async function saveProductionState(key: string, payload: unknown, stateType = 'workspace') { const res = await fetch('/api/market-os/production-state', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ table:'market_os_content_runtime_state', action:'save_runtime_state', row:{ state_key:key, state_type:stateType, payload } }) }); return res.json(); }
export async function saveSeoRow(table: 'market_os_seo_articles'|'market_os_seo_keywords'|'market_os_seo_topic_clusters'|'market_os_seo_internal_links', row: Record<string, any>) { const res = await fetch('/api/market-os/production-state', { method:'POST', headers:{'Content-Type':'application/json'}, body: JSON.stringify({ table, action:'seo_upsert', row }) }); return res.json(); }
