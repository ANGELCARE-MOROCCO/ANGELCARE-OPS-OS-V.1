import { apiError, insertAudit, isFounder, requireCapitalApiActor, requiredString, success, updateRow } from "@/lib/ac-capital-os/server/mz15-api";
export const dynamic="force-dynamic";
const allowedTables=new Set(["ac_capital_coordinator_founder_approvals","ac_capital_case_founder_approvals","ac_capital_ai_human_approval_queue","ac_capital_data_room_approval_events"]);
export async function POST(request:Request,{params}:{params:Promise<{id:string}>}){try{const actor=await requireCapitalApiActor();if(!isFounder(actor))throw Object.assign(new Error("FOUNDER_APPROVAL_REQUIRED"),{status:403});const {id}=await params;const body=await request.json() as Record<string,unknown>;const sourceTable=requiredString(body.sourceTable,"Source table");if(!allowedTables.has(sourceTable))throw Object.assign(new Error("UNSUPPORTED_APPROVAL_SOURCE"),{status:400});const decision=requiredString(body.decision,"Decision");const status=decision==="approve"?"Approved":decision==="reject"?"Rejected":"Revision Requested";const payload:Record<string,unknown>={status};if(sourceTable==="ac_capital_coordinator_founder_approvals"){payload.approving_founder=actor.name;payload.comments=Array.isArray(body.comments)?body.comments:[String(body.note||"")];payload.approval_history=[{decision,status,note:body.note||null,actor:actor.email||actor.name,at:new Date().toISOString()}]}else if(sourceTable==="ac_capital_case_founder_approvals"){payload.approver=actor.name;payload.comments=body.note||null;if(status==="Approved")payload.approved_at=new Date().toISOString()}else if(sourceTable==="ac_capital_data_room_approval_events"){payload.decided_by=actor.email||actor.name;payload.decided_at=new Date().toISOString();payload.comment=body.note||null}const record=await updateRow(sourceTable,id,payload);await insertAudit({actor:actor.email||actor.name,action:`approval_${decision}`,objectType:"approval_request",objectId:id,after:record,reason:String(body.note||decision),approval:"Founder decision"});return Response.json(success({record}));}catch(reason){return apiError(reason)}}
