import { apiError, insertRow, requireCapitalApiActor, requiredString, success } from "@/lib/ac-capital-os/server/mz15-api";
export const dynamic="force-dynamic";
export async function POST(request:Request){try{const actor=await requireCapitalApiActor();const body=await request.json() as Record<string,unknown>;const record=await insertRow("ac_capital_ai_troubleshooting_issues",{issue_title:requiredString(body.issueTitle,"Issue title"),category:body.category||"AI troubleshooting",severity:body.severity||"Medium",affected_agent_id:body.agentId||null,affected_workspace:body.workspace||"AI Command Center",linked_output_id:body.outputId||null,linked_case_id:body.caseId||null,linked_funder_id:body.funderId||null,linked_opportunity_id:body.opportunityId||null,reported_by:actor.email||actor.name,reproduction_note:body.reproductionNote||null,impact:body.impact||null,recommended_fix:body.recommendedFix||"Review source, prompt, doctrine and skill binding.",status:"New",owner:body.owner||"AI System Admin"});return Response.json(success({record}));}catch(reason){return apiError(reason)}}
