import { apiError, envelope, insertAudit, insertRow, isFounder, isWriter, readTable, requiredString, requireCapitalApiActor, success, updateRow } from "@/lib/ac-capital-os/server/mz15-api";

export const dynamic = "force-dynamic";

export async function GET(){try{await requireCapitalApiActor();const [records,stageEvents,followups,tasks,communications,submissions,dueDiligenceRequests,negotiations,outcomes,learningItems,calendarEvents,auditEvents]=await Promise.all([readTable("ac_capital_pipeline_records",250),readTable("ac_capital_pipeline_stage_events",500,"changed_at"),readTable("ac_capital_pipeline_followups",400),readTable("ac_capital_pipeline_tasks",400),readTable("ac_capital_pipeline_communications",500),readTable("ac_capital_pipeline_submissions",300),readTable("ac_capital_pipeline_due_diligence_requests",300),readTable("ac_capital_pipeline_negotiations",300),readTable("ac_capital_pipeline_outcomes",300),readTable("ac_capital_pipeline_learning_items",300),readTable("ac_capital_pipeline_calendar_events",300),readTable("ac_capital_pipeline_audit_events",500)]);return Response.json(envelope({records,stageEvents,followups,tasks,communications,submissions,dueDiligenceRequests,negotiations,outcomes,learningItems,calendarEvents,auditEvents}));}catch(reason){return apiError(reason)}}
export async function POST(request:Request){try{const actor=await requireCapitalApiActor();if(!isWriter(actor))throw Object.assign(new Error("WRITE_PERMISSION_REQUIRED"),{status:403});const body=await request.json() as Record<string,unknown>;const action=String(body.action||"create-deal");if(action==="create-deal"){const record=await insertRow("ac_capital_pipeline_records",{opportunity_id:body.opportunityId||null,qualification_dossier_id:body.qualificationDossierId||null,funder_id:body.funderId||null,case_id:body.caseId||null,data_room_package_id:body.dataRoomPackageId||null,title:requiredString(body.title,"Deal title"),stage:body.stage||"Detected",status:"Active",funding_type:body.fundingType||null,package_type:body.packageType||null,estimated_amount_min:body.amountMin||null,estimated_amount_max:body.amountMax||null,currency_label:body.currencyLabel||"Dh",weighted_value:body.weightedValue||null,probability_percent:Number(body.probabilityPercent||0),deadline:body.deadline||null,next_action:body.nextAction||"Complete transaction setup",next_action_due_date:body.nextActionDueDate||null,owner:body.owner||actor.name,priority:body.priority||"medium",relationship_temperature:body.relationshipTemperature||"Cold",risk_level:body.riskLevel||"Medium",readiness_score:Number(body.readinessScore||0),founder_approval_status:"not_started",data_room_readiness_score:Number(body.dataRoomReadinessScore||0)});return Response.json(success({record}));}
const pipelineId=requiredString(body.pipelineRecordId,"Pipeline record id");if(action==="move-stage"){const nextStage=requiredString(body.newStage,"New stage");if(nextStage==="Submitted"&&!String(body.evidenceReference||body.proofReference||"").trim())throw Object.assign(new Error("SUBMISSION_PROOF_REQUIRED"),{status:400});if(["Approved","Negotiation","Submitted"].includes(nextStage)&&Boolean(body.founderApprovalRequired)&&!isFounder(actor))throw Object.assign(new Error("FOUNDER_APPROVAL_REQUIRED"),{status:403});const event=await insertRow("ac_capital_pipeline_stage_events",{pipeline_record_id:pipelineId,previous_stage:body.previousStage||null,new_stage:nextStage,changed_by:actor.email||actor.name,reason:body.reason||null,evidence_reference:body.evidenceReference||body.proofReference||null,comments:body.comments||null});const record=await updateRow("ac_capital_pipeline_records",pipelineId,{stage:nextStage,last_activity_at:new Date().toISOString(),next_action:body.nextAction||null});return Response.json(success({event,record}));}
if(action==="followup"){const record=await insertRow("ac_capital_pipeline_followups",{pipeline_record_id:pipelineId,followup_type:body.followupType||"Follow-up",channel:body.channel||"Email",recipient_name:body.recipientName||null,recipient_role:body.recipientRole||null,due_date:body.dueDate||null,priority:body.priority||"medium",status:"Planned",script_available:Boolean(body.scriptAvailable),documents_needed:Array.isArray(body.documentsNeeded)?body.documentsNeeded:[],risk_if_missed:body.riskIfMissed||null,owner:body.owner||actor.name});return Response.json(success({record}));}
if(action==="communication"){const record=await insertRow("ac_capital_pipeline_communications",{pipeline_record_id:pipelineId,channel:requiredString(body.channel,"Channel"),contact_person:body.contactPerson||null,subject:body.subject||null,summary:body.summary||null,outcome:body.outcome||null,next_action:body.nextAction||null,proof_reference:body.proofReference||null,occurred_at:body.occurredAt||new Date().toISOString(),logged_by:actor.email||actor.name});return Response.json(success({record}));}
if(action==="submission"){const proof=requiredString(body.proofReference,"Submission proof");const record=await insertRow("ac_capital_pipeline_submissions",{pipeline_record_id:pipelineId,case_id:body.caseId||null,package_id:body.packageId||null,submitted_by:actor.email||actor.name,submitted_at:body.submittedAt||new Date().toISOString(),recipient:requiredString(body.recipient,"Recipient"),method:body.method||"Manual",documents_included:Array.isArray(body.documentsIncluded)?body.documentsIncluded:[],version_submitted:body.versionSubmitted||null,proof_of_submission_reference:proof,confirmation_received:Boolean(body.confirmationReceived),followup_date:body.followupDate||null,result_status:"Submitted"});await updateRow("ac_capital_pipeline_records",pipelineId,{stage:"Submitted",last_activity_at:new Date().toISOString(),next_action:"Confirm receipt and schedule follow-up"});return Response.json(success({record}));}
if(action==="negotiation"){const record=await insertRow("ac_capital_pipeline_negotiations",{pipeline_record_id:pipelineId,amount_discussed:body.amountDiscussed||null,instrument_type:body.instrumentType||null,terms_requested:body.termsRequested||null,repayment_terms:body.repaymentTerms||null,equity_dilution_note:body.equityDilutionNote||null,guarantee_requirement:body.guaranteeRequirement||null,documents_required:Array.isArray(body.documentsRequired)?body.documentsRequired:[],founder_review_required:Boolean(body.founderReviewRequired),risk_note:body.riskNote||null,negotiation_status:body.negotiationStatus||"In Discussion",next_meeting:body.nextMeeting||null,decision_owner:body.decisionOwner||actor.name});return Response.json(success({record}));}
if(action==="outcome"){const record=await insertRow("ac_capital_pipeline_outcomes",{pipeline_record_id:pipelineId,outcome:requiredString(body.outcome,"Outcome"),reason:body.reason||null,funder_feedback:body.funderFeedback||null,missing_proof_identified:body.missingProof||null,objection_learned:body.objectionLearned||null,document_weakness:body.documentWeakness||null,next_improvement:body.nextImprovement||null,doctrine_update_needed:Boolean(body.doctrineUpdateNeeded),data_room_update_needed:Boolean(body.dataRoomUpdateNeeded),qualification_score_adjustment_needed:Boolean(body.qualificationUpdateNeeded),future_relationship_action:body.futureRelationshipAction||null,status:"Recorded"});const learning=await insertRow("ac_capital_pipeline_learning_items",{pipeline_record_id:pipelineId,outcome:body.outcome||null,reason:body.reason||null,objection_learned:body.objectionLearned||null,missing_proof:body.missingProof||null,doctrine_update_needed:Boolean(body.doctrineUpdateNeeded),data_room_update_needed:Boolean(body.dataRoomUpdateNeeded),qualification_update_needed:Boolean(body.qualificationUpdateNeeded),next_recommendation:body.nextImprovement||null,status:"Pending Doctrine Review"});return Response.json(success({record,learning}));}
throw Object.assign(new Error("UNSUPPORTED_PIPELINE_ACTION"),{status:400});}catch(reason){return apiError(reason)}}
