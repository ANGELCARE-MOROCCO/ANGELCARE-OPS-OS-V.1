# Rollback MZ13

Application rollback restores the exact timestamped backup created by the installer. Database rollback uses `apps/ops-web/docs/revenue-command-os/phase-13/ROLLBACK.sql` and returns the installation registry to MZ12. Rollback preserves previous MZ01–MZ12 data.
