.workspace {
  display: grid;
  gap: 22px;
  padding: 6px 0 36px;
  color: #0f172a;
}
.hero {
  display: grid;
  grid-template-columns: minmax(0, 1fr) 360px;
  gap: 22px;
  padding: 30px;
  border: 1px solid #e5e7eb;
  border-radius: 34px;
  background: linear-gradient(135deg, #ffffff 0%, #f8fbff 74%, #eff6ff 100%);
  box-shadow: 0 26px 70px rgba(15, 23, 42, 0.08);
}
.eyebrow {
  display: inline-flex;
  width: fit-content;
  padding: 8px 12px;
  border-radius: 999px;
  background: #fff7ed;
  color: #9a3412;
  font-size: 11px;
  font-weight: 950;
  letter-spacing: .12em;
  text-transform: uppercase;
}
.hero h1 {
  margin: 14px 0 12px;
  max-width: 920px;
  font-size: clamp(34px, 5vw, 72px);
  line-height: .9;
  letter-spacing: -.065em;
  font-weight: 980;
}
.hero p {
  margin: 0;
  max-width: 820px;
  color: #64748b;
  font-size: 16px;
  line-height: 1.65;
}
.actions {
  display: flex;
  flex-wrap: wrap;
  gap: 10px;
  margin-top: 22px;
}
.primaryButton,
.secondaryButton,
.dangerButton {
  min-height: 46px;
  padding: 0 17px;
  border-radius: 15px;
  font-size: 13px;
  font-weight: 900;
  cursor: pointer;
}
.primaryButton {
  border: 0;
  color: #fff;
  background: #0f172a;
  box-shadow: 0 16px 30px rgba(15,23,42,.24);
}
.secondaryButton {
  border: 1px solid #e5e7eb;
  color: #0f172a;
  background: #fff;
}
.dangerButton {
  border: 1px solid #fed7aa;
  color: #9a3412;
  background: #fff7ed;
}
.sideCard,
.panel,
.card,
.modal {
  border: 1px solid #e5e7eb;
  background: rgba(255,255,255,.96);
  box-shadow: 0 22px 56px rgba(15,23,42,.07);
}
.sideCard {
  border-radius: 28px;
  padding: 22px;
}
.sideCard strong {
  display:block;
  font-size: 24px;
  margin-bottom: 10px;
}
.sideCard span {
  color:#64748b;
  line-height: 1.55;
}
.kpis {
  display:grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 14px;
}
.kpi {
  padding: 18px;
  border-radius: 24px;
  background: #fff;
  border: 1px solid #e5e7eb;
}
.kpi small {
  display:block;
  color:#64748b;
  font-weight:900;
  letter-spacing:.05em;
  text-transform:uppercase;
}
.kpi strong {
  display:block;
  margin-top: 12px;
  font-size: 30px;
  letter-spacing:-.04em;
}
.grid {
  display:grid;
  grid-template-columns: minmax(0, 1.2fr) minmax(360px, .8fr);
  gap: 20px;
}
.panel {
  border-radius: 28px;
  padding: 22px;
}
.panelHeader {
  display:flex;
  align-items:flex-start;
  justify-content:space-between;
  gap:16px;
  margin-bottom: 18px;
}
.panelHeader h2 {
  margin:0;
  font-size: 24px;
  letter-spacing:-.035em;
}
.panelHeader p {
  margin:6px 0 0;
  color:#64748b;
  line-height:1.55;
}
.tableWrap {
  overflow:auto;
  border:1px solid #e5e7eb;
  border-radius:20px;
}
.tableWrap table {
  width:100%;
  border-collapse:collapse;
  min-width: 760px;
}
.tableWrap th {
  padding:13px 14px;
  text-align:left;
  color:#64748b;
  background:#f8fafc;
  font-size:11px;
  letter-spacing:.08em;
  text-transform:uppercase;
}
.tableWrap td {
  padding:14px;
  border-top:1px solid #eef2f7;
  vertical-align:top;
  font-size:13px;
}
.tableWrap td strong {
  display:block;
  margin-bottom:4px;
}
.tableWrap td small {
  color:#64748b;
}
.badge {
  display:inline-flex;
  padding:7px 10px;
  border-radius:999px;
  background:#eff6ff;
  color:#1d4ed8;
  font-weight:900;
  font-size:11px;
}
.formGrid {
  display:grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 14px;
}
.field {
  display:grid;
  gap:7px;
}
.field label {
  font-size:12px;
  font-weight:900;
  color:#334155;
}
.field input,
.field select,
.field textarea {
  width:100%;
  border:1px solid #dbe3ef;
  border-radius:14px;
  padding:12px 13px;
  color:#0f172a;
  background:#fff;
  font:inherit;
}
.field textarea {
  min-height:110px;
  resize:vertical;
}
.full { grid-column:1 / -1; }
.cards {
  display:grid;
  gap:12px;
}
.card {
  border-radius:22px;
  padding:16px;
}
.card h3 {
  margin:0 0 8px;
  letter-spacing:-.025em;
}
.card p { margin:0; color:#64748b; line-height:1.55; }
.modalBackdrop {
  position:fixed;
  inset:0;
  z-index:60;
  display:grid;
  place-items:center;
  padding:30px;
  background:rgba(15,23,42,.48);
  backdrop-filter: blur(7px);
}
.modal {
  width:min(1180px, 96vw);
  max-height: 90vh;
  overflow:auto;
  border-radius:32px;
}
.modalHeader,
.modalFooter {
  padding:22px 26px;
  display:flex;
  justify-content:space-between;
  gap:18px;
  align-items:center;
  border-bottom:1px solid #e5e7eb;
}
.modalHeader h2 { margin:6px 0 0; font-size:28px; letter-spacing:-.04em; }
.modalHeader button {
  width:44px; height:44px; border-radius:14px; border:1px solid #e5e7eb; background:#fff; cursor:pointer; font-size:22px;
}
.modalBody { padding:26px; display:grid; gap:20px; }
.modalFooter { border-top:1px solid #e5e7eb; border-bottom:0; justify-content:flex-end; }
.error {
  padding:14px 16px;
  border:1px solid #fecaca;
  background:#fff1f2;
  color:#991b1b;
  border-radius:18px;
  font-weight:800;
}
@media (max-width: 1100px) {
  .hero, .grid { grid-template-columns:1fr; }
  .kpis { grid-template-columns: repeat(2, minmax(0, 1fr)); }
}
@media (max-width: 720px) {
  .formGrid, .kpis { grid-template-columns:1fr; }
  .hero { padding:20px; }
}

/* ============================================================
   ANGELCARE B2B — REAL CAMPAIGNS PREMIUM UI
   Target: B2BMegaIWorkspace mode="campaigns"
   UI-only. No backend / API / CRUD / data logic changes.
   ============================================================ */

.campaignsPremiumSurface {
  position: relative;
  min-height: 100%;
  background:
    radial-gradient(circle at 6% 3%, rgba(37, 99, 235, .17), transparent 30%),
    radial-gradient(circle at 96% 6%, rgba(245, 158, 11, .15), transparent 30%),
    radial-gradient(circle at 70% 96%, rgba(236, 72, 153, .11), transparent 34%),
    linear-gradient(180deg, #f8fafc 0%, #edf4f8 100%) !important;
}

/* Proof + page identity */
.campaignsPremiumSurface::before {
  content: "🚀 CAMPAIGN COMMAND CENTER";
  display: inline-flex;
  align-items: center;
  width: fit-content;
  min-height: 34px;
  margin: 0 0 14px;
  padding: 0 14px;
  border-radius: 999px;
  background: linear-gradient(135deg, #eff6ff, #fff7ed);
  border: 1px solid #bfdbfe;
  color: #1d4ed8;
  font-size: 11px;
  font-weight: 950;
  letter-spacing: .12em;
  box-shadow: 0 12px 28px rgba(37, 99, 235, .12);
}

/* Hero */
.campaignsPremiumSurface .hero,
.campaignsPremiumSurface .header,
.campaignsPremiumSurface .topbar {
  position: relative !important;
  overflow: hidden !important;
  border-radius: 40px !important;
  border: 1px solid rgba(226, 232, 240, .98) !important;
  background:
    radial-gradient(circle at 84% 12%, rgba(37, 99, 235, .22), transparent 30%),
    radial-gradient(circle at 14% 88%, rgba(245, 158, 11, .16), transparent 34%),
    radial-gradient(circle at 96% 92%, rgba(236, 72, 153, .12), transparent 28%),
    linear-gradient(135deg, #ffffff 0%, #f8fbff 54%, #f4f8ff 100%) !important;
  box-shadow:
    0 36px 100px rgba(15, 23, 42, .13),
    inset 0 1px 0 rgba(255,255,255,.96) !important;
}

.campaignsPremiumSurface .hero::before,
.campaignsPremiumSurface .header::before,
.campaignsPremiumSurface .topbar::before {
  content: "";
  position: absolute;
  inset: 0;
  pointer-events: none;
  background-image:
    linear-gradient(rgba(15, 23, 42, .032) 1px, transparent 1px),
    linear-gradient(90deg, rgba(15, 23, 42, .032) 1px, transparent 1px);
  background-size: 30px 30px;
  mask-image: linear-gradient(90deg, rgba(0,0,0,.68), transparent 76%);
}

.campaignsPremiumSurface .hero::after,
.campaignsPremiumSurface .header::after,
.campaignsPremiumSurface .topbar::after {
  content: "📣";
  position: absolute;
  right: 34px;
  bottom: 28px;
  display: grid;
  place-items: center;
  width: 106px;
  height: 106px;
  border-radius: 36px;
  background:
    linear-gradient(135deg, rgba(37, 99, 235, .14), rgba(245, 158, 11, .12)),
    #ffffff;
  border: 1px solid rgba(226, 232, 240, .94);
  box-shadow: 0 30px 70px rgba(37, 99, 235, .18);
  font-size: 46px;
}

.campaignsPremiumSurface .hero :global(h1),
.campaignsPremiumSurface .hero :global(h2),
.campaignsPremiumSurface .header :global(h1),
.campaignsPremiumSurface .header :global(h2),
.campaignsPremiumSurface .topbar :global(h1),
.campaignsPremiumSurface .topbar :global(h2) {
  color: #07111f !important;
  letter-spacing: -0.07em !important;
  line-height: .93 !important;
  font-weight: 950 !important;
}

.campaignsPremiumSurface .hero :global(p),
.campaignsPremiumSurface .header :global(p),
.campaignsPremiumSurface .topbar :global(p) {
  color: #64748b !important;
  line-height: 1.72;
  max-width: 880px;
}

/* KPI strip */
.campaignsPremiumSurface .kpis,
.campaignsPremiumSurface .metrics,
.campaignsPremiumSurface .summaryGrid {
  gap: 14px !important;
}

.campaignsPremiumSurface .kpi,
.campaignsPremiumSurface .metric,
.campaignsPremiumSurface .stat {
  position: relative !important;
  overflow: hidden;
  border-radius: 28px !important;
  border: 1px solid rgba(226, 232, 240, .96) !important;
  background:
    linear-gradient(135deg, rgba(255,255,255,.97), rgba(248,250,252,.91)) !important;
  box-shadow: 0 22px 56px rgba(15, 23, 42, .078) !important;
}

.campaignsPremiumSurface .kpi::after,
.campaignsPremiumSurface .metric::after,
.campaignsPremiumSurface .stat::after {
  content: "";
  position: absolute;
  top: -42px;
  right: -40px;
  width: 116px;
  height: 116px;
  border-radius: 999px;
  background: linear-gradient(135deg, rgba(37, 99, 235, .13), rgba(245, 158, 11, .09));
}

.campaignsPremiumSurface .kpi :global(small),
.campaignsPremiumSurface .metric :global(small),
.campaignsPremiumSurface .stat :global(small) {
  color: #64748b !important;
  font-size: 11px !important;
  font-weight: 950 !important;
  letter-spacing: .10em;
  text-transform: uppercase;
}

.campaignsPremiumSurface .kpi :global(strong),
.campaignsPremiumSurface .metric :global(strong),
.campaignsPremiumSurface .stat :global(strong) {
  color: #07111f !important;
  font-weight: 950 !important;
  letter-spacing: -0.05em;
}

/* Main campaign layout */
.campaignsPremiumSurface .grid {
  gap: 18px !important;
}

.campaignsPremiumSurface .panel {
  position: relative !important;
  overflow: hidden !important;
  border-radius: 34px !important;
  border: 1px solid rgba(226, 232, 240, .98) !important;
  background:
    radial-gradient(circle at top right, rgba(37, 99, 235, .09), transparent 34%),
    linear-gradient(135deg, #ffffff 0%, #f8fbff 100%) !important;
  box-shadow:
    0 28px 76px rgba(15, 23, 42, .088),
    inset 0 1px 0 rgba(255,255,255,.96) !important;
}

.campaignsPremiumSurface .panel::after {
  content: "";
  position: absolute;
  inset: 0;
  pointer-events: none;
  opacity: .52;
  background-image:
    linear-gradient(rgba(15, 23, 42, .024) 1px, transparent 1px),
    linear-gradient(90deg, rgba(15, 23, 42, .024) 1px, transparent 1px);
  background-size: 28px 28px;
  mask-image: linear-gradient(180deg, rgba(0,0,0,.60), transparent 78%);
}

.campaignsPremiumSurface .panelHeader {
  position: relative;
  z-index: 1;
}

.campaignsPremiumSurface .panelHeader :global(h2),
.campaignsPremiumSurface .panelHeader :global(h3) {
  color: #07111f !important;
  font-weight: 950 !important;
  letter-spacing: -0.045em !important;
}

.campaignsPremiumSurface .panelHeader :global(p) {
  color: #64748b !important;
  line-height: 1.6;
}

/* Campaign table */
.campaignsPremiumSurface .tableWrap {
  position: relative;
  z-index: 1;
  margin-top: 14px;
  border-radius: 30px !important;
  overflow: hidden;
  border: 1px solid rgba(226, 232, 240, .98) !important;
  background: #ffffff !important;
  box-shadow: 0 25px 68px rgba(15, 23, 42, .078);
}

.campaignsPremiumSurface .tableWrap :global(table) {
  width: 100%;
  border-collapse: separate !important;
  border-spacing: 0 !important;
}

.campaignsPremiumSurface .tableWrap :global(thead) {
  background: linear-gradient(135deg, #f8fafc 0%, #eef6ff 52%, #fff7ed 100%) !important;
}

.campaignsPremiumSurface .tableWrap :global(th) {
  height: 58px !important;
  padding: 0 18px !important;
  color: #64748b !important;
  font-size: 11px !important;
  letter-spacing: .13em !important;
  text-transform: uppercase !important;
  font-weight: 950 !important;
}

.campaignsPremiumSurface .tableWrap :global(td) {
  padding: 16px 18px !important;
  border-top: 1px solid rgba(226, 232, 240, .88) !important;
  color: #0f172a !important;
}

.campaignsPremiumSurface .tableWrap :global(tbody tr) {
  transition: background .16s ease, box-shadow .16s ease;
}

.campaignsPremiumSurface .tableWrap :global(tbody tr:hover) {
  background: linear-gradient(90deg, rgba(37, 99, 235, .055), rgba(245, 158, 11, .04)) !important;
  box-shadow: inset 4px 0 0 #2563eb;
}

.campaignsPremiumSurface .tableWrap :global(td:first-child) {
  position: relative;
  padding-left: 62px !important;
}

.campaignsPremiumSurface .tableWrap :global(td:first-child)::before {
  content: "🚀";
  position: absolute;
  left: 18px;
  top: 50%;
  transform: translateY(-50%);
  display: grid;
  place-items: center;
  width: 34px;
  height: 34px;
  border-radius: 14px;
  background: linear-gradient(135deg, #eff6ff, #ecfeff);
  border: 1px solid #bfdbfe;
  box-shadow: 0 10px 22px rgba(37, 99, 235, .10);
  font-size: 15px;
}

.campaignsPremiumSurface .tableWrap :global(td strong) {
  display: block;
  color: #07111f !important;
  font-weight: 950 !important;
  letter-spacing: -0.02em;
}

.campaignsPremiumSurface .tableWrap :global(td small) {
  display: block;
  margin-top: 5px;
  color: #64748b !important;
}

/* Badge / status */
.campaignsPremiumSurface .badge {
  display: inline-flex !important;
  align-items: center !important;
  gap: 7px;
  min-height: 30px !important;
  padding: 0 12px !important;
  border-radius: 999px !important;
  border: 1px solid #bfdbfe !important;
  background: #eff6ff !important;
  color: #1d4ed8 !important;
  font-size: 11px !important;
  font-weight: 950 !important;
  box-shadow: 0 8px 18px rgba(37, 99, 235, .06);
}

.campaignsPremiumSurface .badge::before {
  content: "";
  width: 7px;
  height: 7px;
  border-radius: 999px;
  background: #3b82f6;
  box-shadow: 0 0 0 3px rgba(59, 130, 246, .12);
}

/* Sequence cards */
.campaignsPremiumSurface .cards {
  position: relative;
  z-index: 1;
  display: grid;
  gap: 14px !important;
}

.campaignsPremiumSurface .card {
  position: relative !important;
  overflow: hidden;
  border-radius: 27px !important;
  border: 1px solid rgba(226, 232, 240, .98) !important;
  background:
    radial-gradient(circle at top right, rgba(37, 99, 235, .08), transparent 34%),
    linear-gradient(135deg, #ffffff 0%, #f8fbff 100%) !important;
  box-shadow: 0 20px 50px rgba(15, 23, 42, .075) !important;
  transition: transform .18s ease, box-shadow .18s ease, border-color .18s ease;
}

.campaignsPremiumSurface .card:hover {
  transform: translateY(-3px);
  border-color: #bfdbfe !important;
  box-shadow: 0 30px 70px rgba(37, 99, 235, .14) !important;
}

.campaignsPremiumSurface .card::before {
  content: "🧭";
  display: grid;
  place-items: center;
  width: 42px;
  height: 42px;
  border-radius: 17px;
  margin-bottom: 13px;
  background: linear-gradient(135deg, #f5f3ff, #eff6ff);
  border: 1px solid #ddd6fe;
  box-shadow: 0 12px 26px rgba(124, 58, 237, .10);
  font-size: 18px;
}

.campaignsPremiumSurface .card:nth-child(2n)::before {
  content: "📧";
  background: linear-gradient(135deg, #eff6ff, #ecfeff);
  border-color: #bfdbfe;
}

.campaignsPremiumSurface .card:nth-child(3n)::before {
  content: "💬";
  background: linear-gradient(135deg, #ecfdf5, #f0fdfa);
  border-color: #bbf7d0;
}

.campaignsPremiumSurface .card :global(h3) {
  color: #07111f !important;
  font-weight: 950 !important;
  letter-spacing: -0.035em;
}

.campaignsPremiumSurface .card :global(p) {
  color: #64748b !important;
  line-height: 1.58;
}

/* Buttons */
.campaignsPremiumSurface .primaryButton,
.campaignsPremiumSurface .actionButton,
.campaignsPremiumSurface .submitButton {
  background: linear-gradient(135deg, #07111f 0%, #0f172a 48%, #1e3a8a 100%) !important;
  color: #ffffff !important;
  box-shadow: 0 16px 34px rgba(37, 99, 235, .20) !important;
}

.campaignsPremiumSurface .secondaryButton,
.campaignsPremiumSurface .smallButton {
  background: #ffffff !important;
  border: 1px solid #e5e7eb !important;
  color: #0f172a !important;
  box-shadow: 0 10px 24px rgba(15, 23, 42, .055);
}

.campaignsPremiumSurface :global(button) {
  border-radius: 16px !important;
  font-weight: 950 !important;
  transition: transform .16s ease, box-shadow .16s ease, border-color .16s ease !important;
}

.campaignsPremiumSurface :global(button:hover) {
  transform: translateY(-2px);
}

/* Inputs / modals if opened */
.campaignsPremiumSurface :global(input),
.campaignsPremiumSurface :global(select),
.campaignsPremiumSurface :global(textarea) {
  border-radius: 18px !important;
  border: 1px solid #e2e8f0 !important;
  background: #ffffff !important;
  color: #0f172a !important;
  font-weight: 800;
}

.campaignsPremiumSurface :global(input:focus),
.campaignsPremiumSurface :global(select:focus),
.campaignsPremiumSurface :global(textarea:focus) {
  outline: none !important;
  border-color: #93c5fd !important;
  box-shadow: 0 0 0 4px rgba(37, 99, 235, .10) !important;
}

.campaignsPremiumSurface .modalBackdrop {
  z-index: 2147483000 !important;
  background:
    radial-gradient(circle at 50% 0%, rgba(37, 99, 235, .18), transparent 34%),
    rgba(15, 23, 42, .58) !important;
  backdrop-filter: blur(12px);
}

.campaignsPremiumSurface .modal,
.campaignsPremiumSurface .drawer {
  z-index: 2147483001 !important;
  border: 1px solid rgba(226, 232, 240, .98) !important;
  background:
    radial-gradient(circle at top right, rgba(37, 99, 235, .10), transparent 28%),
    linear-gradient(180deg, #ffffff, #f8fafc) !important;
  box-shadow: 0 40px 110px rgba(15, 23, 42, .30) !important;
}

@media (max-width: 1200px) {
  .campaignsPremiumSurface .hero::after,
  .campaignsPremiumSurface .header::after,
  .campaignsPremiumSurface .topbar::after {
    display: none;
  }
}


/* ============================================================
   ANGELCARE B2B — SEQUENCE OPEN / EDIT / DELETE UI
   Target: B2BMegaIWorkspace campaigns mode
   ============================================================ */

.sequenceMeta {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  margin: 14px 0;
}

.sequenceMeta span {
  display: inline-flex;
  align-items: center;
  min-height: 28px;
  padding: 0 10px;
  border-radius: 999px;
  background: #eff6ff;
  border: 1px solid #bfdbfe;
  color: #1d4ed8;
  font-size: 11px;
  font-weight: 950;
}

.sequenceMeta span:nth-child(2) {
  background: #ecfdf5;
  border-color: #bbf7d0;
  color: #047857;
}

.sequenceEditorBackdrop {
  position: fixed;
  inset: 0;
  z-index: 2147483000;
  display: grid;
  place-items: center;
  padding: 28px;
  background:
    radial-gradient(circle at 50% 0%, rgba(37, 99, 235, .18), transparent 34%),
    rgba(15, 23, 42, .58);
  backdrop-filter: blur(14px);
}

.sequenceEditorModal {
  width: min(1180px, 96vw);
  max-height: min(860px, 92vh);
  overflow: auto;
  border-radius: 36px;
  border: 1px solid rgba(226, 232, 240, .98);
  background:
    radial-gradient(circle at 92% 0%, rgba(37, 99, 235, .13), transparent 34%),
    radial-gradient(circle at 6% 100%, rgba(16, 185, 129, .10), transparent 34%),
    linear-gradient(180deg, #ffffff, #f8fafc);
  box-shadow: 0 46px 120px rgba(15, 23, 42, .34);
  padding: 28px;
}

.sequenceEditorHeader {
  display: flex;
  justify-content: space-between;
  gap: 24px;
  align-items: flex-start;
  padding-bottom: 22px;
  border-bottom: 1px solid #e5e7eb;
}

.sequenceEditorEyebrow {
  display: inline-flex;
  align-items: center;
  width: fit-content;
  min-height: 30px;
  padding: 0 12px;
  border-radius: 999px;
  background: linear-gradient(135deg, #eff6ff, #ecfdf5);
  border: 1px solid #bfdbfe;
  color: #1d4ed8;
  font-size: 10px;
  font-weight: 950;
  letter-spacing: .12em;
  text-transform: uppercase;
}

.sequenceEditorHeader h2 {
  margin: 12px 0 8px;
  color: #07111f;
  font-size: clamp(32px, 4vw, 54px);
  line-height: .95;
  letter-spacing: -.06em;
  font-weight: 950;
}

.sequenceEditorHeader p {
  margin: 0;
  color: #64748b;
  line-height: 1.65;
}

.sequenceIconButton {
  width: 46px;
  height: 46px;
  border-radius: 18px;
  border: 1px solid #e5e7eb;
  background: #ffffff;
  color: #0f172a;
  font-size: 24px;
  font-weight: 900;
  box-shadow: 0 14px 32px rgba(15, 23, 42, .08);
}

.sequenceEditorGrid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 16px;
  margin-top: 24px;
}

.sequenceEditorGrid label {
  display: grid;
  gap: 8px;
}

.sequenceEditorGrid label span {
  color: #334155;
  font-size: 12px;
  font-weight: 950;
  letter-spacing: .08em;
  text-transform: uppercase;
}

.sequenceEditorGrid input,
.sequenceEditorGrid select,
.sequenceEditorGrid textarea {
  width: 100%;
  border-radius: 18px;
  border: 1px solid #e2e8f0;
  background: #ffffff;
  color: #0f172a;
  font-weight: 800;
  padding: 13px 14px;
  box-shadow: inset 0 1px 0 rgba(255,255,255,.88);
}

.sequenceEditorGrid textarea {
  min-height: 120px;
  resize: vertical;
  line-height: 1.55;
}

.sequenceEditorGrid small {
  color: #64748b;
  line-height: 1.45;
}

.sequenceEditorWide {
  grid-column: 1 / -1;
}

.sequenceEditorPreview {
  margin-top: 22px;
  border-radius: 28px;
  border: 1px solid #e5e7eb;
  background:
    radial-gradient(circle at top right, rgba(37, 99, 235, .08), transparent 34%),
    #ffffff;
  box-shadow: 0 20px 50px rgba(15, 23, 42, .07);
  padding: 20px;
}

.sequenceEditorPreview h3 {
  margin: 0 0 14px;
  color: #07111f;
  font-size: 18px;
  font-weight: 950;
  letter-spacing: -.03em;
}

.sequencePreviewSteps {
  display: grid;
  gap: 10px;
}

.sequencePreviewStep {
  display: grid;
  grid-template-columns: 42px 120px 70px 1fr;
  align-items: center;
  gap: 12px;
  border-radius: 20px;
  border: 1px solid #e5e7eb;
  background: linear-gradient(135deg, #ffffff, #f8fbff);
  padding: 12px;
}

.sequencePreviewStep b {
  display: grid;
  place-items: center;
  width: 34px;
  height: 34px;
  border-radius: 14px;
  background: #eff6ff;
  border: 1px solid #bfdbfe;
  color: #1d4ed8;
}

.sequencePreviewStep span {
  color: #0f172a;
  font-weight: 950;
}

.sequencePreviewStep strong {
  color: #047857;
  font-weight: 950;
}

.sequencePreviewStep p {
  margin: 0;
  color: #64748b;
}

.sequenceEditorActions {
  display: flex;
  justify-content: space-between;
  gap: 14px;
  align-items: center;
  margin-top: 24px;
  padding-top: 18px;
  border-top: 1px solid #e5e7eb;
}

.sequenceEditorActions > div {
  display: flex;
  gap: 10px;
}

.dangerButton {
  min-height: 46px;
  border-radius: 16px;
  border: 1px solid #fecdd3;
  background: #fff1f2;
  color: #be123c;
  font-weight: 950;
  padding: 0 16px;
}

@media (max-width: 900px) {
  .sequenceEditorGrid {
    grid-template-columns: 1fr;
  }

  .sequencePreviewStep {
    grid-template-columns: 38px 1fr;
  }

  .sequencePreviewStep p {
    grid-column: 1 / -1;
  }

  .sequenceEditorActions {
    align-items: stretch;
    flex-direction: column;
  }

  .sequenceEditorActions > div {
    flex-direction: column;
  }
}


/* ============================================================
   ANGELCARE B2B — CAMPAIGN OPEN / EDIT / DELETE UI
   Target: B2BMegaIWorkspace campaigns mode
   ============================================================ */

.rowActions {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
  justify-content: flex-end;
  align-items: center;
}

.rowActions button:first-child {
  background: #ffffff !important;
  color: #0f172a !important;
  border: 1px solid #e5e7eb !important;
  box-shadow: 0 10px 24px rgba(15, 23, 42, .06) !important;
}

.campaignEditorBackdrop {
  position: fixed;
  inset: 0;
  z-index: 2147483000;
  display: grid;
  place-items: center;
  padding: 28px;
  background:
    radial-gradient(circle at 50% 0%, rgba(37, 99, 235, .18), transparent 34%),
    rgba(15, 23, 42, .58);
  backdrop-filter: blur(14px);
}

.campaignEditorModal {
  width: min(1180px, 96vw);
  max-height: min(860px, 92vh);
  overflow: auto;
  border-radius: 36px;
  border: 1px solid rgba(226, 232, 240, .98);
  background:
    radial-gradient(circle at 92% 0%, rgba(37, 99, 235, .13), transparent 34%),
    radial-gradient(circle at 6% 100%, rgba(245, 158, 11, .12), transparent 34%),
    linear-gradient(180deg, #ffffff, #f8fafc);
  box-shadow: 0 46px 120px rgba(15, 23, 42, .34);
  padding: 28px;
}

.campaignEditorHeader {
  display: flex;
  justify-content: space-between;
  gap: 24px;
  align-items: flex-start;
  padding-bottom: 22px;
  border-bottom: 1px solid #e5e7eb;
}

.campaignEditorEyebrow {
  display: inline-flex;
  align-items: center;
  width: fit-content;
  min-height: 30px;
  padding: 0 12px;
  border-radius: 999px;
  background: linear-gradient(135deg, #eff6ff, #fff7ed);
  border: 1px solid #bfdbfe;
  color: #1d4ed8;
  font-size: 10px;
  font-weight: 950;
  letter-spacing: .12em;
  text-transform: uppercase;
}

.campaignEditorHeader h2 {
  margin: 12px 0 8px;
  color: #07111f;
  font-size: clamp(32px, 4vw, 54px);
  line-height: .95;
  letter-spacing: -.06em;
  font-weight: 950;
}

.campaignEditorHeader p {
  margin: 0;
  color: #64748b;
  line-height: 1.65;
}

.campaignIconButton {
  width: 46px;
  height: 46px;
  border-radius: 18px;
  border: 1px solid #e5e7eb;
  background: #ffffff;
  color: #0f172a;
  font-size: 24px;
  font-weight: 900;
  box-shadow: 0 14px 32px rgba(15, 23, 42, .08);
}

.campaignEditorGrid {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: 16px;
  margin-top: 24px;
}

.campaignEditorGrid label {
  display: grid;
  gap: 8px;
}

.campaignEditorGrid label span {
  color: #334155;
  font-size: 12px;
  font-weight: 950;
  letter-spacing: .08em;
  text-transform: uppercase;
}

.campaignEditorGrid input,
.campaignEditorGrid select,
.campaignEditorGrid textarea {
  width: 100%;
  border-radius: 18px;
  border: 1px solid #e2e8f0;
  background: #ffffff;
  color: #0f172a;
  font-weight: 800;
  padding: 13px 14px;
}

.campaignEditorGrid textarea {
  min-height: 120px;
  resize: vertical;
  line-height: 1.55;
}

.campaignEditorWide {
  grid-column: 1 / -1;
}

.campaignEditorPreview {
  margin-top: 22px;
  border-radius: 28px;
  border: 1px solid #e5e7eb;
  background:
    radial-gradient(circle at top right, rgba(37, 99, 235, .08), transparent 34%),
    #ffffff;
  box-shadow: 0 20px 50px rgba(15, 23, 42, .07);
  padding: 20px;
}

.campaignEditorPreview h3 {
  margin: 0 0 14px;
  color: #07111f;
  font-size: 18px;
  font-weight: 950;
  letter-spacing: -.03em;
}

.campaignPreviewGrid {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: 12px;
}

.campaignPreviewGrid article {
  border-radius: 20px;
  border: 1px solid #e5e7eb;
  background: linear-gradient(135deg, #ffffff, #f8fbff);
  padding: 14px;
}

.campaignPreviewGrid span {
  display: block;
  color: #64748b;
  font-size: 11px;
  font-weight: 950;
  letter-spacing: .1em;
  text-transform: uppercase;
  margin-bottom: 8px;
}

.campaignPreviewGrid strong {
  color: #07111f;
  font-weight: 950;
}

.campaignPreviewGrid p {
  margin: 0;
  color: #64748b;
  line-height: 1.5;
}

.campaignEditorActions {
  display: flex;
  justify-content: space-between;
  gap: 14px;
  align-items: center;
  margin-top: 24px;
  padding-top: 18px;
  border-top: 1px solid #e5e7eb;
}

.campaignEditorActions > div {
  display: flex;
  gap: 10px;
}

@media (max-width: 900px) {
  .campaignEditorGrid,
  .campaignPreviewGrid {
    grid-template-columns: 1fr;
  }

  .campaignEditorActions {
    align-items: stretch;
    flex-direction: column;
  }

  .campaignEditorActions > div {
    flex-direction: column;
  }
}


/* ============================================================
   ANGELCARE B2B — IMPORT HUB PREMIUM UIX
   Page: /b2b-partnerships/imports
   UI-only. No backend / API / CRUD / import logic changes.
   ============================================================ */

.importsPremiumSurface {
  position: relative;
  min-height: 100%;
  background:
    radial-gradient(circle at 6% 3%, rgba(37, 99, 235, .16), transparent 30%),
    radial-gradient(circle at 96% 6%, rgba(16, 185, 129, .14), transparent 30%),
    radial-gradient(circle at 72% 96%, rgba(245, 158, 11, .10), transparent 34%),
    linear-gradient(180deg, #f8fafc 0%, #edf4f8 100%) !important;
}

/* Proof identity badge */
.importsPremiumSurface::before {
  content: "📥 IMPORT CONTROL ROOM";
  display: inline-flex;
  align-items: center;
  width: fit-content;
  min-height: 34px;
  margin: 0 0 14px;
  padding: 0 14px;
  border-radius: 999px;
  background: linear-gradient(135deg, #ecfdf5, #eff6ff);
  border: 1px solid #bbf7d0;
  color: #047857;
  font-size: 11px;
  font-weight: 950;
  letter-spacing: .12em;
  box-shadow: 0 12px 28px rgba(16, 185, 129, .12);
}

/* Hero / top import banner */
.importsPremiumSurface .hero,
.importsPremiumSurface .header,
.importsPremiumSurface .topbar,
.importsPremiumSurface .importHero,
.importsPremiumSurface .commandHero {
  position: relative !important;
  overflow: hidden !important;
  border-radius: 40px !important;
  border: 1px solid rgba(226, 232, 240, .98) !important;
  background:
    radial-gradient(circle at 84% 12%, rgba(37, 99, 235, .18), transparent 30%),
    radial-gradient(circle at 14% 88%, rgba(16, 185, 129, .15), transparent 34%),
    radial-gradient(circle at 96% 92%, rgba(245, 158, 11, .10), transparent 28%),
    linear-gradient(135deg, #ffffff 0%, #f8fbff 54%, #f4f8ff 100%) !important;
  box-shadow:
    0 36px 100px rgba(15, 23, 42, .13),
    inset 0 1px 0 rgba(255,255,255,.96) !important;
}

.importsPremiumSurface .hero::before,
.importsPremiumSurface .header::before,
.importsPremiumSurface .topbar::before,
.importsPremiumSurface .importHero::before,
.importsPremiumSurface .commandHero::before {
  content: "";
  position: absolute;
  inset: 0;
  pointer-events: none;
  background-image:
    linear-gradient(rgba(15, 23, 42, .032) 1px, transparent 1px),
    linear-gradient(90deg, rgba(15, 23, 42, .032) 1px, transparent 1px);
  background-size: 30px 30px;
  mask-image: linear-gradient(90deg, rgba(0,0,0,.68), transparent 76%);
}

.importsPremiumSurface .hero::after,
.importsPremiumSurface .header::after,
.importsPremiumSurface .topbar::after,
.importsPremiumSurface .importHero::after,
.importsPremiumSurface .commandHero::after {
  content: "📊";
  position: absolute;
  right: 34px;
  bottom: 28px;
  display: grid;
  place-items: center;
  width: 106px;
  height: 106px;
  border-radius: 36px;
  background:
    linear-gradient(135deg, rgba(16, 185, 129, .13), rgba(37, 99, 235, .12)),
    #ffffff;
  border: 1px solid rgba(226, 232, 240, .94);
  box-shadow: 0 30px 70px rgba(16, 185, 129, .16);
  font-size: 46px;
}

.importsPremiumSurface .hero :global(h1),
.importsPremiumSurface .header :global(h1),
.importsPremiumSurface .topbar :global(h1),
.importsPremiumSurface .importHero :global(h1),
.importsPremiumSurface .commandHero :global(h1),
.importsPremiumSurface :global(h1) {
  color: #07111f !important;
  letter-spacing: -0.07em !important;
  line-height: .93 !important;
  font-weight: 950 !important;
}

.importsPremiumSurface :global(h2),
.importsPremiumSurface :global(h3),
.importsPremiumSurface :global(strong) {
  color: #07111f !important;
  letter-spacing: -0.035em !important;
  font-weight: 950 !important;
}

.importsPremiumSurface :global(p),
.importsPremiumSurface :global(small) {
  color: #64748b !important;
  line-height: 1.62;
}

/* Core panels and cards */
.importsPremiumSurface .panel,
.importsPremiumSurface .card,
.importsPremiumSurface .section,
.importsPremiumSurface .sectionCard,
.importsPremiumSurface .importPanel,
.importsPremiumSurface .batchPanel,
.importsPremiumSurface .standardsPanel,
.importsPremiumSurface .validationPanel,
.importsPremiumSurface .stagingPanel,
.importsPremiumSurface .automationPanel,
.importsPremiumSurface .box,
.importsPremiumSurface .tile,
.importsPremiumSurface :global(article),
.importsPremiumSurface :global([class*="card"]),
.importsPremiumSurface :global([class*="Card"]),
.importsPremiumSurface :global([class*="panel"]),
.importsPremiumSurface :global([class*="Panel"]) {
  position: relative;
  overflow: hidden;
  border-radius: 32px !important;
  border: 1px solid rgba(226, 232, 240, .98) !important;
  background:
    radial-gradient(circle at top right, rgba(37, 99, 235, .08), transparent 34%),
    linear-gradient(135deg, #ffffff 0%, #f8fbff 100%) !important;
  box-shadow:
    0 26px 72px rgba(15, 23, 42, .085),
    inset 0 1px 0 rgba(255,255,255,.96) !important;
}

/* Add icon identity to visible cards/articles */
.importsPremiumSurface :global(article)::before,
.importsPremiumSurface :global([class*="card"])::before,
.importsPremiumSurface :global([class*="Card"])::before {
  content: "📥";
  display: grid;
  place-items: center;
  width: 42px;
  height: 42px;
  border-radius: 17px;
  margin-bottom: 13px;
  background: linear-gradient(135deg, #ecfdf5, #eff6ff);
  border: 1px solid #bbf7d0;
  box-shadow: 0 12px 26px rgba(16, 185, 129, .11);
  font-size: 18px;
}

.importsPremiumSurface :global(article:nth-of-type(2n))::before {
  content: "✅";
  background: linear-gradient(135deg, #eff6ff, #ecfeff);
  border-color: #bfdbfe;
}

.importsPremiumSurface :global(article:nth-of-type(3n))::before {
  content: "🧹";
  background: linear-gradient(135deg, #fff7ed, #fefce8);
  border-color: #fed7aa;
}

.importsPremiumSurface :global(article:nth-of-type(4n))::before {
  content: "🚀";
  background: linear-gradient(135deg, #f5f3ff, #eff6ff);
  border-color: #ddd6fe;
}

/* KPI cards */
.importsPremiumSurface .kpis,
.importsPremiumSurface .metrics,
.importsPremiumSurface .summaryGrid,
.importsPremiumSurface .stats {
  gap: 14px !important;
}

.importsPremiumSurface .kpi,
.importsPremiumSurface .metric,
.importsPremiumSurface .stat,
.importsPremiumSurface :global([class*="metric"]),
.importsPremiumSurface :global([class*="Metric"]),
.importsPremiumSurface :global([class*="stat"]),
.importsPremiumSurface :global([class*="Stat"]) {
  position: relative !important;
  overflow: hidden;
  border-radius: 28px !important;
  border: 1px solid rgba(226, 232, 240, .96) !important;
  background:
    linear-gradient(135deg, rgba(255,255,255,.97), rgba(248,250,252,.91)) !important;
  box-shadow: 0 22px 56px rgba(15, 23, 42, .078) !important;
}

.importsPremiumSurface .kpi::after,
.importsPremiumSurface .metric::after,
.importsPremiumSurface .stat::after,
.importsPremiumSurface :global([class*="metric"])::after,
.importsPremiumSurface :global([class*="Metric"])::after,
.importsPremiumSurface :global([class*="stat"])::after,
.importsPremiumSurface :global([class*="Stat"])::after {
  content: "";
  position: absolute;
  top: -42px;
  right: -40px;
  width: 116px;
  height: 116px;
  border-radius: 999px;
  background: linear-gradient(135deg, rgba(16, 185, 129, .13), rgba(37, 99, 235, .09));
}

/* Import batch table */
.importsPremiumSurface .tableWrap,
.importsPremiumSurface .tableContainer,
.importsPremiumSurface .importTable,
.importsPremiumSurface .batchTable,
.importsPremiumSurface :global(table) {
  border-radius: 30px !important;
  overflow: hidden;
  border-collapse: separate !important;
  border-spacing: 0 !important;
  background: #ffffff !important;
  box-shadow: 0 25px 68px rgba(15, 23, 42, .078);
}

.importsPremiumSurface :global(thead) {
  background: linear-gradient(135deg, #f8fafc 0%, #eef6ff 52%, #ecfdf5 100%) !important;
}

.importsPremiumSurface :global(th) {
  height: 58px !important;
  padding: 0 18px !important;
  color: #64748b !important;
  font-size: 11px !important;
  letter-spacing: .13em !important;
  text-transform: uppercase !important;
  font-weight: 950 !important;
}

.importsPremiumSurface :global(td) {
  padding: 16px 18px !important;
  border-top: 1px solid rgba(226, 232, 240, .88) !important;
  color: #0f172a !important;
}

.importsPremiumSurface :global(tbody tr) {
  transition: background .16s ease, box-shadow .16s ease;
}

.importsPremiumSurface :global(tbody tr:hover) {
  background: linear-gradient(90deg, rgba(16, 185, 129, .055), rgba(37, 99, 235, .04)) !important;
  box-shadow: inset 4px 0 0 #10b981;
}

/* CSV standards blocks */
.importsPremiumSurface .standardsPanel,
.importsPremiumSurface .standardsCard,
.importsPremiumSurface .schemaCard,
.importsPremiumSurface .fieldCard,
.importsPremiumSurface :global([class*="standard"]),
.importsPremiumSurface :global([class*="Standard"]),
.importsPremiumSurface :global([class*="schema"]),
.importsPremiumSurface :global([class*="Schema"]) {
  background:
    radial-gradient(circle at top right, rgba(245, 158, 11, .10), transparent 34%),
    linear-gradient(135deg, #ffffff 0%, #fffdf7 100%) !important;
}

.importsPremiumSurface .fieldCard::before,
.importsPremiumSurface .schemaCard::before,
.importsPremiumSurface :global([class*="standard"])::before,
.importsPremiumSurface :global([class*="Standard"])::before {
  content: "🧾";
  display: grid;
  place-items: center;
  width: 38px;
  height: 38px;
  border-radius: 15px;
  background: #fff7ed;
  border: 1px solid #fed7aa;
  box-shadow: 0 10px 22px rgba(245, 158, 11, .10);
}

/* Validation / status pills */
.importsPremiumSurface .pill,
.importsPremiumSurface .tag,
.importsPremiumSurface .status,
.importsPremiumSurface .chip,
.importsPremiumSurface .badge,
.importsPremiumSurface .badgeSoft,
.importsPremiumSurface :global([class*="pill"]),
.importsPremiumSurface :global([class*="Pill"]),
.importsPremiumSurface :global([class*="tag"]),
.importsPremiumSurface :global([class*="Tag"]),
.importsPremiumSurface :global([class*="badge"]),
.importsPremiumSurface :global([class*="Badge"]),
.importsPremiumSurface :global([class*="status"]),
.importsPremiumSurface :global([class*="Status"]) {
  display: inline-flex !important;
  align-items: center !important;
  gap: 7px;
  min-height: 30px !important;
  padding: 0 12px !important;
  border-radius: 999px !important;
  border: 1px solid #e5e7eb !important;
  background: #f8fafc !important;
  color: #334155 !important;
  font-size: 11px !important;
  font-weight: 950 !important;
  box-shadow: 0 8px 18px rgba(15, 23, 42, .04);
}

.importsPremiumSurface .pill::before,
.importsPremiumSurface .tag::before,
.importsPremiumSurface .status::before,
.importsPremiumSurface .chip::before,
.importsPremiumSurface .badge::before,
.importsPremiumSurface :global([class*="pill"])::before,
.importsPremiumSurface :global([class*="Pill"])::before,
.importsPremiumSurface :global([class*="tag"])::before,
.importsPremiumSurface :global([class*="Tag"])::before,
.importsPremiumSurface :global([class*="status"])::before,
.importsPremiumSurface :global([class*="Status"])::before {
  content: "";
  width: 7px;
  height: 7px;
  border-radius: 999px;
  background: #10b981;
  box-shadow: 0 0 0 3px rgba(16, 185, 129, .12);
}

/* Form / CSV paste modal / import composer */
.importsPremiumSurface .formPanel,
.importsPremiumSurface .composer,
.importsPremiumSurface .importForm,
.importsPremiumSurface .csvBox,
.importsPremiumSurface .textareaPanel,
.importsPremiumSurface :global(form) {
  position: relative;
  overflow: hidden;
  border-radius: 30px !important;
  border: 1px solid rgba(226, 232, 240, .96) !important;
  background:
    radial-gradient(circle at top right, rgba(16, 185, 129, .08), transparent 34%),
    #ffffff !important;
  box-shadow: 0 24px 62px rgba(15, 23, 42, .075) !important;
}

.importsPremiumSurface :global(input),
.importsPremiumSurface :global(select),
.importsPremiumSurface :global(textarea) {
  border-radius: 18px !important;
  border: 1px solid #e2e8f0 !important;
  background: #ffffff !important;
  color: #0f172a !important;
  font-weight: 800;
}

.importsPremiumSurface :global(textarea) {
  min-height: 160px !important;
  line-height: 1.55;
  font-family: ui-monospace, SFMono-Regular, Menlo, Monaco, Consolas, "Liberation Mono", monospace;
}

.importsPremiumSurface :global(input:focus),
.importsPremiumSurface :global(select:focus),
.importsPremiumSurface :global(textarea:focus) {
  outline: none !important;
  border-color: #86efac !important;
  box-shadow: 0 0 0 4px rgba(16, 185, 129, .12) !important;
}

/* Buttons */
.importsPremiumSurface :global(button) {
  border-radius: 16px !important;
  font-weight: 950 !important;
  transition: transform .16s ease, box-shadow .16s ease, border-color .16s ease !important;
}

.importsPremiumSurface :global(button:hover) {
  transform: translateY(-2px);
}

.importsPremiumSurface :global(button:first-of-type),
.importsPremiumSurface .primaryButton,
.importsPremiumSurface .actionButton,
.importsPremiumSurface .submitButton {
  background: linear-gradient(135deg, #07111f 0%, #0f172a 48%, #065f46 100%) !important;
  color: #ffffff !important;
  border-color: #0f172a !important;
  box-shadow: 0 16px 34px rgba(16, 185, 129, .18) !important;
}

.importsPremiumSurface .secondaryButton,
.importsPremiumSurface .smallButton {
  background: #ffffff !important;
  border: 1px solid #e5e7eb !important;
  color: #0f172a !important;
  box-shadow: 0 10px 24px rgba(15, 23, 42, .055);
}

/* Empty states */
.importsPremiumSurface .empty,
.importsPremiumSurface .emptyState,
.importsPremiumSurface .noData,
.importsPremiumSurface :global([class*="empty"]),
.importsPremiumSurface :global([class*="Empty"]) {
  border: 1px dashed #cbd5e1 !important;
  border-radius: 25px !important;
  background:
    radial-gradient(circle at center, rgba(16, 185, 129, .08), transparent 55%),
    #ffffff !important;
  color: #64748b !important;
  min-height: 132px;
}

/* Modals above overhead panel */
.importsPremiumSurface .modalBackdrop,
.importsPremiumSurface :global([class*="modalBackdrop"]),
.importsPremiumSurface :global([class*="ModalBackdrop"]),
.importsPremiumSurface :global([class*="overlay"]),
.importsPremiumSurface :global([class*="Overlay"]) {
  z-index: 2147483000 !important;
  background:
    radial-gradient(circle at 50% 0%, rgba(16, 185, 129, .18), transparent 34%),
    rgba(15, 23, 42, .58) !important;
  backdrop-filter: blur(12px);
}

.importsPremiumSurface .modal,
.importsPremiumSurface .drawer,
.importsPremiumSurface :global([class*="modal"]),
.importsPremiumSurface :global([class*="Modal"]),
.importsPremiumSurface :global([class*="drawer"]),
.importsPremiumSurface :global([class*="Drawer"]) {
  z-index: 2147483001 !important;
  border: 1px solid rgba(226, 232, 240, .98) !important;
  background:
    radial-gradient(circle at top right, rgba(16, 185, 129, .10), transparent 28%),
    linear-gradient(180deg, #ffffff, #f8fafc) !important;
  box-shadow: 0 40px 110px rgba(15, 23, 42, .30) !important;
}

@media (max-width: 1200px) {
  .importsPremiumSurface .hero::after,
  .importsPremiumSurface .header::after,
  .importsPremiumSurface .topbar::after,
  .importsPremiumSurface .importHero::after,
  .importsPremiumSurface .commandHero::after {
    display: none;
  }
}

