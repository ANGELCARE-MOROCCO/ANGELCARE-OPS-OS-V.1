import { CareLinkMobileShell } from './CareLinkMobileShell'
import { carelinkMissions } from '@/lib/carelink/seed'
import { detectScheduleConflicts } from '@/lib/carelink/schedule-conflicts'
import { StatusPill } from '@/components/carelink/shared/CareLinkPrimitives'

export function CareLinkSchedule() {
  const conflicts = detectScheduleConflicts(carelinkMissions)
  return <CareLinkMobileShell><section className="space-y-5 px-5 py-6"><header className="rounded-[2rem] bg-white p-5 shadow-sm ring-1 ring-slate-200"><p className="text-[11px] font-black uppercase tracking-[0.35em] text-blue-600">Planning intelligent</p><h1 className="mt-2 text-3xl font-black text-slate-950">Agenda & disponibilité</h1><p className="mt-2 text-sm leading-6 text-slate-500">Vue terrain avec missions, buffers déplacement, disponibilité et conflits.</p></header><div className="grid grid-cols-5 gap-2">{['Lun','Mar','Mer','Jeu','Ven'].map((day,index)=><div key={day} className={`rounded-3xl p-3 text-center shadow-sm ring-1 ${index===1?'bg-blue-600 text-white ring-blue-600':'bg-white text-slate-600 ring-slate-200'}`}><p className="text-xs font-black">{day}</p><p className="mt-1 text-lg font-black">{9+index}</p></div>)}</div>{conflicts.length>0 && <section className="rounded-[2rem] bg-amber-50 p-5 ring-1 ring-amber-100"><h2 className="font-black text-amber-900">Alertes planning</h2>{conflicts.map((conflict)=><p key={conflict.message} className="mt-2 text-sm font-semibold text-amber-700">{conflict.message}</p>)}</section>}<section className="rounded-[2rem] border border-slate-200 bg-white p-5 shadow-sm"><h2 className="text-lg font-black text-slate-950">Timeline jour</h2><div className="mt-5 space-y-4">{carelinkMissions.map((mission)=><div key={mission.id} className="flex gap-4"><div className="w-16 text-sm font-black text-blue-600">{new Date(mission.scheduledStart).toLocaleTimeString('fr-FR',{hour:'2-digit',minute:'2-digit'})}</div><div className="flex-1 rounded-3xl bg-slate-50 p-4"><div className="flex items-center justify-between gap-2"><p className="text-sm font-black text-slate-950">{mission.serviceType}</p><StatusPill tone={mission.readinessStatus==='ready'?'green':'amber'}>{mission.zone}</StatusPill></div><p className="mt-1 text-xs font-semibold text-slate-500">{mission.clientLabel} · {mission.durationHours}H</p></div></div>)}</div></section><section className="rounded-[2rem] bg-emerald-50 p-5 ring-1 ring-emerald-100"><h2 className="font-black text-emerald-900">Disponibilité</h2><p className="mt-2 text-sm font-semibold text-emerald-700">Disponible aujourd’hui 08:00–19:30 · zones Agdal, Hay Riad, Souissi.</p></section></section></CareLinkMobileShell>
}
