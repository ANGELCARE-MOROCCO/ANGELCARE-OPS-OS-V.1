import AppShell,{PageAction} from '@/app/components/erp/AppShell'
import { getAcademyData, nameOf } from '../_data'
import { PageHeader, Card, DataTable, page } from '../_components/AcademyUI'
export default async function CalendarPage(){const data=await getAcademyData();return <AppShell title="Academy Calendar" subtitle="Training timeline from groups and attendance sessions." breadcrumbs={[{label:'Academy',href:'/academy'},{label:'Calendar'}]} actions={<PageAction href="/academy/locations-groups">Manage Groups</PageAction>}><div style={page}><PageHeader title="Operations Scheduling" subtitle="Phase 1 calendar is driven by group dates and attendance sessions. Phase 2 adds dedicated session creation."/><Card><h2>Group timeline</h2><DataTable headers={['Group','Course','Trainer','Start','End','Status']} rows={data.groups.map((g:any)=>[g.name,nameOf(data.courses,g.course_id),nameOf(data.trainers,g.trainer_id),g.start_date||'—',g.end_date||'—',g.status])}/></Card><Card><h2>Session history</h2><DataTable headers={['Date','Group','Trainee','Status']} rows={data.attendance.map((a:any)=>[a.session_date,nameOf(data.groups,a.group_id),nameOf(data.trainees,a.trainee_id),a.status])}/></Card></div></AppShell>}
