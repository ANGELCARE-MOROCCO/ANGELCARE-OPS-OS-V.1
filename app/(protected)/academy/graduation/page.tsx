import AppShell,{PageAction} from '@/app/components/erp/AppShell'
import { getAcademyData, nameOf } from '../_data'
import { createGraduationFollowup } from '../_actions'
import { PageHeader, Card, DataTable, Field, Input, Select, Textarea, Submit, Badge, page, formGrid } from '../_components/AcademyUI'
export default async function GraduationPage(){const data=await getAcademyData();return <AppShell title="Graduation" subtitle="Outcome engine: graduation, placement, upsell and post-training follow-up." breadcrumbs={[{label:'Academy',href:'/academy'},{label:'Graduation'}]} actions={<PageAction href="/academy/partners">Partners</PageAction>}><div style={page}><PageHeader title="Graduation & Outcome Engine" subtitle="Move certified trainees into employability, partner placement and post-graduation opportunities."/><Card><h2>Create outcome follow-up</h2><form action={createGraduationFollowup} style={formGrid}><Field label="Trainee"><Select name="trainee_id">{data.trainees.map((t:any)=><option key={t.id} value={t.id}>{t.full_name}</option>)}</Select></Field><Field label="Certificate"><Select name="certificate_id"><option value="">—</option>{data.certificates.map((c:any)=><option key={c.id} value={c.id}>{c.certificate_number}</option>)}</Select></Field><Field label="Partner"><Select name="partner_id"><option value="">—</option>{data.partners.map((p:any)=><option key={p.id} value={p.id}>{p.name}</option>)}</Select></Field><Field label="Opportunity"><Select name="opportunity_type"><option>placement</option><option>upsell</option><option>reactivation</option></Select></Field><Field label="Status"><Select name="status"><option>open</option><option>in_progress</option><option>won</option><option>lost</option></Select></Field><Field label="Next action"><Input name="next_action"/></Field><Field label="Next action at"><Input name="next_action_at" type="datetime-local"/></Field><Field label="Notes"><Textarea name="notes"/></Field><Submit>Create follow-up</Submit></form></Card><Card><h2>Outcome pipeline</h2><DataTable headers={['Trainee','Partner','Opportunity','Status','Next action']} rows={data.graduation_followups?.map((g:any)=>[nameOf(data.trainees,g.trainee_id),nameOf(data.partners,g.partner_id),g.opportunity_type,<Badge>{g.status}</Badge>,g.next_action||'—']) || []}/></Card></div></AppShell>}
