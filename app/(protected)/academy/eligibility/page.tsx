import AppShell,{PageAction} from '@/app/components/erp/AppShell'
import { getAcademyData } from '../_data'
import { validateEligibility } from '../_actions'
import { PageHeader, Card, DataTable, Field, Select, Textarea, Input, Submit, Badge, page, formGrid } from '../_components/AcademyUI'
import { ELIGIBILITY_STATUSES } from '../_lib/academyWorkflow'
export default async function EligibilityPage(){const data=await getAcademyData(); const queue=data.trainees.filter((t:any)=>t.eligibility_status!=='approved')
return <AppShell title="Eligibility" subtitle="Candidate approval gate and controlled progression." breadcrumbs={[{label:'Academy',href:'/academy'},{label:'Eligibility'}]} actions={<PageAction href="/academy/enrollments">Enrollments</PageAction>}><div style={page}><PageHeader title="Eligibility Control Gate" subtitle="Score, approve, reject or request information. Approved trainees become eligible for enrollment."/><Card><h2>Validate candidate</h2><form action={validateEligibility} style={formGrid}><Field label="Trainee"><Select name="trainee_id">{data.trainees.map((t:any)=><option key={t.id} value={t.id}>{t.full_name} · {t.serial_number}</option>)}</Select></Field><Field label="Decision"><Select name="eligibility_status">{ELIGIBILITY_STATUSES.map(s=><option key={s}>{s}</option>)}</Select></Field><Field label="Score /100"><Input name="score" type="number" min="0" max="100" defaultValue="70"/></Field><Field label="Evaluation note"><Textarea name="note"/></Field><Submit>Save eligibility decision</Submit></form></Card><Card><h2>Pending approval queue</h2><DataTable headers={['Candidate','Status','Score','Next']} rows={queue.map((t:any)=>[t.full_name,<Badge>{t.eligibility_status}</Badge>,t.eligibility_score||'—','Validate above'])}/></Card></div></AppShell>}
