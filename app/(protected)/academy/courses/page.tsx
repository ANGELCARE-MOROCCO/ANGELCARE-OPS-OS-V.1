import AppShell,{PageAction} from '@/app/components/erp/AppShell'
import { getAcademyData } from '../_data'
import { createCourse } from '../_actions'
import { PageHeader, Card, DataTable, Field, Input, Select, Textarea, Submit, Badge, page, formGrid } from '../_components/AcademyUI'
import { COURSE_STATUSES } from '../_lib/academyWorkflow'
export default async function CoursesPage(){const data=await getAcademyData();return <AppShell title="Courses" subtitle="Academy product engine: catalog, pricing, duration and lifecycle." breadcrumbs={[{label:'Academy',href:'/academy'},{label:'Courses'}]} actions={<PageAction href="/academy/locations-groups">Groups</PageAction>}><div style={page}><PageHeader title="Course Product Engine" subtitle="Create training products that can be attached to groups, enrollments and certificates."/><Card><h2>Create course</h2><form action={createCourse} style={formGrid}><Field label="Title"><Input name="title" required/></Field><Field label="Category"><Input name="category"/></Field><Field label="Level"><Input name="level"/></Field><Field label="Duration hours"><Input name="duration_hours" type="number"/></Field><Field label="Price MAD"><Input name="price" type="number"/></Field><Field label="Status"><Select name="status">{COURSE_STATUSES.map(s=><option key={s}>{s}</option>)}</Select></Field><Field label="Description"><Textarea name="description"/></Field><Submit>Create course</Submit></form></Card><Card><h2>Course catalog</h2><DataTable headers={['Course','Category','Level','Price','Status']} rows={data.courses.map((c:any)=>[c.title,c.category||'—',c.level||'—',`${Number(c.price||0).toLocaleString()} MAD`,<Badge>{c.status}</Badge>])}/></Card></div></AppShell>}
