import AppShell,{PageAction} from '@/app/components/erp/AppShell'
import { getAcademyData } from '../_data'
import { PageHeader, Card, DataTable, Kpi, page, grid3 } from '../_components/AcademyUI'
export default async function AdministrationPage(){const data=await getAcademyData();return <AppShell title="Academy Administration" subtitle="Governance, controls and audit traceability." breadcrumbs={[{label:'Academy',href:'/academy'},{label:'Administration'}]} actions={<PageAction href="/academy/reports">Reports</PageAction>}><div style={page}><PageHeader title="Governance & Compliance Control" subtitle="Monitor operational integrity, audit history, controls and data readiness for Academy management."/><div style={grid3}><Kpi label="Audit events" value={data.audit.length}/><Kpi label="Active alerts" value={data.alerts.filter((a:any)=>a.status!=='closed').length} tone="#dc2626"/><Kpi label="Core records" value={data.trainees.length+data.enrollments.length+data.payments.length}/></div><Card><h2>Recent audit trail</h2><DataTable headers={['Action','Entity','Record','Note','Date']} rows={data.audit.map((a:any)=>[a.action,a.entity,a.entity_id||'—',a.note||'—',a.created_at?new Date(a.created_at).toLocaleString('fr-FR'):'—'])}/></Card></div></AppShell>}
