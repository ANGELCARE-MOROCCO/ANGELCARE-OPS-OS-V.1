import AppShell,{PageAction} from '@/app/components/erp/AppShell'
import { getAcademyData, nameOf } from '../_data'
import { createAlert } from '../_actions'
import { PageHeader, Card, DataTable, Field, Input, Select, Textarea, Submit, Badge, page, formGrid } from '../_components/AcademyUI'
export default async function AlertsSalesPage(){const data=await getAcademyData();return <AppShell title="Alerts & Sales" subtitle="Follow-up engine for alerts, sales actions and after-sales control." breadcrumbs={[{label:'Academy',href:'/academy'},{label:'Alerts & Sales'}]} actions={<PageAction href="/academy">Command Center</PageAction>}><div style={page}><PageHeader title="Alerts, Reminders & Sales Follow-up" subtitle="Create actionable alerts linked to trainees. Use this as the light Academy CRM and exception follow-up board."/><Card><h2>Create alert / follow-up</h2><form action={createAlert} style={formGrid}><Field label="Title"><Input name="title" required/></Field><Field label="Trainee"><Select name="related_trainee_id"><option value="">—</option>{data.trainees.map((t:any)=><option key={t.id} value={t.id}>{t.full_name}</option>)}</Select></Field><Field label="Severity"><Select name="severity"><option>normal</option><option>high</option><option>critical</option></Select></Field><Field label="Status"><Select name="status"><option>open</option><option>in_progress</option><option>closed</option></Select></Field><Field label="Due at"><Input name="due_at" type="datetime-local"/></Field><Field label="Message"><Textarea name="message"/></Field><Submit>Create alert</Submit></form></Card><Card><h2>Alert center</h2><DataTable headers={['Alert','Trainee','Severity','Status','Due']} rows={data.alerts.map((a:any)=>[a.title,nameOf(data.trainees,a.related_trainee_id),<Badge>{a.severity}</Badge>,<Badge>{a.status}</Badge>,a.due_at?new Date(a.due_at).toLocaleString('fr-FR'):'—'])}/></Card></div></AppShell>}
