import AppShell, { PageAction } from '@/app/components/erp/AppShell'; import { createClient } from '@/lib/supabase/server'; import { requireRole } from '@/lib/auth/session'; import { ActionLink, EmptyState, KpiCard, Panel, WorkspaceHero, formatCurrency } from '../_components/RevenueOpsPrimitives'
export default async function BusinessDevelopmentWorkspace(){await requireRole(['ceo','manager','agent']); const supabase=await createClient(); const [{data:prospects},{data:campaigns},{data:appointments},{data:partnerships}]=await Promise.all([supabase.from('bd_prospects').select('*').eq('is_archived',false), supabase.from('bd_campaigns').select('*').eq('is_archived',false), supabase.from('bd_appointments').select('*').eq('is_archived',false), supabase.from('bd_partnerships').select('*').eq('is_archived',false)]); const ps=prospects||[], cs=campaigns||[], ap=appointments||[], pts=partnerships||[]; const forecast=ps.reduce((s:number,p:any)=>s+Number(p.estimated_value||0),0)+pts.reduce((s:number,p:any)=>s+Number(p.estimated_value||0),0); return <AppShell title="Business Development Workspace" subtitle="Hub corporate B2C/B2B, partenariats, campagnes, rendez-vous et mapping marché." breadcrumbs={[{label:'Revenue',href:'/revenue-command-center'},{label:'BD Workspace'}]} actions={<><PageAction href="/revenue-command-center/cockpit">Cockpit</PageAction><PageAction href="/revenue-command-center/prospects" variant="light">Prospects</PageAction></>}><div style={{display:'grid',gap:20}}><WorkspaceHero eyebrow="MARKET DOMINATION" title="BD Operating Workspace" subtitle="Espace de travail multidimensionnel pour cartographier le marché, organiser les bases prospects, piloter les campagnes, suivre les rendez-vous et convertir les opportunités en contrats." right={<strong>{formatCurrency(forecast)}</strong>}/><section style={metricsStyle}><KpiCard label="Prospects" value={ps.length}/><KpiCard label="Campagnes" value={cs.length} tone="purple"/><KpiCard label="RDV" value={ap.length} tone="green"/><KpiCard label="Partenariats" value={pts.length} tone="amber"/></section><section style={gridStyle}><Panel title="Navigation cockpit" subtitle="Accès direct aux workspaces."><div style={navStyle}><ActionLink href="/revenue-command-center/prospects">Prospects</ActionLink><ActionLink href="/revenue-command-center/campaigns">Campagnes</ActionLink><ActionLink href="/revenue-command-center/appointments">RDV</ActionLink><ActionLink href="/revenue-command-center/partnerships">Partenariats</ActionLink><ActionLink href="/revenue-command-center/tasks">Tasks</ActionLink><ActionLink href="/revenue-command-center/market-mapping">Market Mapping</ActionLink></div></Panel><Panel title="Recent opportunities" subtitle="Prospects récents.">{ps.length?<div style={{display:'grid',gap:10}}>{ps.slice(0,8).map((p:any)=><ActionLink key={p.id} href={`/revenue-command-center/prospects/${p.id}`} tone="light">{p.name}</ActionLink>)}</div>:<EmptyState text="Aucun prospect."/>}</Panel></section></div></AppShell>}
const metricsStyle:React.CSSProperties={display:'grid',gridTemplateColumns:'repeat(4,minmax(0,1fr))',gap:14}.valueOf(); const gridStyle:React.CSSProperties={display:'grid',gridTemplateColumns:'1fr 1fr',gap:18}.valueOf(); const navStyle:React.CSSProperties={display:'grid',gridTemplateColumns:'repeat(2,minmax(0,1fr))',gap:10}.valueOf()
