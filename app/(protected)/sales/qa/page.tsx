import AppShell,{PageAction} from '@/app/components/erp/AppShell'
import { ERPPanel,StatusPill } from '@/app/components/erp/ERPPrimitives'
export default function QA(){const checks=['/api/sales-terminal/options must return options','/api/sales-terminal/clients must create/list clients','/api/sales-terminal/orders must create/list/update orders','/api/sales-terminal/documents must create quote/invoice/delivery','/api/sales-terminal/print?id=DOCUMENT_ID must print/export PDF','/sales/clients must not be white/empty','/sales/configuration Save Option must update dropdowns'];return <AppShell title='Sales QA' subtitle='Manual verification checklist for controlled production.' breadcrumbs={[{label:'Sales',href:'/sales'},{label:'QA'}]} actions={<PageAction href='/sales'>Back to Sales</PageAction>}><ERPPanel title='Required checks' subtitle='Run these after SQL + install.'><div style={{display:'grid',gap:10}}>{checks.map((c,i)=><div key={c} style={{display:'flex',gap:12,alignItems:'center',border:'1px solid #e2e8f0',borderRadius:14,padding:12}}><StatusPill tone='green'>{i+1}</StatusPill><b>{c}</b></div>)}</div></ERPPanel></AppShell>}
