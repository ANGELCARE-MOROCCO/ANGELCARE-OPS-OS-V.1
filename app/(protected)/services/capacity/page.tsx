import AppShell, { PageAction } from '@/app/components/erp/AppShell'
import { ServiceOSCard, ServiceOSGrid, ServiceOSPanel, ServiceOSPill } from '@/components/service-os/ServiceOSPrimitives'
import { getCityDeploymentMatrix } from '@/lib/service-os/expansion-engine'
export default function CapacityPage(){const rows=getCityDeploymentMatrix(); return <AppShell title="Service Capacity Management" subtitle="Capacité, pool staff, risque et demande par ville/service." breadcrumbs={[{label:'Services',href:'/services'},{label:'Capacity'}]} actions={<PageAction href="/services/expansion" variant="light">Expansion</PageAction>}><ServiceOSPanel title="Capacity Matrix" subtitle="La croissance doit suivre la capacité réelle."><ServiceOSGrid>{rows.map(r=><ServiceOSCard key={`${r.serviceCode}-${r.city}`} title={`${r.serviceCode} • ${r.city}`} subtitle={r.serviceName}><ServiceOSPill tone={r.capacity>70?'green':r.capacity>45?'amber':'red'}>Capacity {r.capacity}%</ServiceOSPill><p style={{fontSize:13,color:'#475569'}}>Staff pool: {r.staffPool} • Demand: {r.demandScore} • Risk: {r.riskScore}</p></ServiceOSCard>)}</ServiceOSGrid></ServiceOSPanel></AppShell>}
