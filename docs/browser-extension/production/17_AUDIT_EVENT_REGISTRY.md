# Audit Event Registry

Core events include pairing, refresh, access change, command prepared/executed/failed, approval required, device revocation, production health degraded, production admin action, production admin failure, kill switch, release promotion, rollback and incident transitions. Logs use correlation/entity references and exclude sensitive page content.
