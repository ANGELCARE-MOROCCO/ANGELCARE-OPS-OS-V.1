# Adapter Maintenance Guide

Each adapter records selector version, last success/failure, consecutive failures and error fingerprint. Test Google Maps, Gmail, WhatsApp Web, generic website and ANGELCARE SaaS adapters after browser/platform changes. Disable only the affected adapter; do not crash the extension. Never evade platform restrictions or scrape full private conversations.
