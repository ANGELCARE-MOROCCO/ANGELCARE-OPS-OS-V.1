# Installation MZ13

Run the surgical installer with the monorepo path and ZIP. Apply SQL separately using `apply_revenue_os_mz13_database.sh`. Run `npm run revenue-os:phase13:typecheck` and the packaged smoke script. No production build, Git staging or automatic SQL is performed by the installer.
