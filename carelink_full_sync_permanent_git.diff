--- a/app/api/missions/dossiers/[id]/route.ts
+++ b/app/api/missions/dossiers/[id]/route.ts
@@ -3,6 +3,7 @@
 import { getMissionDossier, patchMission } from '@/lib/missions/repository'
 import { patchMissionOrder } from '@/lib/missions/mission-order'
 import { recordMissionEvent } from '@/lib/missions/events'
+import { resolvedSessionCode, stripMissionCodeSuffix } from '@/lib/missions/mission-codes'
 
 export const dynamic = 'force-dynamic'
 
@@ -26,7 +27,7 @@
 function stripMissionSuffix(value: unknown) {
   const text = String(value || '').trim()
   if (!text) return ''
-  return text.replace(/\*(S|\d+)$/i, '')
+  return stripMissionCodeSuffix(text)
 }
 
 function buildDossierCode(parentMission: any, parentMissionId: number) {
@@ -39,7 +40,7 @@
 }
 
 function sessionCode(baseCode: string, index: number, total: number) {
-  return total <= 1 ? `${baseCode}*S` : `${baseCode}*${index + 1}`
+  return resolvedSessionCode(baseCode, index, total)
 }
 
 function normalizeSessions(body: any) {
@@ -67,9 +68,9 @@
   baseCode: string
 }) {
   const supabase = await createClient()
-  const { parentMissionId, parentMission, requestedId, body, sessions: sessionsWithCodes, familyId, caregiverId, baseCode } = params
+  const { parentMissionId, parentMission, requestedId, body, sessions, familyId, caregiverId, baseCode } = params
   const now = new Date().toISOString()
-  const total = sessionsWithCodes.length
+  const total = sessions.length
 
   const { data: existingChildren, error: childrenError } = await supabase
     .from('missions')
@@ -392,7 +393,7 @@
 
     const sessionsWithCodes = sessions.map((session, index) => {
       const linkedMission = linkedMissions[index]
-      const code = sessionCode(baseCode, index, sessionsWithCodes.length)
+      const code = sessionCode(baseCode, index, sessions.length)
       return {
         ...session,
         code,
@@ -434,7 +435,7 @@
       programLines: activities,
       allowanceRows: allowances,
       mobileBrief: {
-        sessions,
+        sessions: sessionsWithCodes,
         routes,
         allowances,
         program: activities,
@@ -528,7 +529,7 @@
         language: body.language || null,
         requiredSkills: Array.isArray(body.requiredSkills) ? body.requiredSkills : [],
       },
-      sessions,
+      sessions: sessionsWithCodes,
       routes,
       allowances,
       program: activities,
--- a/app/api/missions/dossiers/route.ts
+++ b/app/api/missions/dossiers/route.ts
@@ -2,6 +2,7 @@
 import { createClient } from '@/lib/supabase/server'
 import { distributeMissionDossier } from '@/lib/missions/dossier-distribution'
 
+import { resolvedMissionCode } from '@/lib/missions/mission-codes'
 export const dynamic = 'force-dynamic'
 export const revalidate = 0
 
@@ -76,7 +77,9 @@
 
     const missions = (data || []).map((row: any) => ({
       ...row,
-      code: row.mission_reference || row.dossier_reference || `M-${row.id}`,
+      code: resolvedMissionCode(row),
+      missionCode: resolvedMissionCode(row),
+      mission_reference: resolvedMissionCode(row),
       title: row.service_type || row.designation || 'Mission dossier',
       serviceType: row.service_type || row.designation || 'Mission dossier',
       familyName: row.client_name || `Family #${row.family_id || '—'}`,
--- a/components/carelink/ops/CareLinkOpsLiveBridgePanel.tsx
+++ b/components/carelink/ops/CareLinkOpsLiveBridgePanel.tsx
@@ -3,6 +3,7 @@
 import { useEffect, useMemo, useState } from 'react'
 import { RefreshCw, Layers3, UserCheck, AlertTriangle, ClipboardCheck, Radio, ArrowRight } from 'lucide-react'
 import Link from 'next/link'
+import { resolvedMissionCode } from '@/lib/missions/mission-codes'
 
 type AnyRecord = Record<string, any>
 
@@ -15,7 +16,7 @@
 }
 
 function codeOf(row: AnyRecord) {
-  return text(row.code || row.reference || row.mission_reference || row.dossier_reference || `M-${row.id}`)
+  return resolvedMissionCode(row)
 }
 
 function clientOf(row: AnyRecord) {
--- a/lib/carelink/ops-dispatch-repository.ts
+++ b/lib/carelink/ops-dispatch-repository.ts
@@ -14,6 +14,7 @@
   DispatchSector,
 } from './ops-dispatch-types'
 
+import { resolvedMissionCode } from '@/lib/missions/mission-codes'
 const TABLES = {
   missions: 'missions',
   agents: 'caregivers',
@@ -298,7 +299,7 @@
   const status = mapLifecycleToDispatchStatus(String(mission.status || mission.lifecycleStage || mission.lifecycle_stage || 'draft'), riskLevel, readinessStatus, reportStatus)
   return {
     id: String(mission.id),
-    mission_code: String(mission.code || mission.dossier_reference || mission.mission_reference || `MISSION-${mission.id}`),
+    mission_code: resolvedMissionCode(mission),
     status,
     service_type: mission.serviceType ? String(mission.serviceType) : mission.service_type ? String(mission.service_type) : null,
     client_name: mission.familyName ? String(mission.familyName) : mission.client_name ? String(mission.client_name) : null,
--- a/lib/carelink/ops-enterprise.ts
+++ b/lib/carelink/ops-enterprise.ts
@@ -22,6 +22,7 @@
   completeMissionChecklist,
 } from './mobile-persistence'
 
+import { resolvedMissionCode } from '@/lib/missions/mission-codes'
 type AnyRecord = Record<string, any>
 
 export type OpsEnterpriseSnapshot = {
@@ -448,7 +449,7 @@
   const progress = missionChecklistProgress(checklistItems)
   return {
     id: Number(record.id),
-    code: asString(record.code || record.mission_code || record.dossier_reference || `MISSION-${record.id}`),
+    code: resolvedMissionCode(record),
     status: asString(record.status || 'draft'),
     lifecycleStage: missionStatus(record),
     missionKind: asString(record.missionKind || record.mission_kind || 'single'),
--- a/lib/carelink/ops-live-missions-bridge.ts
+++ b/lib/carelink/ops-live-missions-bridge.ts
@@ -1,6 +1,6 @@
 import { createClient } from '@supabase/supabase-js'
-import { resolvedMissionCode } from '@/lib/missions/mission-codes'
 
+import { resolvedMissionCode } from '@/lib/missions/mission-codes'
 type AnyRecord = Record<string, any>
 
 function env(name: string) {
@@ -39,7 +39,7 @@
 }
 
 function missionCode(row: AnyRecord) {
-  return row.mission_reference || row.dossier_reference || `M-${row.id}`
+  return resolvedMissionCode(row)
 }
 
 function normalizeMission(row: AnyRecord) {
@@ -55,6 +55,9 @@
     missionId: row.id,
     mission_id: row.id,
     code: missionCode(row),
+    missionCode: missionCode(row),
+    mission_code: missionCode(row),
+    mission_reference: missionCode(row),
     reference: missionCode(row),
     title: row.service_type || row.designation || 'Mission dossier',
     serviceType: row.service_type || row.designation || 'Mission dossier',
--- a/lib/carelink/server.ts
+++ b/lib/carelink/server.ts
@@ -2,6 +2,7 @@
 import { createClient } from '@/lib/supabase/server'
 import type { CareLinkMission, CareLinkStatus } from './types'
 
+import { resolvedMissionCode } from '@/lib/missions/mission-codes'
 const seedAgent: any = null
 const seedMessages: any[] = []
 const seedMissions: any[] = []
@@ -173,7 +174,7 @@
 function mapMissionRow(row: any): CareLinkMission {
   return {
     id: String(row.id),
-    code: row.mission_code || row.code || `AC-MISSION-${row.id}`,
+    code: resolvedMissionCode(row),
     serviceType: row.service_type || 'MISSION TERRAIN ANGELCARE',
     serviceCategory: (row.service_category || 'home_support') as CareLinkMission['serviceCategory'],
     clientName: row.client_name || 'CLIENT PRIVÉ',
--- a/lib/missions/dossier-distribution.ts
+++ b/lib/missions/dossier-distribution.ts
@@ -6,6 +6,7 @@
 import { getMissionDossier } from './repository'
 import { patchMissionOrder } from './mission-order'
 import { recordMissionEvent } from './events'
+import { resolvedMissionCode } from './mission-codes'
 
 function isUuid(value: unknown): value is string {
   return typeof value === 'string' && /^[0-9a-f]{8}-[0-9a-f]{4}-[1-5][0-9a-f]{3}-[89ab][0-9a-f]{3}-[0-9a-f]{12}$/i.test(value)
@@ -132,7 +133,7 @@
   const service = getServiceCharacteristic(order.serviceType)
   return {
     missionId: mission.id ? Number(mission.id) : null,
-    reference: dossier?.mission.code || mission.dossier_reference || buildDossierReference(String(mission.mission_group_id || ''), Number(mission.id || 0)),
+    reference: dossier?.mission?.code || resolvedMissionCode(mission as any) || buildDossierReference(String(mission.mission_group_id || ''), Number(mission.id || 0)),
     client: {
       familyId: order.familyId,
       label: dossier?.mission.familyName || 'Famille liée',
@@ -229,6 +230,10 @@
     notes: session.notes || null,
   })))
 
+  const syncedDossier = await getMissionDossier(missionId).catch(() => dossier)
+  const syncedMission = (syncedDossier?.raw || syncedDossier?.mission || parent) as DistributionRow
+  const missionDisplayCode = syncedDossier?.mission?.code || resolvedMissionCode(syncedMission as any)
+
   const routeRows = (input.routes || []).map((row, index) => ({
     sort_order: index,
     route_type: row.type || 'caregiver_travel',
@@ -297,7 +302,7 @@
     client_profile: input.parameters?.client_profile ?? null,
     dossier_number: input.parameters?.dossier_number ?? null,
     designation: input.parameters?.designation ?? input.serviceType,
-    client_name: input.parameters?.client_name ?? dossier?.mission.familyName ?? null,
+    client_name: input.parameters?.client_name ?? syncedDossier?.mission?.familyName ?? dossier?.mission.familyName ?? null,
     client_address: clientAddress,
     client_city: input.parameters?.client_city ?? input.city ?? null,
     mission_reason: input.parameters?.mission_reason ?? input.notes ?? `Dossier distributé pour ${input.serviceType}`,
@@ -329,7 +334,7 @@
   const order = await patchMissionOrder(missionId, missionOrderPatch)
 
   const checklistItems = await loadMissionChecklist(missionId, input.serviceType, input.caregiverId ?? null)
-  const mobileBrief = buildMobileBrief(parent, dossier, checklistItems, sessions, input)
+  const mobileBrief = buildMobileBrief(syncedMission, syncedDossier || dossier, checklistItems, sessions, input)
   const reportDraft = await upsertDraftReport(missionId, input.serviceType, input.caregiverId ?? null, mobileBrief, input.activities || [], checklistItems)
 
   const createdNotifications = []
@@ -338,7 +343,7 @@
     if (!(existingNotification as any).data?.id) {
       const notification = await createNotification({
         type: 'mission_assigned',
-        title: `Nouvelle mission · ${dossier?.mission.code || parent.dossier_reference || `Mission #${missionId}`}`,
+        title: `Nouvelle mission · ${missionDisplayCode || `Mission #${missionId}`}`,
         body: `Le dossier a été distribué pour ${input.serviceType}.`,
         priority: input.riskLevel && ['high', 'critical'].includes(String(input.riskLevel).toLowerCase()) ? 'critical' : 'high',
         missionId,
@@ -356,7 +361,7 @@
     if (!(existingAlert as any).data?.id) {
       const alert = await createAlert({
         type: 'mission_risk',
-        title: `Risque dossier · ${dossier?.mission.code || parent.dossier_reference || `Mission #${missionId}`}`,
+        title: `Risque dossier · ${missionDisplayCode || `Mission #${missionId}`}`,
         body: input.backupCaregiverId ? 'Le dossier comporte une alerte de risque à suivre.' : 'Aucun caregiver de secours n’a été défini.',
         priority: 'critical',
         missionId,
@@ -379,7 +384,7 @@
         senderType: 'ops',
         senderId: 'carelink_ops',
         recipientType: 'caregiver',
-        subject: `Mission assignée · ${dossier?.mission.code || parent.dossier_reference || `Mission #${missionId}`}`,
+        subject: `Mission assignée · ${missionDisplayCode || `Mission #${missionId}`}`,
         body: `Le dossier a été distribué. Consultez le détail, la liste de contrôle et le programme de mission.`,
         priority: 'high',
         status: 'sent',
@@ -434,7 +439,7 @@
 
   return {
     mission: finalDossier?.mission || null,
-    parent,
+    parent: syncedMission,
     dossier: finalDossier,
     subMissions,
     order,
@@ -447,4 +452,4 @@
     groupId,
     allowanceTotal,
   }
-}
\ No newline at end of file
+}
--- a/lib/missions/dossiers.ts
+++ b/lib/missions/dossiers.ts
@@ -1,6 +1,7 @@
 import { createClient } from '@/lib/supabase/server'
 import { recordMissionEvent } from './events'
 import type { MissionRow } from './types'
+import { resolvedMissionCode, stripMissionCodeSuffix } from './mission-codes'
 
 export type CreateDossierInput = {
   requestId?: string | null
@@ -26,6 +27,18 @@
   recurrenceEndDate?: string | null
 }
 
+
+function dossierBaseCode(row: Record<string, any> | null | undefined, fallbackId?: number | string | null) {
+  const base = stripMissionCodeSuffix(row?.dossier_reference || row?.mission_reference || row?.code || '')
+  if (base) return base
+  const id = row?.id ?? fallbackId
+  return id ? `CARE-${id}` : `CARE-${Date.now()}`
+}
+
+function dossierSessionCode(baseCode: string, index: number, total: number) {
+  return total <= 1 ? `${baseCode}*S` : `${baseCode}*${index + 1}`
+}
+
 export async function createMissionDossier(input: CreateDossierInput): Promise<MissionRow> {
   const supabase = await createClient()
   const groupId = input.requestId || crypto.randomUUID()
@@ -45,7 +58,7 @@
     recurrence_start_date: input.recurrenceStartDate || input.missionDate || null,
     recurrence_end_date: input.recurrenceEndDate || null,
     dossier_status: 'active',
-    dossier_reference: `DOS-${groupId.slice(0, 8).toUpperCase()}`,
+    dossier_reference: input.recurrenceRule?.dossierCode ? String(input.recurrenceRule.dossierCode) : `DRAFT-${groupId.slice(0, 8).toUpperCase()}`,
     city: input.city || null,
     zone: input.zone || null,
     notes: input.notes || null,
@@ -65,20 +78,186 @@
   if (existing.data?.id) {
     const { data, error } = await supabase.from('missions').update(payload).eq('id', existing.data.id).select('*').single()
     if (error) throw new Error(error.message)
-    await recordMissionEvent({ missionId: Number(data.id), eventType: 'mission_dossier_updated', content: 'Mission dossier updated', metadata: { mission_group_id: groupId, backup_caregiver_id: input.backupCaregiverId || null }, source: 'mission_dossier_engine' })
-    return data as MissionRow
+    const baseCode = dossierBaseCode(data, Number(data.id))
+    const { data: coded, error: codeError } = await supabase
+      .from('missions')
+      .update({ dossier_reference: baseCode, mission_reference: baseCode, updated_at: new Date().toISOString() })
+      .eq('id', Number(data.id))
+      .select('*')
+      .single()
+    if (codeError) throw new Error(codeError.message)
+    await recordMissionEvent({ missionId: Number(coded.id), eventType: 'mission_dossier_updated', content: 'Mission dossier updated', metadata: { mission_group_id: groupId, backup_caregiver_id: input.backupCaregiverId || null, dossierCode: baseCode }, source: 'mission_dossier_engine' })
+    return coded as MissionRow
   }
 
   const { data, error } = await supabase.from('missions').insert([payload]).select('*').single()
   if (error) throw new Error(error.message)
-  await recordMissionEvent({ missionId: Number(data.id), eventType: 'mission_dossier_created', content: 'Mission dossier created', metadata: { mission_group_id: groupId, backup_caregiver_id: input.backupCaregiverId || null }, source: 'mission_dossier_engine' })
-  return data as MissionRow
+  const baseCode = dossierBaseCode(data, Number(data.id))
+  const { data: coded, error: codeError } = await supabase
+    .from('missions')
+    .update({ dossier_reference: baseCode, mission_reference: baseCode, updated_at: new Date().toISOString() })
+    .eq('id', Number(data.id))
+    .select('*')
+    .single()
+  if (codeError) throw new Error(codeError.message)
+  await recordMissionEvent({ missionId: Number(coded.id), eventType: 'mission_dossier_created', content: 'Mission dossier created', metadata: { mission_group_id: groupId, backup_caregiver_id: input.backupCaregiverId || null, dossierCode: baseCode }, source: 'mission_dossier_engine' })
+  return coded as MissionRow
 }
 
 export async function generateSubMissions(parentId: number, occurrences: Array<{ missionDate: string; startTime?: string | null; endTime?: string | null; caregiverId?: number | null; notes?: string | null }>): Promise<MissionRow[]> {
   const supabase = await createClient()
   const { data: parent, error: parentError } = await supabase.from('missions').select('*').eq('id', parentId).maybeSingle()
   if (parentError) throw new Error(parentError.message)
+  if (!parent) throw new Error('Parent mission dossier not found')
+
+  const missionGroupId = parent.mission_group_id || crypto.randomUUID()
+  const total = Math.max(occurrences.length, 1)
+  const baseCode = dossierBaseCode(parent, parentId)
+  const now = new Date().toISOString()
+
+  const { data: existingRows, error: existingError } = await supabase
+    .from('missions')
+    .select('*')
+    .eq('parent_mission_id', parentId)
+  if (existingError) throw new Error(existingError.message)
+
+  const existingByIndex = new Map<number, MissionRow>()
+  ;((existingRows || []) as MissionRow[]).forEach((row) => {
+    const index = Number((row as any).occurrence_index || 0)
+    if (index) existingByIndex.set(index, row)
+  })
+
+  if (total <= 1) {
+    const occurrence = occurrences[0]
+    const singleCode = dossierSessionCode(baseCode, 0, total)
+    const { data, error } = await supabase
+      .from('missions')
+      .update({
+        parent_mission_id: null,
+        mission_group_id: missionGroupId,
+        mission_kind: 'single',
+        occurrence_index: 1,
+        dossier_reference: baseCode,
+        mission_reference: singleCode,
+        mission_date: occurrence?.missionDate || parent.mission_date || null,
+        start_time: occurrence?.startTime || parent.start_time || null,
+        end_time: occurrence?.endTime || parent.end_time || null,
+        caregiver_id: occurrence?.caregiverId ?? parent.caregiver_id ?? null,
+        status: occurrence?.caregiverId || parent.caregiver_id ? 'assigned' : (parent.status || 'draft'),
+        lifecycle_stage: occurrence?.caregiverId || parent.caregiver_id ? 'assigned' : (parent.lifecycle_stage || 'ready_for_assignment'),
+        recurrence_rule: {
+          ...(typeof parent.recurrence_rule === 'object' && parent.recurrence_rule ? parent.recurrence_rule : {}),
+          dossierCode: baseCode,
+          singleMissionCode: singleCode,
+          linkedMissionCodes: [singleCode],
+          linkedMissionCount: 1,
+          syncedAt: now,
+        },
+        updated_at: now,
+      })
+      .eq('id', parentId)
+      .select('*')
+      .single()
+    if (error) throw new Error(error.message)
+
+    const staleIds = ((existingRows || []) as MissionRow[]).map((row) => Number(row.id)).filter(Number.isFinite)
+    if (staleIds.length) {
+      const { error: deleteError } = await supabase.from('missions').delete().in('id', staleIds)
+      if (deleteError) throw new Error(deleteError.message)
+    }
+
+    await recordMissionEvent({ missionId: parentId, eventType: 'sub_missions_synced', content: 'Single mission code synced', metadata: { count: 1, dossierCode: baseCode, missionCode: singleCode }, source: 'mission_dossier_engine' })
+    return []
+  }
+
+  const linkedCodes = occurrences.map((_, index) => dossierSessionCode(baseCode, index, total))
+  const { error: parentUpdateError } = await supabase
+    .from('missions')
+    .update({
+      parent_mission_id: null,
+      mission_group_id: missionGroupId,
+      mission_kind: 'dossier',
+      occurrence_index: null,
+      dossier_reference: baseCode,
+      mission_reference: baseCode,
+      recurrence_rule: {
+        ...(typeof parent.recurrence_rule === 'object' && parent.recurrence_rule ? parent.recurrence_rule : {}),
+        dossierCode: baseCode,
+        linkedMissionCodes: linkedCodes,
+        linkedMissionCount: total,
+        syncedAt: now,
+      },
+      updated_at: now,
+    })
+    .eq('id', parentId)
+  if (parentUpdateError) throw new Error(parentUpdateError.message)
+
+  const created: MissionRow[] = []
+  for (const [index, item] of occurrences.entries()) {
+    const occurrenceIndex = index + 1
+    const missionCode = dossierSessionCode(baseCode, index, total)
+    const payload = {
+      parent_mission_id: parentId,
+      mission_group_id: missionGroupId,
+      mission_kind: 'sub_mission',
+      occurrence_index: occurrenceIndex,
+      dossier_reference: baseCode,
+      mission_reference: missionCode,
+      family_id: parent.family_id,
+      caregiver_id: item.caregiverId ?? parent.caregiver_id ?? null,
+      contract_id: parent.contract_id || null,
+      service_type: parent.service_type,
+      service_family: parent.service_family || null,
+      mission_scope: parent.mission_scope || null,
+      internal_procedure_level: parent.internal_procedure_level || null,
+      mission_date: item.missionDate,
+      start_time: item.startTime || parent.start_time || null,
+      end_time: item.endTime || parent.end_time || null,
+      city: parent.city || null,
+      zone: parent.zone || null,
+      status: item.caregiverId || parent.caregiver_id ? 'assigned' : 'draft',
+      lifecycle_stage: item.caregiverId || parent.caregiver_id ? 'assigned' : 'ready_for_assignment',
+      readiness_status: 'pending',
+      validation_status: 'pending',
+      report_status: 'pending',
+      notes: item.notes || null,
+      recurrence_rule: {
+        source: 'mission_dossier_engine',
+        parentMissionId: parentId,
+        dossierCode: baseCode,
+        missionCode,
+        occurrenceIndex,
+        occurrenceTotal: total,
+        syncedAt: now,
+      },
+      updated_at: now,
+    }
+
+    const existing = existingByIndex.get(occurrenceIndex)
+    if (existing?.id) {
+      const { data, error } = await supabase.from('missions').update(payload).eq('id', existing.id).select('*').single()
+      if (error) throw new Error(error.message)
+      created.push(data as MissionRow)
+      continue
+    }
+
+    const { data, error } = await supabase.from('missions').insert([payload]).select('*').single()
+    if (error) throw new Error(error.message)
+    created.push(data as MissionRow)
+  }
+
+  const staleRows = Array.from(existingByIndex.entries()).filter(([index]) => index > occurrences.length).map(([, row]) => row)
+  if (staleRows.length) {
+    const { error } = await supabase.from('missions').delete().in('id', staleRows.map((row) => row.id))
+    if (error) throw new Error(error.message)
+  }
+
+  await recordMissionEvent({ missionId: parentId, eventType: 'sub_missions_synced', content: `${created.length} linked sub-missions synced`, metadata: { count: created.length, dossierCode: baseCode, linkedCodes }, source: 'mission_dossier_engine' })
+  return created
+}>): Promise<MissionRow[]> {
+  const supabase = await createClient()
+  const { data: parent, error: parentError } = await supabase.from('missions').select('*').eq('id', parentId).maybeSingle()
+  if (parentError) throw new Error(parentError.message)
   if (!parent) throw new Error('Parent mission dossier not found')
   const missionGroupId = parent.mission_group_id || crypto.randomUUID()
   const { data: existingRows, error: existingError } = await supabase.from('missions').select('*').eq('parent_mission_id', parentId)
--- a/lib/missions/repository.ts
+++ b/lib/missions/repository.ts
@@ -1,8 +1,8 @@
 import { createClient } from '@/lib/supabase/server'
 import type { MissionControlRecord, MissionDossier, MissionEvent, MissionRow } from './types'
 import { getServiceCharacteristic } from './service-characteristics'
-import { resolvedMissionCode } from '@/lib/missions/mission-codes'
 
+import { resolvedMissionCode } from '@/lib/missions/mission-codes'
 function text(value: unknown, fallback = '—'): string {
   return typeof value === 'string' && value.trim() ? value : fallback
 }
@@ -20,7 +20,7 @@
   const upcoming = children.filter((child) => child.status !== 'completed' && child.status !== 'cancelled').length
   return {
     id: Number(row.id),
-    code: (row as any).dossier_reference || (row as any).mission_reference || `MISSION-${row.id}`,
+    code: resolvedMissionCode(row as any, { total: children.length }),
     title: missionKind === 'dossier' ? `Dossier · ${serviceType}` : serviceType,
     missionKind,
     status: text(row.status, 'draft'),
