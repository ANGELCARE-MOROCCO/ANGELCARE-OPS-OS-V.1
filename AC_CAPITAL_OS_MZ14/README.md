# AC CAPITAL OS — Mega ZIP 14
## Capital Command Institution Universe UX

Transforms AC CAPITAL OS from flat/static dashboard into a premium clickable, modal-driven, workflow-first capital command universe.

## Apply

```bash
cd ~/Desktop/angelcare-platform

unzip -o AC_CAPITAL_OS_MEGA_ZIP_14_CAPITAL_COMMAND_UNIVERSE_UX.zip -d .

node ./AC_CAPITAL_OS_MZ14/scripts/apply_ac_capital_os_mz14.mjs
node ./AC_CAPITAL_OS_MZ14/scripts/verify_ac_capital_os_mz14.mjs
```

## TypeScript

```bash
cd ~/Desktop/angelcare-platform/apps/ops-web
npx tsc -p tsconfig.json --noEmit --pretty false
```

No SQL. No backend API deletion. No live AI. No automatic email. No automatic submission.
