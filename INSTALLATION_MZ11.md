# Installation MZ11

Run the surgical installer from the monorepo root handoff. Apply SQL manually with the Session Pooler through `apply_revenue_os_mz11_database.sh`. Run targeted verification, tests and TypeScript. Do not run a production build on the user machine.
